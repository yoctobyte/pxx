unit graphics;

{ PCL Graphics compatibility stub. The real unit defines TColor, TCanvas, TFont,
  TBitmap etc.; the minimal helloworld references none of them directly, so this
  is an empty stub that only needs to satisfy `uses`. Grow as widgets gain
  drawing surfaces. }

interface

implementation

end.
