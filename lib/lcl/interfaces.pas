unit interfaces;

{ LCL widgetset selector. In real Lazarus this unit's initialization picks and
  installs the GTK/Qt/Win32 widgetset. Here the widgetset is wired directly in
  forms/controls, so this is an empty compatibility stub: its presence in a
  program's `uses` is all that matters. }

interface

implementation

end.
