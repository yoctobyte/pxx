# Track P backlog — one standing pointer

Supported dialects are **FPC** and **Delphi**. Every other Pascal dialect is
**deferred, not refused** — see
`devdocs/progress/backlog-decide/decide-which-pascal-dialects-pxx-targets.md`
(slug: `decide-which-pascal-dialects-pxx-targets`), which is the single source.

Deliberately a pointer and not a restatement: two copies of a policy is two
copies to go stale.
