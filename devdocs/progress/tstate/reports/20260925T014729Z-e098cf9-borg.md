---
sha: e098cf93f0848af6387bb0ac036b2f9a5211d80a
parent_tested: none
date: 2026-09-25T01:47:29Z
host: borg
tier: full
wall: 1107.6
scale: 1.0
probe: native=0.03 emulated=0.42
verdict: RED
code_fp: f35167cd4e55
toolchain: kernel=7.0.0-31-generic gcc=13.3.0 qemu=8.2.2(6 of 6) espqemu-riscv32=9.2.2 espqemu-xtensa=9.2.2 git=2.43.0 wasmtime=48.0.1
toolchain_fp: 892e942692d7
compiler_sha256: 426b2fbf3f08f08ab498e063e9e8b1dc5963cc9eee057bebff304036d41e287a
skips: 1
skip_holes: 1
flaky: 0
---

> **COVERAGE: 1 job(s) DID NOT RUN on this box** (of 1 skipped). They are scored passlike, so they are invisible in the verdict above — a `RED` here speaks for the jobs that ran, not for the suite.

<details><summary>skipped jobs, by reason (1)</summary>

- **host dev dependency absent: /usr/include/x86_64-linux-gnu; — this box does not have it, so the job cannot pass here and a red would be a statement about the box rather than about the tree** — test-core#2047

</details>

> **-O3 IS UNTESTED ON THIS TREE.** The newest `opt` sweep is 6h old and ran at `46d2a9430a96` (RED). `opt` is disjoint from `full`, so no optimisation level above the default has seen this sha.

## NEW-RED
- tools-devtest#00
  - `verify_pin left the fixture clean                          ok | the real checkout's pinned artefacts are untouched         RED | 1 RED: | - the real checkout's `

## FIXED
- demos#00
- lib-test#src:tools/crtl_reachability.py — tools/crtl_reachability.py tools/gen_crtl_map.py +58
- optdiff#shard6/12
- optdiff#shard8/12

## STILL-RED
- test-core#src:test/c_crtl_wait.c — test/c_crtl_wait.c tools/expect_same.sh +2
  - `after-cont       ret=ok   errno=0  exited=1 code=5 signalled=0 sig=-1 stopped=0 ssig=-1 cont=0 | wait             ret=ok   errno=0  exited=1 code=3 signalled=0 `

## failure detail: tools-devtest#00 (fail)
selected: NEW RED this run
repro: `tools/testmgr.py --tier full --job 'tools-devtest#00'` at e098cf93f0848af6387bb0ac036b2f9a5211d80a
```
ls through in order        checking only the first is the false-skip bug this file exists for
2. it fires when the header is genuinely absent
  PASS a `uses gtk` job with no header anywhere is flagged        ('/nonexistent-b/gtk.h', None)
3. AND NOT when the unit resolves — the false-skip control
  PASS not flagged while the header is present on this box        live=None (header present=True)
  PASS a hit in the SECOND root satisfies it                      checking only the first root would have skipped this job
4. the -I rule is about the directory, not about a test name
  PASS an -I naming a missing directory is flagged                ('/usr/include/pxx-absent-dir/', None)
  PASS and one naming a present directory is not                  None
5. a job that RUNS under xvfb needs xvfb, which is a third profile
  PASS flagged exactly when xvfb-run is absent from PATH          xvfb-run present=True, verdict=None
6. the reason names the PACKAGE, which is the actionable half
  PASS gtk-2.0 maps to libgtk2.0-dev                              
  PASS and gtk-3.0 to libgtk-3-dev — they are DIFFERENT packages  one flag for both is how five jobs became eighteen tickets
testmgr: 1 job(s) SKIPPED — absent host dev dependencies (/usr/include/pxx-absent-dir/ x1). That is coverage this box is not providing, not a verdict on the tree.
  PASS apply_host_lib_skips moves the job to SKIP, not to fail    skip
  PASS and the reason says whose fault it is NOT                  host dev dependency absent: /usr/include/pxx-absent-dir/ — this box do
  PASS and it does not relabel an already-skipped job             host capability absent: rdrand
7. THE REAL POPULATION — the control the synthetic rows could not be
  PASS the scan reached a real population, not an empty one       2637
  FAIL no job on a fully-provisioned box is skipped for an absent library 1 false skip(s): test/c_extern_data_legitimate_shapes.c
  PASS and with the roots absent the real jobs DO fire            11: c_extern_data_legitimate_shapes.c test_c_crypt.pas test_c_define_const.pas test_c_gtk.pas test_c_gtk_call.pas test_c_gtk_types.pas test_c_gtk_window.pas test_sqlite_crud.pas test_sqlite_crud_autotyped.pas test_sqlite_crud_lazy.pas test_string_to_pchar_auto.pas
  PASS a shell-computed -Fu means DO NOT JUDGE, not "path absent" None

  18 guard(s), 1 FAIL
  FAIL: tools/twatch_pin_identity_devtest.py
  the two are 4 commits apart (the ticket's control)         ok
  pinned_tree_for accepts 'v407' and '407' alike             ok
  pin_commit_for accepts 'v407' and '407' alike              ok
  an unknown version resolves to None, not to the tree       ok
  fixture: pinned_tree_for finds the TREE                    ok
  fixture: pin_commit_for finds its CHILD, the pin commit    ok
  DEFECT REPRODUCED: at the pinned tree, VERSION is the PREVIOUS pin ok
  restore from the pin commit makes it the pin under test    ok
  WITHOUT the cleanup, checking the branch back out FAILS (the wedge) ok
  ...and the wedge left HEAD where it was, still detached at the tree ok
  after `git checkout HEAD -- <artefacts>`, the tree is ours again ok
  ...and clone_head_back's plain checkout now SUCCEEDS       ok
  no dirt is left behind                                     ok
  MISMATCH refuses: publishes nothing                        ok
  MISMATCH says both versions                                ok
  MISMATCH names the pin COMMIT                              ok
  MATCH does not refuse                                      ok
  a spelling difference is not a mismatch                    ok
  no pin-built job: does NOT refuse                          ok
  no pin-built job: SAYS the check could not run             ok
  verify_pin left the fixture clean                          ok
  the real checkout's pinned artefacts are untouched         RED

1 RED:
  - the real checkout's pinned artefacts are untouched
  tools-devtest: 167 green, 2 RED -- tools/host_dev_lib_skip_devtest.py tools/twatch_pin_identity_devtest.py

```
