---
sha: 886bfe597d69cb89399f63d0f501eea00845e7bc
parent_tested: fc43936b00d9c032757157762ac2adf344b22407
date: 2026-09-06T11:24:33Z
host: seven
tier: native
wall: 166.1
scale: 1.0
probe: native=0.47 emulated=0.88
verdict: RED
code_fp: 7327e547732c
toolchain: kernel=7.0.0-31-generic gcc=15.2.0 qemu=10.2.1(6 of 6) wasmtime=48.0.1
toolchain_fp: b926fcc528d9
compiler_sha256: 3e54aadacbc8a3391b0a97cbe9057feff8c0b72cfd5bd7679c668523e9cb1e7e
skips: 6
skip_holes: 1
flaky: 0
---

> **COVERAGE: 1 job(s) DID NOT RUN on this box** (of 6 skipped). They are scored passlike, so they are invisible in the verdict above — a `RED` here speaks for the jobs that ran, not for the suite.

<details><summary>skipped jobs, by reason (6)</summary>

- **host capability absent: rdrand — this CPU does not implement RDRAND/RDSEED (Intel Ivy Bridge 2012 and later), so the job cannot pass on this box and a red would be permanent** — test-core#1194
- **host dev dependency absent: /home/seven/trackt-watch/compiler/gtk.h — this box does not have it, so the job cannot pass here and a red would be a statement about the box rather than about the tree** — test-core#1760 test-core#1761 test-core#1762 test-core#1763 test-core#1765

</details>

> **-O3 IS UNTESTED ON THIS TREE.** The newest `opt` sweep is 2d3h old and ran at `cf9b14600039` (GREEN). `opt` is disjoint from `native`, so no optimisation level above the default has seen this sha.

## STILL-RED
- size-canary#src:tools/size_canary.py — tools/size_canary.py
  - `x86_64-empty        65304      +4025       2792       +832      43524      +1072 | size-canary: 1 FAILURE(S) | esp32c3-bare.code: 50528 -> 57900 (+7372, +14.6%)`
- test-core#src:test/test_a_named_dynamic_array_parameter_still_takes_nil_as_a_default.pas — test/test_a_named_dynamic_array_parameter_still_takes_nil_as_a_default.pas tools/expect_same.sh
  - `pascal26:40: error: a parameter of an open-array or fixed-array type cannot have a default value: there is no array literal to write there, so the value would b`
- test-core#src:test/test_libwriteln_parity.pas — test/test_libwriteln_parity.pas tools/expect_same.sh +1
  - `qword    builtin=9000000000 | -qword    library=9000000000 | +qword    library= | bool     builtin=TRUE | bool     library=TRUE | boolF    builtin=FALSE`

## failure detail: test-core#src:test/test_a_named_dynamic_array_parameter_still_takes_nil_as_a_default.pas — test/test_a_named_dynamic_array_parameter_still_takes_nil_as_a_default.pas tools/expect_same.sh (fail)
selected: first failure in job order; no new red this run
repro: `tools/testmgr.py --tier native --job 'test-core#src:test/test_a_named_dynamic_array_parameter_still_takes_nil_as_a_default.pas'` at 886bfe597d69cb89399f63d0f501eea00845e7bc
```
(diagnostics)
pascal26:40: error: a parameter of an open-array or fixed-array type cannot have a default value: there is no array literal to write there, so the value would be a scalar whose bytes the callee reads as a length header. A named DYNAMIC array type takes a default (`a: TArr = nil`) because it is a handle (bug-a-managed-string-arg-temp-predicate-is-duplicated-seven-times-and-guarded-nowhere)
(tail)
pascal26:40: error: a parameter of an open-array or fixed-array type cannot have a default value: there is no array literal to write there, so the value would be a scalar whose bytes the callee reads as a length header. A named DYNAMIC array type takes a default (`a: TArr = nil`) because it is a handle (bug-a-managed-string-arg-temp-predicate-is-duplicated-seven-times-and-guarded-nowhere)
  near: ( const a : TArr = >>> nil ) ; 

```
