# Steering note — 2026-09-18, owner AT the keyboard

**This file exists because the steering seat loses everything at a context
boundary.** An hourly timer re-enters and reads this. It is NOT a handoff and
NOT an instruction to anyone but the steering seat (frankuser).

**The timer is an owner-authorised override of CLAUDE.md's no-timed-callback
rule**, granted 2026-09-18: *"i also launched another frank instance. so i
suggest you steer them, keep yourself available for interaction with me, and set
an hourly timer to keep them busy as i'll be doing things today."* It expires
when he says so or when today's work ends. **Do not generalise it.** Every other
timed-callback prohibition stands.

## The two priorities, in his words

> *"there's 2 prio's - ESP (and SRAM usage/stripping/etc) and lekkerzeilen.
> those are pretty distinct so should be able to run in parallel."*

## Dispatch as of 2026-09-18

| seat | group | first ticket |
| --- | --- | --- |
| **frankh-3f** (new) | ESP — THE INSTRUMENT (T) | `bug-t-code-is-page-quantised-so-there-is-no-instrument-for-size-work` |
| **franks-ee** | ESP — what RESERVES SRAM (A/S) | rung 0: measure IDF's own SRAM cost (UNFILED — file it) |
| **frankb-56** | ESP — STRIPPING (A) | `bug-a-dce-refuses-every-target-except-x86-64` |
| **lekkerzeilen-c8** | lekkerzeilen | continuing; asked which seams it is not touching |
| **neo-a2** | offered: read-only data (A) or lekkerzeilen | `feature-opt-static-literal-blocks-should-never-be-written-to` |
| **frankuser** | steering + owner-facing | stays available; does not hand-code |

## The umbrellas, filed today at his direction (`68e5aa55c`)

- `umbrella-a-hosted-program-is-as-small-as-it-can-be` (PC)
- `umbrella-an-esp32-image-is-as-small-as-it-can-be` (ESP)

Both `prio: 70` — **a placeholder set by an agent. Umbrella prio is the one
number a human sets and he has not set it.** Ask him if it comes up; do not
quietly keep 70 as if it were his.

## The measured facts every seat needs, and the traps in them

1. **`code=` IS PAGE-QUANTISED.** `0xe8 + code` is a page multiple in every row
   measured. 3864 -> real 600 or 195; 69400 -> 67104; 20248 -> 18790. It is why
   `--no-signals`, which removes exactly 405 bytes always, read as a no-op in all
   six configurations and got reported to the owner as an anomaly. **There was
   none.** Until frankh-3f fixes it, grade on the segment with trailing zeros
   stripped.
2. **`--dce` removes ZERO bytes of data or bss.** code 1,347,352 -> 745,240,
   data 86,084 -> 86,084, bss 66,796 -> 66,796. Code and RAM are different
   problems with different passes. Cross-target DCE is the FLASH answer.
3. **There is no `.rodata` at all.** Two LOAD segments, `R E` and `RW`.
   `grep -c rodata` = 0 in `elfwriter.inc` and `defs.inc`.
4. **`--esp-profile=bare` is qemu's shape, not a chip's** — `defs.inc:2275`:
   *"qemu's esp32c3 machine models it as one RWX region."* The IDF profile keeps
   `.text` in flash and is one branch away. Say which profile beside every number.
5. **The x86-64 floor is 195 B code / 336 B data / 41,800 B bss**, verified to
   print `hello`. 32,768 of the bss is the signal alt stack, reserved even under
   `--no-signals`. The owner's remembered "267 bytes" for a no-ansistring hello
   world **still holds — it is 195.** What regressed is the DEFAULT.
6. **IDF's own SRAM cost is UNMEASURED.** Until it is, no rung can claim an
   image fits. It is rung 0 for a reason.

## What the steering seat does each tick

1. `git pull --rebase` and skim what landed.
2. Check each seat: has it moved? A seat that has gone quiet gets ASKED to check
   its own transcript — never judged from outside, never sent keystrokes, not
   even a deny. Read WHO refused and WHEN: a hook decline and a user denial wear
   the same string, and the newest may be days old in a seat working fine. The
   discriminator is whether the newest denial falls AFTER the last successful
   tool call.
3. Re-task anything that finished. Work in GROUPS, never a lone ticket.
4. Append a dated line to the log below. A quiet tick still gets a line.

## Hard limits while steering

- **No `make pin`** without asking him — he is at the keyboard, so ask.
- **No loosening any hook, allowlist or `settings.json`.** Tightening is mine;
  loosening is his, however small the diff.
- **No keystrokes into a peer's pane**, including a deny.
- **No starting seats he did not start.**
- **No `pgrep`/`pkill -f` waits** — they match their own command line.
- **No `rm` with a variable or glob** — there is a hook, and the built-in prompt
  stalls a session for hours.

## Check-in log

- **2026-09-18, dispatch.** Five seats tasked as above. Two umbrellas + four
  blockers filed (`68e5aa55c`); the page-quantisation measurement and the
  defs.inc comment correction landed at `ad85bf019`. Rejected
  `decide-is-a-whole-python-program-meant-to-fit-inside-an-esp32` (premise
  compared code against SRAM) and told franks-ee directly with the reasoning.
  Owner has ~10x16GB DDR3 RDIMM to install; **socket 1 of plexus is
  UNPOPULATED**, so the 8 free DIMM slots are CPU2's and are dead — the swap is
  replace-8-with-16GB for 128 GB, not add. Reboot pending his timing; Track T is
  on borg so breadth survives it.

- **2026-09-18, tick 1 (12:23).** 39 commits landed since `ad85bf019`. **The
  DCE port CLOSED** (frankb-56): riscv32 `40ab2b91c`, i386 `6fbe582a3`, xtensa
  both ABIs `095a7a794`, arm32+aarch64 `95b92d920`. Only wasm32 left, and it is
  genuinely different (function indices, not a displacement). xtensa call0
  694,520 -> 161,840. The xtensa blocker was ALIGNMENT — it is the only ISA here
  with 2- and 3-byte instructions, so the only one where a dropped body's extent
  is not a multiple of 4, and CALL0/CALL8 need a 4-aligned target. **Four
  fixed-width targets could not have shown it.** franks-ee finished all three of
  its items (`96689165c` rung 0, `66f564165` writeln silence, `16ebf18ce` alt
  stack, −32,792 B on every image) and frankh-3f both of its (`578907347`
  code= is real bytes with codeseg= beside it, `ff2aefa20` ESP suites enrolled).
  **Re-tasked both:** frankh-3f -> the read-only-data group (owner directive 1,
  and it is warm on the ELF writer); franks-ee -> its OWN find,
  `bug-a-writeln-diverges-between-the-two-esp-backends-on-the-idf-profile` p70,
  where the primary ESP target silently drops writeln on the profile a real chip
  uses, then LINE_BUF_SIZE (now 4,096 of the remaining 9,008 bss). neo-a2 is on
  `bug-n-a-callable-attribute-dispatched-at-run-time-takes-at-most-4-arguments`
  in its own checkout, **frankD** — it correctly refused to edit a tree other
  seats are live in. lekkerzeilen-c8 is parked-and-responsive by its user's
  instruction; that is a boundary, not idleness, and I asked it to work twice
  before respecting it.
  **BORG IS PUBLISHING AGAIN** after 3 days, so the newly enrolled ESP rows get
  a real verdict soon. One live red there, flapping:
  `regression-test-threads-test-threadsafe-heap-lock-deadlock-diag-2` in
  `backlog/` — NEW-RED then FIXED then "green but NOT closed (race-unsafe on one
  green)". Auto-filed, so its `track: T` is a FALLBACK — it is threads/heap-lock
  and belongs to A. Unassigned; nobody is on it.
  **THREE ITEMS STILL WITH THE OWNER**, all raised ~1h ago: (1) commit
  lekkerzeilen's 2,126 uncommitted lines — the working SDL2+OpenGL backend
  exists ONLY in `/home/neo/lekkerzeilen`'s working tree, and my scratchpad
  patch is a /tmp stopgap reaped at 6h, refreshed this tick; (2) run
  `tools/install_esp32_target.sh` on borg, or the ESP rows skip there; (3)
  go-ahead on the `llm.py:13` timeout. **No pin asked for or taken.**
  Corrections I owed and made: my DCE ticket prescribed per-target branch-patch
  arms that `symtab.inc:17963` already had (frankb-56 fixed it in place); I
  relayed a six-day-stale lekkerzeilen frame to two seats; and I suggested a
  module-docstring ticket that has been in `done/` since 09-12.

- **2026-09-18, tick 2 (13:23).** 15 substantive commits since tick 1, all three
  ESP seats landing. Headlines: the **read-only PT_LOAD is in** (`b05b7bb0a`,
  hosted x86-64, string-literal pool, `--no-ro-data` is the off switch) and **it
  found a live silent heap corruption on its first run** — x86-64's inlined
  SetLength released through an arm with no MSTR_STATIC_RC guard, so
  `a := 'abc'; SetLength(a,2)` decremented a saturated literal refcount, leaving
  it live-counted and freeable. The bare heap now **says** when it is exhausted
  (`7f86a12a6`) — before today it printed fifteen dots and hung, at the DEFAULT
  size. Heap arena is a command-line knob (`7bc2467e3`). The extern-only ticket
  was ANSWERED and declined (`f0aac2f58`, 241 B/TU on busybox, zero on ESP).
  **THE BIG MEASURED LEVER, scoped and UNSTAFFED:** a non-allocating bare program
  under `-uPXX_MANAGED_STRING` is **−98% code and −93% bss** (58,900/71,452 ->
  1,156/5,288). franks-ee scoped it and deliberately did not take it — it flips a
  default for every Pascal program on every target. Blocked on STAFFING, not
  knowledge; the owner has been told.
  **LIVE SIGNAL: five NEW test-core reds on borg** (`c0361f32e`, tree
  `4fbed6c4157e`) — cast_deref_varparam, dynarray_to_pointer_seam_leaks,
  indexing_a_string_cast_of_a_pointer_slot, interface_containers @1 and @2.
  `b05b7bb0a` IS an ancestor of that tree; two other commits are in the same
  window, so the row names a TESTED TREE and not a culprit. All three named
  files are literal-heavy (21/7/3), which fits frankh-3f's own prediction that
  the segment turns unguarded stores into crashes. Told it, with the ask:
  separate exposed-bug from segment-bug PER ROW using `--no-ro-data` as the A/B,
  and say which class each is BEFORE fixing any.
  **Corrections I made this tick:** my "RTTI pays on both profiles" was wrong
  (bare pulls ONE class, ~160 B; the population is IDF and hosted), and my
  relocation hypothesis was false on BOTH ISAs (riscv32 and xtensa both emit
  .rodata with .rela.rodata). Promoted one rule to CLAUDE.md (`daa6c4621`) on
  three same-named-constant pairs in one day.
  **Still with the owner, unchanged:** the lekkerzeilen commit (patch refreshed
  again this tick), `install_esp32_target.sh` on borg, the `llm.py` timeout, and
  now the staffing call on the no-heap lever. **No pin asked for or taken.**

- **2026-09-18, tick 3 (14:23).** **Every red from tick 2 is FIXED and borg has
  published the clears** — all five test-core rows, plus test-aarch64
  seam_leaks and a test-nilpy row. Three of the five were frankh-3f's own
  read-only-split regression (RTTI descriptors hold SELF-RELATIVE 32-bit words
  at five sites in rtti_emit.inc; the split changed distances and produced
  `rdyn 0 instead of 3` — **a silent wrong value, no fault**, in the instrument
  built to stop silent corruption). A sixth red was stale paperwork from
  `efe06a903` (Track P, yesterday), attributed by frankb-56 and closed.
  **THE READ-ONLY SEGMENT HAS NOW EXPOSED THREE LIVE SILENT CORRUPTIONS**: the
  SetLength literal refcount decrement (live since `d782926ce`, **carried by pin
  v411**, bounded — only freed after ~1.07e9 net decrements, so corrupt
  accounting rather than a practical use-after-free), `t(r)[2] := 'X'` doing no
  copy-on-write at all, and a synthesised soname interned inside `.dynstr`.
  ESP-IDF objects now carry `.rodata` (`c44fa2642`).
  readln was rebuilt twice: a long line silently became TWO and shifted every
  later readln (`0ab100740`), and `read(c: Char)` never handed over the `#10`
  (`969dcad40`) so the canonical `while not Eof do read(c)` scanner merged every
  line — in BOTH readers, so the cross-target census could not see it. **My
  census spec was the population error**: I named the TARGET axis and never the
  BUILD MODE axis, and `-uPXX_MANAGED_STRING` — the mode `compiler.pas` itself
  is built with — had readln broken on all five cross targets. 285 lines of
  x86-64 asm deleted; deleting it FIXED a target.
  TLS measured per profile: −3,072 hosted x86-64, **ZERO on ESP** — hosted
  umbrella only. My "the 3,072 reserve is the guard" reframe was **narrowed and
  was wrong**: the fold is sound because the size is a compile-time CONSTANT,
  not because it is 3072.
  Seats: frankh-3f shell, franks-ee busy, frankb-56 busy, neo-a2 idle with 7
  uncommitted files in frankD and HEAD unmoved — **asked it to check its own
  transcript**, not judged from outside. lekkerzeilen-c8 parked-and-responsive
  by its user's instruction; not asked to work.
  **Still with the owner:** the lekkerzeilen commit (patch refreshed again), the
  borg Espressif toolchain, the `llm.py` timeout, and the staffing call on the
  no-heap lever (−98% code / −93% bss, scoped, unstaffed). **No pin asked for or
  taken** — worth noting the next pin would carry the v411 literal fix.

## 2026-09-18 ~14:00 — WIND-DOWN (owner directive; the timer is deleted)

Owner: *"ok slow down on parallel devving (slowly), as we have a new project
that needs some tokens."* This is the last block; the steering role stops here.

**The word that shapes it is "(slowly)".** Not a stop mid-edit — a wind-down.
Each seat was told: land what is in hand (non-green is allowed, **say so in the
commit message**), **PUSH**, write the next step into the ticket body so a cold
seat can resume without re-deriving, then park and NOT take the next item off
`ready`. No seat was stopped, and none was started — fleet size is the owner's
dial, and a new seat `tuxspaceprogram-6e` appeared at 13:57, which is almost
certainly where the tokens are going.

**Hourly cron `a4587d96` DELETED.** It existed to "keep them busy"; that is the
thing being wound down, so it goes first. The timer was an owner-authorised
EXPIRING OVERRIDE of CLAUDE.md's no-timed-callback rule — it has now expired and
**must not be generalised into a precedent**. No seat should wait on a tick.

**Why the push step is the load-bearing one, and it caught something:** frankh-3f
reported everything banked, and in the same breath named frankb-56 holding
`9729073df` **unpushed** — the test-core fix that its own run depends on. A
local commit is not banking; a restart takes it and the next session is told to
distrust a diff it cannot explain. One seat's park notes made another seat's
stranded state visible, which is the whole argument for winding down out loud
rather than each seat going quiet on its own.

**frankh-3f — parked clean, all three read-only-segment corruptions FIXED:**
`b05b7bb0a` RO segment x86-64 + SetLength guard; `120e3a3cd` RTTI relfix +
`t(r)[i]` COW; `1cdb560f4` soname interned in `.dynstr` (its own regression,
found by test-core); `c44fa2642` ESP-IDF `.rodata` in both ELF32 object writers
— **xtensa `test_emit_obj` SRAM `.data` 6,304 -> 2,624 B**, verified by linking
with the esp gcc; `3c9dbfef5` park notes. Residual written into
`feature-a-there-is-no-read-only-load-segment-...`, section "PARKED for the
wind-down": hosted aarch64/i386/arm32 with an exact recipe, then RTTI/VMT after
measurement. One open fact it flagged honestly: test-core past
`test_object_value_constructor_error` is **unmeasured on origin**, that row is
stale (`efe06a903` deliberately allowed constructors), and its own clean run was
from a scratch Makefile copy with the row dropped.

**The safety artefact had a hole and it is closed.** `git diff` carries no
untracked files, and lekkerzeilen has **fourteen** — `lekkerzeilen/facades.py`,
`tools/texture.py`, `tools/fronts.py`, three test files, `_vocab.py`,
`_parity_main.py`, `parity_generated.py`, two shell scripts and `devdocs/`. A
patch-only snapshot would have silently dropped every one. Now both:
`worktree-2026-09-18.patch` (16 files, 2,126 insertions) **and**
`untracked-sources-2026-09-18.tgz` (105 entries). Excluded deliberately:
`.pxx/` (a built `pascal26` binary) and `assets/facades.lzx` (6.8 MB asset).
**Both still live under `/tmp`, which reaps at 6h** — this remains a stopgap and
the commit decision is still the owner's.

**Still with the owner, unchanged:** the lekkerzeilen commit; the borg Espressif
toolchain (`tools/install_esp32_target.sh`); the `app/services/llm.py:13`
timeout; and the staffing call on the `-uPXX_MANAGED_STRING` lever (−98% code,
−93% bss, scoped, unstaffed). **No pin asked for or taken** — and the next pin
would carry the SetLength literal-refcount fix, which v411 does not.

**Open and unassigned, deliberately not staffed during a wind-down:** two xtensa
correctness bugs on the primary ESP target (variant-record arms at different
offsets; unaligned packed-field access SIGBUSes), and the collapsed
`decide-a-is-a-pxx-object-...` fork — *"do we promise that a pxx object can
supply the runtime for something else, or only for itself?"*

### Final resting state (same day, ~14:30) — all four seats parked

**franks-ee** — seven commits on origin, tree clean. TLS threadvar area is now a
command-line knob (`-dPXX_TLS_USER_0/_1K/.../_16K`, default unchanged at 3072);
both silent stdin bugs fixed with **the pre-fix values recorded in the Makefile
comment beside each fixture**, so the wrong answers survive the fix. Left
`feature-a-the-threadvar-area-is-3072-bytes-of-bss-...` **deliberately open** —
the knob is opt-in, so a program passing no flag still pays the 3,072, and
resolving it would have claimed otherwise. Its own correction, which I accept as
stated: the census scope error was **not half mine** — I asked for existing
fixtures under two more `--target=` flags and got exactly that; writing "NO
BLOCKER IS KNOWN" into a summary is a claim about a population the run never
enumerated, and that step was its own. The axis is banked as
`A CROSS-TARGET CENSUS IS NOT A CROSS-BUILD-MODE CENSUS`.

**neo-a2** — five commits, tree clean, PIL untouched and **unclaimed** (no
partial state, so a cold seat takes it clean). Both tickets in `done/`.

**frankh-3f** — parked, and then corrected itself AGAINST its own result: the
sync that settled the ghost-sha question pulled 9 files of `compiler/` plus
`lib/rtl/palthread.pas` **while its scratch test-core run was in flight**. Binary
still the `c44fa2642` build, later rows compiled against a moved `lib/rtl` — which
is a compiler build input, the part that surprises people. Recorded as
**mixed-tree evidence, not a clean run of any sha**, and not re-run during the
wind-down. Both halves right. The rule it caught is the one nobody scans for:
**banking your work does not feel like touching the instrument**, and the sync
that makes you safe is the same event that moves the population.

**frankb-56** — one real find in hand at wind-down time: the RTTI registry
interned the RAW declaration spelling while the class blob holds
`ClassRttiName`, so `GetClass(X.ClassName)` — the one round trip the registry
exists for — **failed for every specialized class**, while `GetClass('TIntBox')`,
a name no instance ever reports, succeeded. Its positive control is the PIN
rather than anything manufactured, and the third row (a plain class, FOUND on
both) is what stops the test passing on a registry that finds nothing at all.
Asked only to state run provenance in the commit message, **not** to re-run.

**A GHOST SHA COST TWO SEATS AN ALARM AND IS NOW PERMANENT IN A COMMIT MESSAGE.**
frankh-3f reported frankb-56 stranding `9729073df`; measured after a fetch, that
object does not resolve here and the surviving commit is `cdf0c0539`, an ancestor
of origin/master. Nothing was stranded. But `0a77fe3b8`'s own message says
*"cause was efe06a903, fixed in 9729073df"* — **the doomed sha, now permanently
on origin**, where a later reader will chase an object that does not exist. The
ticket was repointed at `cdf0c0539` (`f89dcf573`). The commit message cannot be.

**One promotion decision, refused, and recorded here because a refusal that is
never explained reads as a judgement on quality.** neo-a2 found a sharper form of
the sibling-is-a-spelling rule: the shared thing can be **a numeric CONSTANT**,
not a spelling — *when you extend a ladder, grep for who counted its OLD TOP*.
It beats what CLAUDE.md says today. **Banked, not promoted: one subsystem.**
Promotion needs a SECOND independent subsystem, and that test exists precisely
because merit-based promotion is how this fleet's rules file reached 72KB the
first time. neo-a2 then added the half that makes the trigger decidable for
whoever finds the second instance, and it is the better half: **why the existing
rule cannot already cover it.** CLAUDE.md prescribes grepping for the other
spelling's HANDLER, and that grep cannot reach this class — *a consumer that
merely stopped at four is not a handler for anything, names no ladder, shares no
construct, and reads as ordinary correct code.* That is why it needs a different
instruction rather than better compliance with the existing one.

Correction to my own note above: the callable-value concept has **three ARMS in
one file**, not three owners — `PyInferExprType`'s lambda arm,
`PyInferDefRetTypeScan`'s return arm, and the bound-method fix, each
rediscovering *"does this expression denote a callable value"* for one construct.
Two is a smell, three is a design flaw; stated as a QUESTION in the ticket
(whether they want to be one predicate asked once), flagged as a refactor needing
a corpus, and correctly not acted on during a wind-down.

### frankb-56 final (~14:45) — and a correction to the ghost paragraph above

Parked, tree clean, level with origin. Five commits, each verified on origin by
merge-base; `fed4373ab` is the `GetClass(X.ClassName)` registry keying fix.
Verdict read from the JOB's own line, not a wrapper status: `TEST-CORE-DONE
rc=0`, zero `make: ***`, zero FAIL; `gate.sh quick` GREEN, 23 PASS; fixedpoint
converged in 1 round.

**It ruled out the mixed-tree case with evidence rather than with a probably**,
which is the answer I wanted and not the one I asked for. Run started 14:01:30;
last HEAD movement in that checkout 13:34:02, twenty-seven minutes earlier; and
**zero files under `compiler/`, `lib/rtl` or `test/` carry an mtime after the run
started** — the only post-start writes in the repo are devdocs prose, which is
not a build input. So: a clean run at `9ce71480b1e2` plus the patch, and that is
the phrasing in the commit rather than a bare "green". Note the instrument it
chose: **mtimes of the build inputs**, which is a state the TREE maintains, where
"did a sync land" is a question about the observer's relationship to the job.

**CORRECTION TO THE PARAGRAPH ABOVE, and it is the stale-hazard class caught in
this note's own text.** I wrote that `0a77fe3b8`'s permanent citation of the ghost
`9729073df` could not be repaired — true about the commit message, and it reads
as *nothing can be done*, which is now false. frankb-56 fixed it where it was
still fixable: `done/regression-test-core-test-object-value-type` now cites
`cdf0c0539` **and carries a parenthesis saying why `9729073df` does not exist**,
so a cold seat arriving from that permanent commit message is met with the
explanation instead of an object that will not resolve. The immutable half stays
immutable; the reachable half was repaired, twice now (`f89dcf573`, and this).
**A hazard note that says a thing is unfixable succeeds by stopping the reader,
and generates nothing that would reveal it was wrong** — which is exactly why it
had to be corrected here rather than left to be discovered.

## 2026-09-18 ~20:55 — NIGHT WATCH (owner: *"you guard the toko troughout the night"*)

**No timer, and that is the reading rather than an omission.** Three hours ago
the instruction was *"slow down on parallel devving (slowly), as we have a new
project that needs some tokens."* A guard that wakes hourly to re-read its whole
context spends exactly the resource that was just reclaimed, and CLAUDE.md's
no-timed-callback rule says why: a timed wake-up learns nothing. **Peer messages
wake this session on their own** — that is the real signal, it is free, and every
seat is parked, so the volume is near zero. If he wants ticks he can say so and
the override is one call; absent that, guarding means being the awake seat, not
the polling one.

**THE ONE THING THE NIGHT ACTUALLY THREATENED IS FIXED.** `/etc/tmpfiles.d/tmp.conf`
reads `D /tmp 1777 root root 6h`, and the lekkerzeilen safety artefacts were
already **seven hours old** at 20:53 — they survived on the reaper's scheduling,
not by design, and would not have survived the night. They are now at
**`/home/neo/lz-safety-2026-09-18/`**, outside `/tmp`, which needed no permission
because copying a backup to durable storage decides nothing the owner has not
decided. **The commit call on those 2,126 lines remains entirely his.**

**The restore path is VERIFIED, not assumed** — a patch nobody has tried to apply
is not a backup, and this note would otherwise have recorded a stopgap that could
fail silently at the moment it was needed:
- the patch **reverse-applies** against the live worktree, so it is a true
  `HEAD -> worktree` diff and not a partial capture;
- it **applies onto a clean `git archive HEAD` checkout**, which is the actual
  restore scenario;
- all eleven untracked source files are present in the tarball and three spot
  checks are **byte-identical by sha256**; `devdocs/` contributes 94 entries.
- Tracked side re-diffed at 20:53 and **byte-identical to the 13:58 capture** —
  nothing moved, and the newest modified tracked file is dated **2026-09-15**, so
  this work has been sitting uncommitted for three days, not hours.

**Correction, line 243 of this file: the untracked count is FOURTEEN, not
thirteen** — I had counted the twelve entries I tarred and dropped the two
deliberate exclusions (`.pxx/`, a built `pascal26`; `assets/facades.lzx`, 6.8 MB)
from the total. The exclusions were right and the arithmetic was not. It matters
only because a later reader checking the snapshot for completeness would compare
against a wrong denominator and conclude something was missing.

**Resting state entering the night:** frankh-3f, franks-ee, frankb-56 and neo-a2
all parked, trees clean, level with origin, nothing unpushed, no claims, no
timers in any seat. lekkerzeilen-c8 parked by its own user's instruction.
`tuxspaceprogram-6e` is the owner's new seat and is not mine to steer.
**Nothing is in flight anywhere.** No pin taken; the next one would carry the
SetLength literal-refcount fix that v411 does not.

### Night tick 1 — 22:17. Quiet, except one row that was about to cost someone a day.

T is **UP** (`b291b321f185`, rc=0). `/tmp` on plexus **7% bytes, 1% inodes** —
both read, because the bytes-only reading is the one that cannot fail for the
inode outage. Nothing landed but borg's own tstate commits; **no seat activity,
which is correct for a parked fleet.** All four still parked.

**The one find: `test-threads#...deadlock_diag` is a FLAKY job whose bisect had
just falsely implicated `c44fa2642`** — frankh-3f's ESP-IDF `.rodata` commit from
this afternoon. It was the only one of seven open regressions WITHOUT twatch's
*"bad touches NO buildable file"* caveat, and it had narrowed 3 -> 1, so it read
as the single real lead on the board. Three things refute it, none needing a test
run:
- the failure is **exit 124**, which is `timeout`'s own status — the recipe wraps
  a twelve-worker contention diagnostic in `timeout 60`, so what failed is the
  wall clock, not the assertion;
- **three separate shas each report NEW-RED in the full tier and FIXED in the
  native tier** (`91ba5968354b`, `7f86a12a6628`, `95b92d92023e`) — present and
  absent at one tree is a race against a clock, not a defect;
- the **`bad` sha MIGRATED** from `aa43f495ab87` (filed 09-14) to `c44fa26429e9`
  four days later. A stable regression bisects once and stays. **Bisection assumes
  a monotone good->bad boundary; a timing flake has none**, so it terminates on
  whatever it sampled red and reports it with full confidence.

Annotated on the ticket rather than left for a cold seat to rediscover, because
the cost lands on whoever reads it next: a 330-line ELF-writer diff is a
plausible-looking thing to audit and would have eaten a day. **The exculpation is
scoped**: this says the instrument cannot locate a race, NOT that none exists.
Residual question — longer/adaptive timeout, pin to an unloaded host, or mark the
job non-bisectable — is **Track T's and is left unassigned**, because this tick is
a watch and the fleet is wound down. Owner told the same night.

### Night tick 2 — 23:17. Nothing moved.

T **UP** at `b291b321f185`; the seven open regressions are the same seven at the
same shas — no new red, none cleared. `/tmp` 7% bytes / 1% inodes. Only commit
since tick 1 is my own (`5a959980f`). No peer messages; all four seats parked.
lekkerzeilen worktree byte-identical to the 13:58 capture, so the durable
snapshot stands and was not re-taken.

One drift worth a line because it is the kind that is invisible until someone
quotes it: **breadth is now 7h stale** (was 6h at tick 1) — borg has run no full
tier since 13:03Z, so there is still **no cross-target verdict on this tree**, and
a native GREEN does not cover i386/arm32/riscv32/aarch64. Not actionable
overnight, and not a fault; recorded so nobody reads tomorrow's native green as
breadth.

### Night tick 3 — 00:17. Nothing. Identical to tick 2.

T UP, same seven regressions at the same shas, `/tmp` 7% / 1%, all four seats
parked, lekkerzeilen unmoved. The only commits on origin are this watch's own.
borg has tested nothing since 13:03Z and is **correctly** idle — the three
commits since are documentation, and it samples the tip every ~8. Breadth 8h.

### Night tick 4 — 01:17 (2026-09-19). THE REAPER FIRED, AND IT TOOK THE SNAPSHOT.

`/tmp` went **6.3G -> 424M and 50,287 inodes -> 1,670** between 00:17 and 01:17:
the 6h reaper ran. **`.../scratchpad/lz-safety/` no longer exists** — the
lekkerzeilen patch and tarball that were the only copy of 2,126 uncommitted lines
plus fourteen untracked files are gone from where they lived all day.

The durable copy at **`/home/neo/lz-safety-2026-09-18/` is intact and still
works**, re-verified rather than assumed: tarball lists 105 entries, and the patch
still applies onto a clean `git archive HEAD` checkout. Nothing was lost.

**This is the tick that pays for the other three.** The move happened at 20:53,
about four and a half hours before the reaper ran, and it was made on a
calculation — files already seven hours old against a six-hour window — not on a
warning. The failure mode it avoided is the quiet one: had it not been moved,
nothing tonight would have ERRORED. `git status` in lekkerzeilen would read
exactly the same, the working tree is untouched, and the loss would have surfaced
only when someone reached for the backup, which is the moment it is least
recoverable. **A backup's absence is not observable from the thing it backs up.**

Everything else unchanged: T UP, the same seven regressions at the same shas,
breadth 9h, four seats parked, lekkerzeilen worktree still byte-identical to the
13:58 capture. **The commit decision on those 2,126 lines is still the owner's,
and it is now the only remaining risk to them** — the snapshot is safe, the
working tree is not backed up anywhere but that snapshot.

### Night tick 5 — 02:17. Quiet.

Post-reaper `/tmp` has settled (433M, 1,838 inodes, 1% on both) — refilling
normally, not runaway. Durable copy still four files. T UP, same seven
regressions at the same shas, breadth 10h, four seats parked, lekkerzeilen
unmoved. Nothing landed but tick 4's own commit.

### Night tick 6 — 03:17. Nothing.

T UP, same seven regressions at the same shas, breadth 11h, `/tmp` 1%/1%, four
seats parked, lekkerzeilen unmoved. Only tick 5's own commit landed.

### Night tick 7 — 04:17. Nothing.

Unchanged from tick 6 in every field: T UP, same seven regressions, breadth 12h,
`/tmp` 1%/1%, four seats parked, lekkerzeilen unmoved.

### Night tick 8 — 05:17. Nothing. Breadth 13h.

### Night tick 9 — 06:17. Nothing. Breadth 14h.

### Night tick 10 — 07:17. Nothing. Breadth 15h.

Ten ticks, one finding (tick 1's flaky-bisect attribution) and one save (tick 4's
reaper). Eight found nothing, which is the correct outcome for a parked fleet.

### Night tick 11 — 08:17. Nothing. Breadth 16h.

## 2026-09-19 08:30 — THE WIND-DOWN WAS A MISREADING. FLEET RESUMED.

Owner: *"lol the instruction was not to sleep. the instruction was to calmly
progress, while you watch the toko."* And, on the night's output: *"you mean
parked fleet? everyone been sleeping last night?"*

**I read "slow down on parallel devving (slowly)" as STOP, and it meant reduce
the PACE.** Then I read *"you guard the toko throughout the night"* as watch-only,
when minding a shop means RUNNING it. Eleven hourly ticks, four idle seats, one
night of no development — the cost was entirely in the reading, not in anything
anyone did. Every seat parked correctly on what I told it.

**Resumed 08:30, each onto the next step it had written in its own hand** — which
is the one thing the wind-down did well: no seat had to re-derive anything.
- **frankh-3f** — hosted aarch64/i386/arm32 read-only segment (exact recipe
  already in its ticket), then RTTI/VMT after measurement.
- **franks-ee** — the TLS prescan, route A. `ApplyTlsUserBytesOption` already
  exists and runs at the right moment; `uses` must force the area back ON.
- **frankb-56** — gate the RTTI registry on the `AN_RTTI_REG` NODE at the
  `if regEntries > 0` arm in `rtti_emit.inc`, never on `uses typinfo`.
- **neo-a2** — PIL, hold lifted. Untouched and unclaimed, so it starts clean.

**CALM PACE is the standing constraint and is not a softener:** one group at a
time, depth over breadth, land green where the gate is cheap, push often, no
widening gates. Tokens are still wanted for the new project.

**THE TIMER'S PROMPT WAS THE DANGEROUS ARTEFACT AND IS REPLACED** (`6bfa7765` ->
`66e70136`). It said *"THE FLEET IS PARKED"* and *"this is a WATCH, NOT A
DISPATCH"* — true when written, false within the hour, and **a cold tick would
have obeyed it and re-parked everyone, hourly, with no signal that anything was
wrong.** That is a stale imperative being obeyed by tooling while false in the
world, in this note's own machinery. The new prompt says the fleet is WORKING,
names each seat's task, and scopes the role to relay and topic-collision.

**Two things I got wrong in the reporting itself, worth more than the misreading
because they are habits:** I reported "four seats parked" on eleven consecutive
ticks **from this note rather than from `ListAgents`** — quoting a record and
calling it a state, which is exactly what I had praised frankb-56 for NOT doing
hours earlier. And the roster moved under me: **`regenmeter-32` started ~20:30 and
went unnoticed for twelve hours**, because every tick checked git, tstate and disk
and none re-listed the seats. The night-watch checklist had no roster step; it
does now.

**Live gap the seats need to know about:** breadth was **16h stale** at 08:17 — no
full tier since 2026-09-18T13:03Z, so there is no cross-target verdict on this
tree, and three of the four resumed seats are doing cross-target work.

### Toko tick — 09:17. Breadth gap CLOSED, and borg refuted the flake attribution by itself.

**Breadth: 16h stale -> 1 MINUTE old, 3 commits behind.** borg ran a full tier at
`a330bf12fed7`. The caveat I attached to all four seats this morning is retired;
cross-target verdicts now exist on this tree.

**`test-threads#...deadlock_diag` CLEARED** (`dd89a970c`, FIXED at
`a330bf12fed7`) and **borg filed its own refusal to close it**: `8f8cd83de`
*"green but NOT closed (race-unsafe on one green)"*. That is an independent
instrument reaching last night's conclusion by a different route — the job went
green on a tree that CONTAINS `c44fa2642`, so the `bad=c44fa26429e9` attribution
is refuted by data rather than by my argument. Open regressions 7 -> 6. **borg's
own "one green does not close a race" is the better guard and it is automatic;
tick 1's annotation is now corroboration, not the finding.**

**Seats all working** (`ListAgents`, looked rather than quoted): frankh-3f busy,
franks-ee busy, neo-a2 busy, frankb-56 in shell. `/tmp` 3% / 1%.

**Landed since 08:30:** `a330bf12f` + `300d76389` (frankh-3f — read-only segment
now on EVERY hosted target, three tiers green in full); `a2a7d408e` (franks-ee —
route A, hello.pas bss 38,396 -> 35,324); `43b4d9e45` (neo-a2 — PIL differential
no longer inert, 32 rows byte-identical under pin v411); `04ce1c7f1` (neo-a2 —
pins the method-passes-a-list shape: **the defect is gone and the cause is not
known**, so it pinned the shape rather than claiming a fix).

### Toko tick — 11:xx. Two corrections against ME, both from measurement.

**1. BREADTH: I said the gap closed and it did not. franks-ee checked instead of
quoting me and was right.** The tier is FRESH as an instrument (18m old) and has
tested NONE of today's work: `merge-base --is-ancestor` puts `a330bf12fed7`
BEFORE route A (`a2a7d408e`), the inliner fix (`4d1a64eba`) and both NilPy arms
(`6a87b7f89`, `9eb283728`) — **9 testable commits behind, and the tier itself is
RED.** I told the owner and two seats "cross-target verdicts now exist on this
tree" and the tree had moved under the claim. **FRESH IS NOT COVERING**, and
franks-ee named why nobody catches it: *"the direction the number moved was the
one I wanted, which is the direction nobody checks."* Retracted to frankh-3f and
franks-ee; frankh-3f's own per-target scoping was right all along.

**2. CLAUDE.md's OWN worked example was stale, and it decays toward wasted
work.** The F-lane rule cites `threading` as *"a real program imports it and it
does not exist at all"*. Measured today: `lib/rtl/mimic_threading.pas` is **553
lines, 25 routines, ZERO `NotImplemented`/`PAL_ERR_UNSUPPORTED` stubs**, with
FPC's `BeginThread`/`EndThread`/`TThread` surface present and tested beside it.
What remains is a HOLE (`feature-b-the-rtlevent-family-...`, p35), not an
absence. **I nearly dispatched franks-ee to build a module that already exists**
— the FOURTH stale pointer to bite in one day. Corrected in place: the owner's
quote and the attention rule stand untouched; only the factual half is retired,
dated, with the measurement that retires it. **A worked example decays faster
than the rule it illustrates.**

**franks-ee's own corrections, which is the pattern of the day:** its blocker on
the NilPy TLS arm was imaginary — it feared a SILENT collision of per-thread
state and the Pascal allocator **errors loudly**, naming unit, line and flag, so
worst case is an acceptance regression carrying its own remedy over a population
of zero. It had that loud-failure property in its own comment and reasoned from
fear without re-reading it. **And the silent case it feared exists one frontend
over:** C's arm WARNS and compiles — one copy shared by every thread, a data
race surviving a green build. Filed in C's lane, p50, as a fork in goal terms.
It also caught its own census error (`__rttireg` "called once": three sites,
`lib/pcl` was outside the glob).

**Landed today:** read-only segment on every hosted target + the silent
full-disk fix (frankh-3f); TLS route A, the `-O2` inliner threadvar
segfault, both NilPy arms (franks-ee); RTTI registry gate (frankb-56); PIL pin
verification + a pinned NilPy fixture (neo-a2).
