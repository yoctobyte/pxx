# Steering — 2026-09-19 (coordinator: frankz-e5)

Took the coordinator role from frankuser on the owner's instruction, after the
RAM reboot. Handover: `resume-after-reboot-2026-09-19.md`. Earlier detail:
`steering-2026-09-18.md`.

## 2026-09-19 — first tick

### Pin v412 — `71ba24727`, owner-approved, pinned by franks-5b
- Checked here: on origin; `stable_linux_amd64/default/last.sha256` =
  `a6a2a1cc2278…`. franks-5b's own pre-pin checks: `converged after 2
  round(s)`; `gate.sh quick` GREEN 23/23, fixedpoint and FPC canary included.
- Grade **reds(10) full + reds(1) slow**. These come from borg at `b4104386a`, which
  built the same binary; nothing from there to the pin touches code. Range
  8d9d69bdc..934f40376, 428 commits, 86 in compiler/ or lib/.
- The two NilPy NEW-REDs are fixed by frankh-c0 in `c101128f6`, and that fix is NOT an
  ancestor of the pin (checked in that direction), so the reds stay red under
  v412 and clear at v413.

### Fleet
- **frankh-c0 (N)**: the two reds were its own `d90ddd2b7`. The late-layout
  override refusal also fired on pylib bases (TPyList). Fixed in `c101128f6`. **Full
  test-nilpy tier GREEN at `c101128f6`, 982/982** (binary `1d04915e8a77`, clean
  tree, rebuilt first, verdict read from the log). That closes the verdict left open at shutdown.
  Next: TSP census.
- **frankb-8e (A)**: Route 2, stages 3–5 as a group, starting with section layout,
  checked against readelf/ld.
- **franks-5b (S/A)**: ESP group. Entry point: slow-tier red
  `test-esp-idf#src:tools/compiler_srchash.sh` (esp32c3, an esp_timer callback
  triggers a watchdog Guru Meditation and the board appears to reset in a loop).
  It reproduces at HEAD before any bisect. It re-claims the parked p85
  `bug-a-nilpy-on-cross-targets-four-remaining-walls` only if the timer failure leads there.
- **franks-5b ESP result**: `fe2d955b4`, a real runner defect. Every esp_run.sh
  shared one IDF project dir and the fixed `/tmp/esp_run_flash.bin`, so a run started
  10s after another printed the OTHER program's output (2/2), and a caller grepping
  for its own `ok` would pass a program that never ran. Now it takes flock on the project and uses a
  per-run mktemp image; esp_flash.sh got the same fix (syntax-checked only, no board).
  **It is NOT shown to be borg's timer red**: HEAD ran 10/10 clean on c3+s3 and 4/4
  under CPU starvation. Borg's reds are watchdog resets mid-run, not a program swap,
  and its tiers ran sequentially. **Residual owner**: borg's next slow tier. If still
  red, the tstate regression stays live and the cause is elsewhere. Nobody waits on it.
  The p85 cross-target ticket stays parked (`unfinished/`): the ESP work did not lead there.
  franks-5b ended its turn and is left idle on purpose (calm pace).
- **frankb-8e — ROUTE 2 DONE** (`d04ba0ab9`; stages `0b4608f85`, `cfccdc53f`,
  `401c308ac`; plus compiler fix `7700e99f1`, a LoadFile path given as an array element or field read a garbage handle).
  `pascal26 --link` links all 521 objects of the 394-applet busybox with no ld, no
  assembler and no libc. Per frankb-8e the result matches the gcc build over 938 cases, taking 2.8s against ld's 1.0s at half
  ld's memory. Checked here: all commits on origin, ticket in `done/`. **Inert under
  `$(PXX_STABLE)` until v413.** Next group, cleared by me: the crtl-per-object size ticket
  (re-claim from frankA, who is not live), taking an approach that is correct under both answers to
  `decide-a-is-a-pxx-object-a-self-contained-runtime-or-a-translation-unit`, which I
  have put to the owner.
- No topic collisions.

### Housekeeping
- `/tmp` 1% bytes, 1% inodes.

## ~19:50 — owner priorities: LEKKERZEILEN and ESP, keep seats busy

Relayed by frankuser (not heard firsthand here): *"just watch the toko please.
make sure them agents are busy. priorities: lekkerzeilen and esp"*.

- **franks-5b → ESP, attempted from the application end.** A small real .npy on
  esp32c3 + esp32s3 × `--platform=esp` (IDF) + `--esp-profile=bare`, run under qemu,
  first wall per cell. Re-claims the p85 `bug-a-nilpy-on-cross-targets-four-remaining-walls`
  and re-measures its bare-metal `PXXVarBinOp` wall, because
  `bug-a-builtin-pas-calls-a-declaration-that-esp-compiles-out` is in `done/` and the
  summary may be stale.
- **frankb-8e**: crtl-per-object group underway. `785d8a4cb`: `--function-sections`
  now emits one `.text.<name>` per function on x86-64, and objects built without the flag are
  byte-identical. One-printf C program: `ld --gc-sections` keeps 62727/328439 B of
  .text and runs (compiler `a8ebd47aca6d`). Next: section GC inside `--link`, oracled against
  `ld --print-gc-sections`. **Not on tonight's list. It goes to lekkerzeilen/ESP when it
  finishes this group.** Deferred for Track D: `docs/examples/minimal-linux-system.md:195`
  goes stale once the busybox number exists.
- **frankh-c0**: TSP census. It goes to lekkerzeilen blockers when it comes free. The pxx
  blocker list comes from lekkerzeilen-7a directly. Known candidate, unticketed:
  **ctypes** (`devdocs/dev/ctypes-under-pxx.md`). It must land whole or not at all,
  because the `try: import ctypes` arm flips silently at compile time.
- **20:10 — frankh-c0 → lekkerzeilen NilPy blockers.** It gets the fresh list DIRECTLY
  from lekkerzeilen-7a, which is re-running the demo, and does NOT re-run the whole app itself
  (that would be two seats on one question). Meanwhile it re-measures the 09-16 items at HEAD
  as isolated probes, groups them by mechanism, and fixes them. The ctypes start needs a word
  with the coordinator first (whole or nothing).
- **20:15 — frankb-8e: crtl line landed and STOPPED** (`a2126afa1`: `--link` GCs
  sections from the entry by default; `--no-gc-sections` turns it off). busybox, 19 applets / 86 TUs /
  132 cases, compiler `f00848234ed2`: `--emit-obj` 37,855,248 B → with
  `--function-sections` 3,859,936 B, both GREEN (upstream 116,848 B, so 279x → 33x).
  The rest (1.73 MB of code and 1.40 MB of data, per-object private runtime) cannot be merged
  without changing what an object exports, so the feature ticket moved to `blocked/`, blocked-by
  `decide-a-is-a-pxx-object-...`. Inert under `$(PXX_STABLE)` until v413.
  **Next group: lekkerzeilen, the A side**: `bug-a-something-in-lekkerzeilen-s-startup-still-leaves-an-exception-frame-on-the-chain`
  (p70, re-claimed from frank-user). Re-measure at HEAD first; ~/lekkerzeilen is
  read-only (it belongs to lekkerzeilen-7a). Told to tell frankh-c0 it holds the EH-frame
  question (the "Wayland segfault after init" on the 09-16 list may be the same thing).
- **For Track D, when one is up**: `docs/examples/minimal-linux-system.md:195` is stale:
  `--function-sections` now produces per-function sections as well as relocations, and `--link` GCs them.
- **20:25 — lekkerzeilen-7a's fresh blockers**, measured with the PINNED compiler
  `a6a2a1cc2278` (v412), not HEAD. Filed directly with frankh-c0; repros are in
  `~/lekkerzeilen/devdocs/pxx-blockers/`.
  01: the unmodified tree does not COMPILE. `self.x = mod.FLOATCONST` (app.py:745) gives
  "cannot infer the type of field" (7-line repro).
  02: WRONG VALUE. `f(d.pop(k))` passes the object's repr STRING, so every streamed tile
  mesh becomes a str and `App.close()` dies on `.delete()` (13-line repro).
  With 01 worked around, `--probe`, `--starts` (identical to CPython) and `--conform` pass. The chart
  builds in 4.2 s compiled against 0.4 s under CPython: a 10x perf gap, **unowned, and not yet a blocker**.
- **frankb-8e: EH-frame ticket CLOSED** (`9d364688a`, `b00ffe7b3`; in `done/`, checked
  via `ls-tree origin/master`). Re-measured at HEAD, pxx `0f7b946e8b2e` against lekkerzeilen `01d0fec`:
  EXC_TOP == rsp at app.py:780/781/784, 4/4 runs print their diagnostic. The 09-14 producer
  cannot be rebuilt (it was measured on an UNCOMMITTED lekkerzeilen tree), so it stays unnamed. The reopen signature
  is in the summary. The sibling grep found a LIVE third route: a `goto` into or out of a
  try/finally left a dead frame. It is now refused at the goto's line, as FPC refuses it. The unwinder's stale-frame
  walk no longer segfaults before printing, and it has its first test (`PXXDBG=a.gotocross`).
  Filed `bug-a-a-label-inside-a-finally-body-is-a-duplicate-ir-label`. Inert until v413.
  Next: split lekkerzeilen blockers 01/02 with frankh-c0 directly, or take the 10x chart perf gap.
- **~21:00 — franks-5b: THE ESP32-C3 IDF CELL IS GREEN** (`399712fd2`, checked on origin). A
  static Python app (a class, a list, a loop, `//`, print) runs under qemu, byte-identical to CPython.
  `examples/esp32/nilpy-c3`, `PXX=compiler/pascal26 ./build.sh qemu-assert`, and it is in
  demo-status.md. **Under QEMU, not on a chip.** Inert under the pinned default until v413.
  Seven walls cleared in one cell: arena in BSS on IDF; softfloat pulled; riscv32 IR_SYSCALL answers ENOSYS
  on ESP; ParamCount/ParamStr; riscv32 exit arms keyed to TargetPlatform; print
  goes through libc stdout (write(1) is EBADF under IDF); the program end deletes its FreeRTOS task.
  S3 IDF: walled by `bug-a-xtensa-an-exception-landing-pad-cannot-reach-past-128-kib`
  (p55), then `...-silences-write-and-busy-parks-at-exit` (p45). Bare C3/S3: still
  `PXXVarBinOp`, by design (builtin is unsupported on bare), parked on feature-bare-esp-supports-uses-builtin.
  Filed `bug-a-dce-drops-a-called-body-on-the-riscv32-idf-profile` (p45), so the image is ~3 MB
  and needs a 4 MB partition. **Next: the S3 cell** (the owner's hardware), plus narrowing the goal file's
  "compiles Python to ESP32 is FALSE" to its true scope.
- **frankb-8e: chart perf.** `578ca2ca9`: a Char literal passed to an AnsiString value param is now
  a static const_str. The old ShortString temp plus PXXStrFromLit cost one PXXAlloc per NilPy variant `<`/`>` via
  PyOrdCheck. Chart bench (lekkerzeilen `01d0fec` with a workaround, min-of-5): 3.97/3.88 → 3.60/3.67 s,
  against CPython's 0.42 s. **The main lever is TYPING, not the runtime**: ~1M `values[...]` reads in
  `Tiles._sound` at ~1.1 µs each go through dynamic dispatch, because `self.values = array.array("h")`
  types the FIELD as a variant. frankh-c0 owns that (the same pre-pass as blocker 01), queued after 01/02.
  frankb-8e is not duplicating it. Split with frankh-c0 and in flight: `len()` of a variant shim object raised
  TypeError, and `if x:` on a variant ignored `__len__`/`__bool__` (an empty array read True, a
  correctness bug). Next: `list(array)` takes 1.88 s against 0.011 s.
- **~21:40 — franks-5b: THE ESP32-S3 IDF CELL IS GREEN under qemu** (`b4a92ea58`, `fcd8354f6`,
  checked on origin). `examples/esp32/nilpy-s3` shares build.sh/main.npy with nilpy-c3, and C3 re-ran
  green. Walls behind walls: the landing pad past 128 KiB; nested windowed EH frames sharing the
  spill stack; calls with more than 22 argument words (the extras now go below sp via MOVSP); virtual/indirect/ctor call sites
  passing Int64/Double as one word (quotients printed /16); the encoder silently MASKING
  out-of-range immediates, now refused; Write silent plus a busy park at exit. New tests in test-xtensa
  (both ABIs) and test-esp-idf, each failing on v412. The goal file now reads: TRUE for C3+S3 under
  IDF, UNDER QEMU, no silicon; bare still FALSE. The cross-targets ticket went p85→40 (its ESP reason is used up).
  **For the owner**: the S3 needs `--xtensa-long-calls`, and
  `feature-a-xtensa-should-not-need-a-flag-to-build-a-large-image` is held at p35 on a premise
  that is no longer true (now two programs need it, and one is this app). Prio left for him.
  **Next**: `bug-a-dce-drops-a-called-body-on-the-riscv32-idf-profile` (images ~3 MB).
- **Inert-until-pin pile**: 19 commits touching compiler/ or lib/ since v412. They include `--link`
  with GC, both ESP demos, the EH-frame fixes, and the perf fix.
- **~22:05 — franks-5b: DCE works on the IDF profile** (`357d13162`, on origin). Cause was neither
  riscv32's nor the profile's: a body whose range holds a stub target is KEPT (something jumps into it) but was
  never MARKED live, so its outgoing calls were never followed and its callees were compacted away.
  Such a body is now a root before the fixpoint. Per-target difference is only which bodies end up
  kept-but-dead — riscv32 71, xtensa 0. Flashed image, same program: C3 3,326,224 → 2,307,648 B (-31%),
  S3 3,246,288 → 1,996,848 B (-38%), both still booting once with CPython's output under qemu. The stock 1 MB
  factory partition would need -66%, so the demos keep the 4 MB table and still build WITHOUT `--dce`
  (`PXX_EXTRA_FLAGS` added to build.sh). Positive control was HEAD with the mark loop reverted, NOT the pin,
  which refuses the program earlier for an unrelated reason. Banked without chasing:
  `bug-a-riscv32-dce-keeps-135-more-bodies-than-xtensa-on-one-program` (p25, ~344 KB). Retired a stale
  goal-file line claiming dce.inc refuses every non-x86-64 target; wasm32 is the only one left.
  **Next, offered**: (a) a NilPy demo that touches HARDWARE (gpio/timer exist in Pascal only) — recommended,
  and the one that could retire the UNDER QEMU caveat if the owner flashes it; or (b)
  `umbrella-an-esp32-image-is-as-small-as-it-can-be` (p70, unowned).
- **~22:20 — frankb-8e: chart perf done as far as it is his.** `578ca2ca9`, `7a8db5e97`
  (len()/truthiness of a variant consult `__len__`/`__bool__` — an EMPTY array.array had read True, a
  silent wrong branch, not a perf item), `06defc393` (the old-style sequence cursor looks its dunders
  up once, not per element: `list(array.array)` over 786432 items 1.73 → 1.18 s). Chart bench, same driver
  and min-of-5 throughout: 3.97 → 3.60 → 3.40 s (the last including frankh-c0's `0664366e7`), against
  CPython 0.42 s. **The remaining lever is `values, base = grid.values, grid.base`: NilPy types an
  unpack target as the TPyList the unpack builds, not as the element**, so the field-typing fix cannot
  reach it — `bug-n-a-tuple-unpack-target-is-typed-as-the-tuple-not-as-its-element` (p55), taken by
  frankh-c0 and ranked above its queue. frankb-8e is not touching it: two seats on one inference
  question is the collision the roster exists to stop. **A WRONG class is worse than none**: annotating
  `grid: world.Grid` gives 16.85 s, 5x SLOWER than the variant, while the bare import is 3.55 s.
  **Next, in order**: ask lekkerzeilen-7a directly for a non-NilPy blocker toward the first frame; failing
  that, `umbrella-an-esp32-image-is-as-small-as-it-can-be` (p70), split with franks-5b, which holds the hardware-demo question.
- **~22:40 — franks-5b: a Python program drives GPIO and is paced by an IDF timer callback**
  (`81d317926`, on origin; `examples/esp32/nilpy-hw-c3` and `-hw-s3`, green on both chips under qemu).
  **Read the pass narrowly, and relay these caveats unchanged**: the TIMER half is witnessed (the
  Python loop sees `timer_ticks()` advance); the LED is NOT (qemu models no observable pin, so this is the
  demo worth flashing first — silicon retires two caveats at once); `main.expected` is the
  program's SPECIFICATION, not CPython's output (CPython cannot run it), and build.sh no longer
  says "== CPython" for these; and **Python POLLS a counter — it does not take the interrupt**
  (`bug-n-a-pascal-function-handed-to-a-procedural-parameter-from-nilpy-is-not-a-code-address`).
  Mechanism is the crash course's pattern, not a new wrapper: `esptimer.pas` gained
  timer_start_periodic_ms/timer_ticks/timer_stop/sleep_ms and `espgpio.pas` is new, each carrying a Python surface
  beside its Pascal one. `gpio_config` deliberately absent: that struct's last field is per-chip.
  **Census finding worth remembering**: xtensa had NO `IR_ZERO_SYM` arm while the other five backends did —
  the same wall that cost arm32 34 of 52 NilPy tests in August. **An absent backend arm is invisible
  until a program of that LANGUAGE reaches the target**, and the earlier rows were measured by
  compiling Pascal, which never emits that node. The demo is its own positive control (a def with a local).
- **Collision avoided**: both frankb-8e and franks-5b were pointed at the small-image umbrella.
  Asked frankb-8e which it took; first answer wins. franks-5b offered the NilPy
  procedural-parameter ticket (p55) meanwhile, which its own demo motivates.
- **~23:10 — franks-5b: what an ESP NilPy image is MADE OF** (`880472933`, on origin). Every sized
  symbol in the `--dce` object for nilpy-c3, attributed to the file that declares it; unattributed
  remainder 0.6% riscv32 / 0.8% xtensa. pylib 53.6/47.5%, **pyeval 30.1/39.3%**, promocore 7.6/5.6%,
  builtinheap 4.1/3.4%. Compare WITHIN a column, never across: xtensa instructions are shorter.
  **30-39% of the image is a tree-walking `eval()`/`exec()` interpreter for a program that never calls
  eval** — 624,684 B live on riscv32, `PyHostCall` alone 109,396 B (5.3% of the image in one routine), and
  pyeval's own header says it is "NOT auto-used by NilPy yet". The tell that a ROOT holds it rather than a
  call: `Tokenize` is DROPPED while `ParsePrimary`/`ParseMethodCall`/`DoAssignment` live — a parser
  outliving its own lexer is not being reached through a call.
  **LIVE is not REACHABLE** and the measurement does not settle which: pylib declares hook variables
  pyeval installs into. Nothing deleted or guarded. New rung
  `bug-a-a-static-nilpy-program-links-the-runtime-eval-interpreter` (p60).
  **Next, approved**: a per-body root report (`--dce-report` says which bodies DIED; nothing says why one
  LIVED), naming the first root and the chain, with a positive control on a known VMT-reached and a
  known call-reached body. The p25 lead is priced and folded in: the 122 bodies riscv32 keeps and
  xtensa drops are 381,416 B, 18.4% of its image — **direction to rule out FIRST**, because if xtensa
  emits a code-offset call without recording a CodeRef, its smaller live set is a pass running blind, not a win.
  Rungs taken, umbrella owner left empty per that file's rule; two stale rows in it retired.
- **~23:30 — frankb-8e: threading group.** `974db9eb9`: on wasm32, `import threading` told the user to
  rebuild with `--threadsafe` and the driver then REFUSED that same flag — advice and its rejection in
  one sitting. The two refusals now differ (flag named where locks exist, threading reported absent where
  they do not) and the target list moved into one authority, `TargetHasThreadSafeLocks` (util.inc), called by
  both the driver gate and the import site. **CORRECTION TO MY OWN FRAMING**: this ticket is
  ergonomics, not capability. lekkerzeilen compiles today WITH the flag; it is unwelcoming, not blocked.
  I told the owner otherwise and have corrected it. Cost of `--threadsafe`, measured interleaved,
  min-of-5 best-of-3, compiler `6b3f65304a6e`: chart phase +0.4% (3.384 → 3.396 s), alloc churn +2%,
  console I/O 0%, hello image +0.9%, **string/ARC churn +26%** (0.043 → 0.054 s, CPython 0.031) — small and
  concentrated in refcount traffic. Two assumptions became measurements: the mode cannot be switched on
  at the import (builtinheap and thirteen units are already compiled at that line, probed not assumed), and the
  owner's waived "void all and restart" is ~+15%, not a doubled compile (lekkerzeilen 122 s, the import
  refused at 18.5 s) — though it would still need in-process state reset, as the compiler deliberately has no execve.
  **Escalated, not implemented**: `decide-should-a-python-program-that-imports-threading-compile-as-written`
  (`70fc564c3`), stated in one sentence with no implementation noun. The stance being weighed is the
  owner's own — threads are opt-in because pxx answers concurrency with coroutines — and it was set
  before anyone had the price. Recommendation in the ticket: look at the imports, so only programs that
  ask for threads pay. **Next**: the procedural-slot ticket (p55), released by both other seats with their
  measurements banked in it.
- **~23:55 — franks-5b: `--dce-why` is built and answered pyeval on its first run** (`10c4a6262`,
  control `test/test_dce_why_root_report.pas` in test-quick, both on origin). Prints
  `<name> <- <caller> <- ... <- [root kind]`, 8 hops, plus live bytes by reason; the CHAIN is what made
  it useful, the root kind alone said `[@proc taken]` and named nothing. Control asserts the chain,
  with one body reachable only via a VMT slot and one only by a direct call placed PAST hop 1.
  **The control's own lesson, worth banking**: written flat, the call-rooted helper was INLINED at the
  default -O and correctly DROPPED, so the first run came back empty — *"a silent report and a broken
  filter are the same output"*. Both helpers are self-recursive now and a matched-but-dead name prints
  `<- DROPPED`, so absence and silence stop looking alike. Asked for it in debugging-playbook.md, NOT
  CLAUDE.md: one instance, and the promotion test is a second independent subsystem.
  **The finding**: pyeval is not kept by any call from the compiled program —
  `PyHostCall <- PyFieldGet <- DoAssignment <- ExecStatement <- ExecSuite <- CallUserFn <- PyBodyTramp <- [@proc taken]`.
  Four `@proc` roots hold 17 KB directly and drag 624 KB through ordinary call edges. Ticket p60
  rewritten to that mechanism. Why riscv32's column was uninformative: 143 stub targets, 139 landing
  INSIDE a body, so 128 bodies are rooted before the reachability walk runs and the @proc chain is never
  first. xtensa has 4, none inside a body. **The report counts RECORDED CodeRefs**, so an unrecorded
  code-offset call is precisely what it cannot see — rule that out before anyone spends riscv32's extra 381 KB as a win.
  **Next, approved**: design against the @proc root — does PyBodyTramp's address need taking in a
  program with no Python-bodied words, which is every NilPy program today. Compiler+pylib, not pyeval.
- **~00:05 — frankh-c0: the unpack-typing lever is fixed** (`47841c55b`, on origin; blockers 01/02 in
  `9c70e35f6`, two of frankb-8e's five gaps in `8fbabc2c3` and `0664366e7`). Wider than the ticket said:
  `PyParseUnpackAssign` drained EVERY unpacked value through `PyDrainIfCursor`, which materialises
  anything declaring `__iter__` into a TPyList — correct for `a, b, c = bag` where one value is INDEXED
  into several targets, wrong for a value per target where nothing is indexed. So every parallel-unpack
  target holding a shim or user iterable was a list, not just array. *"The builder was never wrong; the
  value had been replaced before it got there."* Full test-nilpy GREEN, gate quick GREEN after one
  reviewed AST-slot snapshot row, pinned positive control fires.
  **A claim that does NOT reproduce, recorded rather than filed**: the same ticket's second item said an
  annotation naming an unimported module is accepted silently against CPython's NameError. At HEAD pxx
  WARNS and types it Any, in every shape; and this box's only CPython is 3.14.4, where PEP 649 defers
  annotation evaluation, so there is no NameError to compare against. Holds for <= 3.13, and no such
  interpreter is here. Residual left with the two seats holding the tree that produced it.
  **Next**: tell frankb-8e the typing fix landed (its chart re-measure is the deliverable), then the top of
  `ready --track N`, preferring rows a real application hits, dropping for lekkerzeilen-7a's blocker 03+.
  ctypes still unstarted, as it should be while the owner holds the fork.
- **~00:30 — franks-5b: the @proc lead was a DECOY worth 1,036 bytes, and the real root is one arm of
  one dispatcher.** In flight, not landed; tiers running, and it will not commit until the job's own
  verdict line says green (the wrapper said exit 0 over rc=2 last run).
  **@proc answered**: the only site taking `@PyBodyTramp` is inside `EvalPyStmts`, a body DCE itself
  drops — an @proc inside a deleted body writes nothing, so it is now an EDGE (owner → target) resolved
  in a fixpoint beside the call walk, and only an @proc in code no body owns is a root. Sound because a
  jumped-into body is already marked live. Chain now spells `<- X` (calls it) against `<- @X` (takes its address).
  **The real root**: pyeval's initialization runs `PyIterCallHook := @PyCallKey1`, which IS written by code
  that always runs, so rooting it is correct and no precision fix reaches it.
  `PyHostCall <- PyFieldGet <- DoAssignment <- ExecStatement <- PyClosureInvoke <- pyclosure_call1 <- PyCallKey1 <- [@proc in unowned code]`.
  **Priced by REMOVAL, not a proposal — both removals break the program** (xtensa, nilpy-c3, --dce):
  HEAD 1,721,914 B live; whole init removed 712,617 B (-59%); only the `pyclosure_call1` arm removed
  824,155 B (-52%). riscv32 is 2,070,376 B in all three — its stub-target rule roots the same bodies
  independently, so the win does not appear there until that rung moves. Quote the caveat with the percentage.
  **Design endorsed**: route that arm through a second hook installed by `PyExecSrc`/`EvalPyStmts`, the only
  two entries that can mint such a closure, so the tree hangs off exec() by construction — a reason the pass
  can SEE. The unconditional PyIterCallHook install STAYS: lazy installation caused
  `bug-nilpy-min-max-with-a-key-held-in-a-variable`.
  **A red it caused and self-reported**: `undefined symbol: putchar` on test_platform_defines
  --platform=esp, from the IDF Write fix. Two spellings of "IDF" differing on exactly one configuration
  (`PXX_ESP_IDF` versus `PXX_PLATFORM_ESP and not bare`, hosted `--platform=esp`), **compiling and linking
  fine either way and failing only when RUN**; the identical mistake was already guarded for `calloc` in
  the same file. Attributed to a RANGE with a pinned control before assuming it was its own diff. Asked
  for it in the playbook.
- **~01:40 — frankh-c0 re-fed.** It ran out of queue rather than finishing a step. Group given:
  finish frankb-8e's remaining gaps (#2 and #5 by my count — asked frankb-8e directly rather than
  re-deriving), then `feature-nilpy-math-module-twelve-absent-names-measured` (p90, measured already, so
  implementation not investigation, and a sailing simulator will want it), then the top of
  `ready --track N` preferring rows a real application hits.
- **lekkerzeilen-7a has shown "waiting" for 2h+ and did not answer a 23:37 status check** (frankuser).
  Possibly a permission prompt only the owner can clear. Nobody has touched its pane, correctly. **So
  lekkerzeilen blocker 03+ is not coming tonight**, and no seat holds work for it. The owner has been told.
- **~01:50 — frankh-c0 was NOT idle (a tier was running), and it refuted my math dispatch.**
  `fece1a3ea`: population = every `math.<name>` in lekkerzeilen and tuxspaceprogram, 29 distinct names,
  1129 occurrences. **NOT ONE of the fifteen absent names appears in either tree**, and the 27
  comparable names they DO call all diff clean against CPython 3.14.4 at %.12g. My line — "lekkerzeilen
  will want them the moment it renders" — was a quantifier I invented beside a real fact, and I withdraw
  it. Ranked on application need the row is a zero; recorded in the ticket with what would retire it
  (a target application importing one of the fifteen). The title is oracle-dependent besides: "twelve
  absent" and "eighteen absent" are one measurement against two CPythons.
- **TSP census re-run at `47841c55b`** (67 files, oracle = compile success): 38 OK against 37, expectation
  written first, and the +1 attributed to the RANGE, not claimed. Walls by MECHANISM:
  **ctypes 10 of 67 — the single biggest wall in TSP** (3 import it directly; 7 fail on `undefined
  variable (_backend)` because the backend binds only in the `else:` arm of a guarded import). **So TWO
  applications now wall on the same fork the owner holds** — relayed as new evidence, nobody starts it.
  dataclass 6 files: the ctor's field cap read `> 15`, one less than MAX_PROC_PARAMS when that was 16, and
  stayed behind when it became 32 — a constant outliving the constant it was derived from. Now derived; Scene
  (17 fields) had walled four files. Rest: `undefined variable (os)` 3, `Coast has no method get` 2,
  threading 2, random arity 1, keyword-through-callable 1, tuple-unpack refusal 3.
  **Caveat to carry with those counts**: three "different" files failing at line 161 and two at 236 are each
  ONE site in an imported module printed with that module's line number. File counts are not work counts.
- **The find it stayed on, and I endorsed**: `def __init__(self, target: tuple)` then `self.target = target`
  gives the FIELD tyClass with NO class identity (rec=-1). `self.target[0]` COMPILES and answers
  -1855979488 where CPython gives 3.0 — a silent wrong value in live application code (TSP landing.py),
  and REMOVING the annotation makes it correct. Second instance in one evening of *a wrong type costs
  more than no type*; asked for a playbook entry when the fix lands.
- **~02:10 — franks-5b: a stale COMPILER WARNING, which is a hazard block with a bigger audience.**
  Second red from its own landing, and the better one. The `espnoop` test row asserted that
  write/writeln are a no-op on BOTH ESP profiles — correct when written, and reversed by its own IDF
  fix (PXXSysWrite to the libc stdout stream, booted on both chips, UART diffed against an x86-64
  oracle). The row did its job. **The compiler was still telling every IDF user
  `write/writeln emits nothing in an ESP-IDF object`**, pointing them at esp_rom_printf — false, and the
  warning's own comment block had PREDICTED this failure mode three weeks ago before falling into it one
  behaviour change later. Removed rather than reworded (nothing left to warn about on that profile);
  bare still warns, with what would retire that arm recorded (a bare console, not a measurement).
  Measured, `--emit-obj`, empty vs `WriteLn('hi')`: xtensa bare 732→732 and riscv32 bare 276→276, both
  no-op and warned; xtensa IDF 211844→211860 and riscv32 IDF 261568→261608, both PRINTING and warned at
  anyway. The row now pins the DECISION plus the invariant that outlives both eras (the two backends must
  agree on the same profile), with the pin as the control for THAT ARM, not as a general control it
  cannot serve.
  **Promotion decision, said out loud**: this is an EXTENSION of CLAUDE.md's existing hazard-block rule
  — same class, second medium, wider blast radius — so one sentence added to that paragraph, evidence in
  the playbook. Not a new rule. Told franks-5b to argue back if it reads the test differently.
  **And the transferable self-criticism**: Track T had auto-filed the putchar regression a day earlier
  (`regression-test-core-test-platform-defines-2`, 2026-09-19T19:12Z, sitting in backlog/). It found it by
  running the tier, not by reading the queue — free information about its own landing, walked past. After
  landing in a shared file, check what T has filed against the range BEFORE running your own tiers.
- **~02:25 — franks-5b DECLINED to edit CLAUDE.md on my ask, and it was right.** It holds a
  session-level constraint against editing CLAUDE.md because a peer asked; a coordinator asking is still
  a peer asking, and I hold the same constraint. **My error: I routed a promotion to a seat instead of
  to the owner.** The sentence and its argument are going to the owner verbatim; if he says nothing or
  no, the playbook entry stands and nothing is lost. Its added argument, which I had not thought of and
  which is the strongest line in the case: the existing rule's remedy — *re-measure the hazard before you
  obey it* — is NOT available to a user reading a compiler warning. They cannot re-measure it. Only we can.
  Two landings that needed nothing from me: the watcher line went in the playbook rather than the
  logbook (a logbook line is a record; this is guidance needed at the moment a seat picks tiers), and
  `docs/targets/esp32.md` Mode 2 gained PROSE ONLY on writeln/readln working under IDF, with the two libc-stream
  consequences (end every line with a newline; `write(1,...)` is not the console) — no snippet, because a
  snippet there must compile against a pin that predates the behaviour and would have been red on arrival.
- **~02:40 — frankb-8e: the procedural-parameter group is closed** (`8826e6aec`, `e502d115f`,
  `8fc498700`, `d8cf64714`). `PyCoerceCallableArgsIn` is the one place a callable value is coerced into a
  raw slot and it knew Pointer/cbuf/cptrarray only; a procedural parameter is marked by
  `ProcParamProcSig` and nothing read that mark, so the callable CARRIER (a heap record) went into a slot
  that wants code and every call through it segfaulted, including from Pascal. Now recovers the named
  routine from the carrier's `AN_PYSIGREF` and stores an `AN_PROCADDR`, gated on `ProcSigCompatible` so a
  wrong-shaped routine gets a named warning instead of a silent coercion. Six-row fixture, pin control segfaults.
  **THE SENTENCE IS ONLY HALF TRUE AND I HAD ALREADY OVERCLAIMED IT TO THE OWNER** (corrected):
  this recovers an address that already EXISTS, for a PASCAL routine. **A NilPy def has no native-ABI
  entry point at all** — its only real code is `$pycallwrap_<pi>`, all-Variant, which is why
  ProcSigCompatible refuses it for a shaped C slot. `examples/esp32/nilpy-hw-c3` still polls and its
  header says why. Filed as `feature-n-a-nilpy-def-has-no-native-abi-entry-point-to-hand-to-a-c-callback`
  (p55, backlog-nilpy), deliberately UNCLAIMED because frankh-c0 owns the carrier half.
  **Next**: frankb-8e takes that ticket, agreeing the boundary with frankh-c0 BEFORE either starts.
  Acceptance test is on disk: nilpy-hw-c3 stops polling and takes the timer callback in Python.
  **The chart number owed, now measured by the non-author**: frankh-c0's `47841c55b` takes the resolving
  arm 16.85 s → 2.35 s, **32% FASTER than the unannotated 3.46 s arm** rather than 5x slower. Control:
  the unannotated arm does not move. `PXXDBG=n.locals` asserts the annotation actually RESOLVED
  (`tk=6 rec=154`, zero "treating it as Any"), because an annotation degrading to Any would have
  produced the unannotated number and read as a partial win.
  **Its own correction, landed at the sentence's location and not only in a reply**: it had claimed an
  annotation naming an unimported module is accepted silently and that CPython raises NameError. pxx
  warns twice, and python3 here is 3.14.4 (PEP 649). It grepped that compile's output for the timing
  line and never for "warning:", and asserted the oracle without running the interpreter that is
  installed — in a ticket that names its oracle on every other row.
- **~03:00 — the callback work is ONE job, held whole by frankb-8e** (claimed `60b973755`; boundary read
  banked `7f8d25a88`; **implementation NOT started**). frankh-c0 confirmed the boundary and keeps the
  carrier untouched. It collapsed to one job because the carrier does not need to hold the thunk address:
  three synthesised-adapter sites already sit on one pending-lambda queue, none touching a carrier, and
  `PyGetOrMakeCloneThunk` is already this exact shape. **The correction that collapsed it is
  frankh-c0's**: all three synthesisers build a NAME and call FindProc on it, so the (def, target
  signature) pair key is just a longer name — `$pycbthunk_<realPi>_<sigPi>` — and per-pair caching falls
  out of the existing lookup, with no structure change. It checked the sites itself rather than taking
  the description.
  **MY OVERREACH, CORRECTED — second time tonight, same shape**: I attached
  "nilpy-hw-c3 stops polling" to this ticket. It does not carry that. This ticket delivers the HOST-SIDE
  proof (a def into a procedural parameter answering 502, against CPython's 502); the demo stops polling
  only if the ISR path can be shown not to allocate, which is a SEPARATE ticket. Both overreaches were
  caught by the seat doing the work, not by me.
  **Two exclusions, frankh-c0's view recorded under its name**: nothing retains the def once a C slot
  holds a bare thunk address, so the top-level-def case ships and the case needing a live carrier is
  REFUSED BY NAME — a named refusal beats a retention scheme with no use case; and boxing into Variants
  allocates, so **an ISR that allocates is a latent crash with good latency numbers** — a non-allocating
  restricted thunk (fixed arity, scalar-only, no Variant) is a different contract and gets its own ticket.
  **First step held to**: build the smallest thunk and READ THE EMITTED IR, because "the existing body
  builder's coercion covers both directions with no new runtime" is currently a read of the code and it
  is the sentence that decides whether this is small or large.
- **~03:15 — frankh-c0: the silent wrong value is ROOT-CAUSED; tier running, ETA ~25 min.**
  Landed `e18e9f114` (dataclass ctor cap derived from MAX_PROC_PARAMS, full test-nilpy GREEN).
  **Two readers of one annotation disagreed**: `PyHeaderParamType` runs the FULL annotation grammar
  through `PyAnnTypeAt`, so `target: tuple` answers tyClass with the class already resolved (TPyList);
  its rec companion `PyHeaderParamRec` re-scanned with the SINGLE-TOKEN reader, which knows only user
  classes — and tuple/list/dict/set/bytes are not. The pair answered **tyClass with REC_NONE**, and
  **a member read against REC_NONE is an offset of ZERO**, which is why it compiled and returned garbage
  instead of refusing. The rec reader now runs the same grammar, single-token kept as fallback.
  **The BACKSTOP is the durable part** (frankb-8e's formulation): at the end of the field pre-pass, a
  tyClass with a rec below REC_UCLASS_BASE degrades to a VARIANT. *An inference step that cannot name the
  class must not claim one.* Downstream reads tyClass as "some class" and resolves members against the
  rec, so REC_NONE is not "unknown" there, it is zero — the variant says unknown in the one way the
  frontend already understands, and keeps the runtime tag. Future instances become correct-but-slower
  instead of silently wrong.
  Census: `tsp/landing.py` now COMPILES; `mission.py` and `__main__.py` MOVED to a different wall
  ("missing argument for dataclass field without default"). Moved is not fixed.
  Playbook entry to lead with the inversion: **ADDING a correct annotation is what breaks it**, because
  the unannotated field falls back to a variant. Three instances in two days across two pre-passes clears
  the RECURRENCE bar; if a third subsystem that is not a pre-pass hits it, that is the promotion
  argument, and it goes to the OWNER, not to a seat.
  frankb-8e's five gaps all accounted for (`0664366e7`, `7a8db5e97`, `8fbabc2c3`, `06defc393`, `47841c55b`).
- **~03:40 — frankb-8e: the flagged read was HALF WRONG, found in the IR before the feature was
  written.** Inbound coercion was free as read (`var_store tk=22` boxing natives into the by-ref
  Variant, no new runtime). Outbound was not: `PyCompileLambdaBody` creates `$pyresult` only under
  `RetType = tyVariant`, a thunk carries the SLOT's return type, so RetSymIdx stayed -1,
  `EmitProcEpilog(-1)` emitted nothing and the result register held whatever the call left —
  **1637568216 against CPython's 502**. *A wrong answer has no location, which makes it worse than the
  segfault it replaced.* One arm beside that condition fixes it. **Re-estimate: still small** (one
  synthesiser, one guard, one arm) — the shape changed, not the size, and **a null re-estimate is still
  a result**. Nothing pushed; full test-nilpy running, its row passed at line 518; gate quick GREEN read
  from the log's verdict line; oracle RUN rather than asserted (CPython 3.14.4, unit stubbed, diffed clean).
  **Four targets, not one**: compiled AND run under qemu on x86-64, i386, aarch64, arm32 against one
  .expected — i386 matters most (32-bit, different Double return convention, the 3.75 row goes through
  it), because a thunk exists to bridge calling conventions and x86-64 is where the dev loop, gate quick
  and the pin already agree. CLAUDE.md's structural blind spot, avoided deliberately.
  **Practice worth keeping**: (1) the "closes over nothing" guard was first written as `ASTKind = AN_NIL`
  from what nil SHOULD be; the builder emits `AN_INT_LIT 0` typed tyPointer, so that guard would have
  refused every carrier and **silently disabled the whole feature** — another instance of "an assertion
  written from a PREDICTION pins the prediction", found by reading the builder. (2) The three sibling
  synthesisers were ENUMERATED off the queue rather than recalled; the queue lists its own producers.
  (3) A capturing nested def is excluded by CONSTRUCTION — the lambda lift appends captures as extra
  parameters, so arity and all-Variant both fail — better than the hand-written exclusion.
  **Pre-existing gap, now its own ticket** (told to file it): a capturing nested def handed by NAME gets
  no warning at all (`PyCarrierNamedProc` answers -1), segfaulting identically on v412 and HEAD.
- **~04:00 — franks-5b landed green** (`ee5c84f46`, on origin; five tiers plus gate quick, verdict read
  from the job's own log line after the wrapper said exit 0 over rc=2 twice tonight). Contents: the DCE
  @proc-as-edge fix, the `PXX_ESP_IDF` spelling fix, removal of the now-false IDF write warning, the
  espnoop row rewritten to pin the decision plus the cross-backend invariant, esp32.md Mode 2 prose,
  three playbook sections, and the p60 ticket rewritten to the measured mechanism.
  `regression-test-core-test-platform-defines-2` resolved with its cause written in.
  **Its own account, and my counterweight**: it notes that two of tonight's three findings were its own
  regressions, found late, one already in the watcher's queue with its range on it. Accurate, and worth
  having a seat who writes it. The half it understates: **both were caught before they reached anyone,
  inside the same session, by its own process**, and the fix that fell out of the second — a user-facing
  warning that had gone false — is worth more than the regression cost. The debt is paid by the playbook
  line (check what T filed against your range before choosing tiers). Not to be carried forward as caution.
  **The 52% guard rail, to hold in every relay**: priced by REMOVAL, not a proposal; ONE program, ONE
  ISA; the removal breaks the program; riscv32 shows nothing until its stub-target rung moves. It is a
  bound on what one edge is holding, not a promise about the image.
  **Next, approved**: implement the second hook — route PyCallKey1's closure arm through a hook installed
  by PyExecSrc/EvalPyStmts, so the interpreter hangs off exec() by construction. Behind the existing
  tests plus a fixture exercising a closure through a `key=` path, measuring the same three trees.
- **Inert behind v412: 29 commits touching compiler/ or lib/.** The pin is the owner's call; he has been
  told; nobody waits on it.
- **~04:20 — frankb-8e: the ESP demo's wall is NOT the C seam, and I had relayed that wrong twice.**
  Measured at HEAD rather than quoted from the demo's header:
  a def reaching native code as a procedural PARAMETER on a Pascal routine WORKS (thunk); as a
  procedural parameter on `external cdecl` WORKS (thunk minted, ptypes 17 17); as a bare `Pointer` param
  on `external` it is a silent DATA BUFFER (the header's sentence, accurate about ITS row); as a
  procedural **FIELD** it is a SILENT SIGSEGV. **`esptimer.pas` uses the last row twice** — the callback is
  the field `TEspTimer.OnElapsed: TTimerProc`, crossing to the SDK as the `Pointer` field
  `args.callback`. So the C-side crossing already works when the slot is a declared procedural TYPE;
  the wall is that `PyCoerceCallableArgsIn` sees ARGUMENTS ONLY. `cb.MkTwo(two)` works and
  `s.two = two` segfaults with no diagnostic — same def, same signature, one door along.
  **Consequences, as given**: the demo's header is NOT retired; the ISR/allocation question is unchanged
  and still separate; the residual is the same mechanism at a second site (a field assignment asking
  the ProcSigCompatible/thunk question), with nothing about the thunk changing; and its own summary
  said "OPEN for the C side" for ten minutes, wrong in the direction that would have sent someone to
  build unneeded C-frontend work — caught by the author.
  **Practice, asked for in the playbook**: (1) *measure the SHAPE you are about to assert, not the one
  the source was written about* — third instance today, and the reason it does not feel like
  carelessness is that every source sentence was ACCURATE and was read as describing the CASE.
  (2) *a pipeline's `$?` is the LAST command's* — `$BIN | head -2; echo exit=$?` read head's success
  while the binary had segfaulted; CLAUDE.md's wrapper-versus-job rule covers background jobs and does
  not reach a one-line pipeline.
- **~04:40 — frankb-8e: both playbook lines written (479 → 481 sections), and a judgement call worth
  recording as a DECISION.** It found the field-assignment hook (`RecFieldProcSig(rec, field)` already
  exists, so it genuinely is the same mechanism at a second site) and is **NOT implementing it tonight**:
  the tier is running the binary it would have to replace to test anything, so it could write the code
  and could not measure it. Its own sentence: *writing a second site untested, at the end of a session,
  on the strength of "it is the same mechanism", is how the careful version of tonight's bug gets
  shipped.* Twice tonight it was half wrong about a symmetric-looking read and found both by building the
  smallest thing and looking. Next step on the ticket, not this commit.
  Playbook framings, each naming where the EXISTING rule does not reach: the shape/case one leads with
  all three source sentences being ACCURATE (so care does not prevent it) and discharges with a table of
  neighbouring shapes, because a single row cannot disagree with itself; the pipeline one says CLAUDE.md
  has this for BACKGROUND jobs and that the background framing is why it does not reach a `| head`, then
  adds the teeth — **a pipeline converts a CRASH into a CLEAN EXIT and the missing output reads as the
  program having nothing to say**, so both halves point away from the truth, and "succeeded but printed
  nothing" is interesting enough to think about.
  Exposure named, not acted on: eleven files unpushed at the end of a long session. Holding is correct
  while a tier runs (push before a measurement, never during), so the landing sequence is atomic —
  verdict green, rebuild, re-gate, commit, push — and a post-tier red gets the green part pushed with
  the red named in the message, not another hour of unpushed work.
- **~05:00 — frankh-c0: the silent wrong value is FIXED** (`5c2e692f9`, on origin; full test-nilpy GREEN
  at compiler `d175d3ceb88a`, gate quick GREEN, pinned control fires). Three parts: the fix at its source
  (`PyHeaderParamRec` runs the same annotation grammar as `PyHeaderParamType`, single-token USER-class
  reader kept as fallback so nothing it used to answer moves); the BACKSTOP (a tyClass with a rec below
  REC_UCLASS_BASE degrades to tyVariant at the end of the field pre-pass — frankb-8e's formulation,
  credited in the comment); and the playbook entry, led by the inversion: **"ADDING A CORRECT TYPE
  ANNOTATION IS WHAT BREAKS IT — A CLASS WITHOUT IDENTITY IS AN OFFSET OF ZERO, NOT AN 'UNKNOWN'"**,
  with all three instances and their shas. Its detection advice is the durable part: look for
  `tk=6 rec=-1` under `PXXDBG=n.locals`, and **do not check by reading the value back** — a read against
  offset zero can return something plausible. A defect whose symptom is a believable number cannot be
  confirmed by looking at the number.
  **Process worth banking**: the push hit a playbook conflict (another seat had appended in the same
  place). It kept BOTH sections — a conflict in an append-only file is not a disagreement — and then
  checked WHAT the pull had carried before re-quoting its green: devdocs and tstate only, no
  `compiler/**` and no `lib/**` (which is also a compiler build input), so the tier and gate still
  describe that tree. Had it carried compiler sources it would have rebuilt and re-gated.
  Census re-running at `5c2e692f9`, same population, **expectation written first: 41 OK from 38**
  (dataclass cap clears scene.py and view/demo.py; the annotation fix clears landing.py), with a
  commitment to report a difference as the finding rather than adjusting the story.
  Three NilPy fixes tonight: `47841c55b`, `e18e9f114`, `5c2e692f9`.
- **~05:15 — frankb-8e landed** (`2b28c3302`, on origin; nothing held unpushed). Full test-nilpy GREEN at
  `f91880a5d88c` read from the log's own rows (the wrapper said 0 and it checked anyway), gate quick
  GREEN from its verdict line, fixture OK, pin control segfaults, oracle RUN, four targets under qemu.
  **The rebuild it could have talked itself out of paid twice.** First: the post-tier rebuild came out
  BYTE-IDENTICAL (comments and a local rename do not change codegen), so the tier's verdict TRANSFERS to
  the moved tree exactly rather than by argument — a stronger claim than it had promised. Second, and
  this is the one that nearly got dropped: **the push rebased onto frankh-c0's `5c2e692f9` in the SAME
  FILE it had just changed** (`compiler/pyparser.inc`), so the tree it pushed was not the tree it
  measured. It rebuilt after the pull settled (`f17bf2485348`, converged), re-ran the fixture against the
  MERGED compiler and re-gated at that sha. **Two seats in one file, both diffs applying cleanly, is the
  collision git hides and nothing would have reported it.**
  In the commit beyond the feature: the ESP correction as the measured four-row table rather than a
  conclusion; the ticket summary corrected from "OPEN for the C side"; two tickets filed rather than
  absorbed (the capturing-nested-def silent segfault, p40 unowned, and the field residual); two playbook
  sections. One playbook conflict kept both sides in order, 484 sections — the same call frankh-c0 made
  an hour earlier, independently.
  **The field site stays UNCLAIMED in a clean tree**, with its hook named — the best thing to hand a
  rested seat and the worst thing to write in a tired hour.
- **Inert behind v412: 31 commits** touching compiler/ or lib/.
- **~05:30 — frankb-8e ENDED ITS TURN. Carry it as finished, not idle-blocked**: nothing unpushed, no
  prompt pending, clean tree at the merged sha `f17bf2485348`. It released its own ticket
  (`997102261`), my release landed on top (`011086ee0`) and I resolved in favour of its line —
  `feature-n-a-nilpy-def-has-no-native-abi-entry-point-to-hand-to-a-c-callback` is now owner-empty,
  status backlog, summary PARTLY DONE, with the next step and its hook (`RecFieldProcSig`, which already
  exists) in the body and the four-row table showing a field assignment segfaults silently where the
  argument path works. **A one-sitting job for a rested seat.** Also unowned:
  `bug-n-a-capturing-nested-def-into-a-procedural-slot-segfaults-with-no-diagnostic` (p40).
  **The practice line from its handover, which is better than the features**: it did the rebuild that
  caught the same-file rebase not out of vigilance but because it had PROMISED me it would quote which
  sha each verdict came from, and keeping that promise required going to look. **A commitment made to a
  peer before the tempting moment is a cheaper guard than intending to be careful at it** — a mechanism
  anyone can copy, where "be careful" is not.
  Its own three errors, listed by it for whoever reads the session back: "accepted SILENTLY by pxx /
  CPython raises NameError" (both halves wrong, in a ticket that named its oracle on every other row);
  two coercion directions from one paragraph of reasoning, exactly one true, the false one returning a
  plausible number rather than crashing; and "OPEN for the C side" in a ticket SUMMARY, from a header
  sentence accurate about a neighbouring shape. **Each caught by building or running the smallest thing;
  none would have been caught by re-reading.**
- **~05:45 — frankh-c0: census 41, exactly as predicted, and a FOURTH find (tier running, ETA ~25 min).**
  Census at `5c2e692f9`, same population: **41 OK from 38, the number written before the run**, and the
  row-list diff shows the ONLY additions are `tsp/landing.py`, `tsp/scene.py`, `tsp/view/demo.py` —
  nothing moved in either direction, so the delta is ATTRIBUTABLE rather than merely equal.
  **The find, another silent wrong value**: `class Site` beside `def site()` — Python's commonest naming
  convention — and `s = site(key)` CONSTRUCTS Site, because `FindUClass` folds case as Pascal does and
  Python does not. Two doors took the folded match: the ASSIGNMENT arm tested `IsClassType` with no
  exactness test, where the EXPRESSION arm has required `PyIsExactCtorName` since the
  receiver-versus-class bug (**a sibling arm left behind, 20,000 lines from the one already fixed**), and
  the from-import binding took `FindUClass(member)`.
  **Severity depends on ARITY, which is why it reads as two unrelated bugs**: a wrong argument count
  refuses with a diagnostic about THE WRONG CALLEE ("missing argument for dataclass field without
  default" — what walled `mission.py` and `__main__.py` and sent it into dataclasses), and a matching
  count builds the object silently — `site("a", 5.0)` printed `a 5.0` where CPython raises TypeError.
  Found by **following the diagnostic to its callee instead of believing it**. Both doors fixed;
  mission.py compiles, __main__.py advances to the "Coast has no method get" wall.
  **A control that was RED FOR THE WRONG REASON, nearly banked as a good one**: the whole fixture reds on
  pin v412 — because the pin predates tonight's package-import fix (`8fbabc2c3`) and dies at the import,
  never reaching the case question. The real control is the same-file shape alone. CLAUDE.md carries the
  PASS direction of this ("verifying under the pin can pass for a reason that has nothing to do with your
  fix"); **this is the MIRROR, and red is the direction nobody interrogates — a green invites suspicion,
  a red invites relief.** Asked for it in the playbook; a third instance outside NilPy is the promotion
  argument, and it goes to the owner.
- **frankb-8e is FINISHED, not out of queue — do not health-check it as idle.** frankuser read it as
  idle-with-nothing-running and asked me to re-dispatch; I declined. It sent an explicit handover (turn
  ended, nothing unpushed, no prompt pending, clean tree at the merged sha) and it declined the obvious
  next step ON PURPOSE, because its tier held the binary it would have had to replace to measure
  anything. Overriding that at 03:37 would be pushing work at the exact seat that told me why it stopped.
  If the owner wants that seat working again, he restarts or resumes it.
- **The callback ticket has a reason not to sink, now IN THE TICKET** (`abb2ae020`): relayed secondhand
  by frankuser, **the owner is interested in this mechanism for a possible `ffsfs` demo** — a Python
  program handing a compiled routine to C as a callback. Marked as secondhand. An unowned ticket with a
  ready next step is what gets lost, and a reader who knows a demo wants it ranks it differently from
  one who reads it as a frontend nicety.
- **~06:10 — franks-5b: THE 52% IS DELIVERED, not priced by removal** (`6c211e043`, on origin). xtensa ESP
  NilPy live code **1,721,914 → 827,330 B (-52%)**, object 1,923,596 → 990,656. **riscv32 unchanged at
  ~2,070,000 as predicted**, and that caveat is now in the commit, the ticket AND the logbook so it
  cannot travel without the number.
  Shape: `PyClosureInvoke` is called by nothing; all eight dispatch sites go through
  `PyCallClosureBody`, which calls a hook installed by `PyMakeClosureObj` — the only place a closure
  object can come from, since both its callers copy the interpreter's live token buffer into a
  `Closures[]` row. So `pyclosure_is(p)` True implies the hook is installed and the nil arm is
  unreachable **by construction**: a reason the pass can SEE. `PyIterCallHook` stays unconditional.
  **Today's @proc fix was a PREREQUISITE, not a neighbour**: the install takes `@PyClosureInvoke` inside
  `PyMakeClosureObj`, so under the old rule that address would have rooted the evaluator from a body the
  pass deletes and the hook would have bought ZERO. Unplanned; noticed while writing the comment.
  **THE CARRY-FORWARD: the first fixture was GREEN AND WORTHLESS.** Most NilPy lambdas are LIFTED to
  compiled code, so a file of `key=lambda w: len(w)` passes IDENTICALLY with the hook removed — found
  only by running the negative control, and `--dce-why=pyclosureinvoke` then answered
  `PyClosureInvoke <- DROPPED` in one command. The lifter refuses exactly one shape (a capture of a
  managed string that is a LOCAL of the enclosing function); a module-level string resolves as a global
  and the lambda lifts. **The twist on "a control from the RIGHT POPULATION": the population was not
  wrong by subsystem or magnitude — an EARLIER COMPILER PASS transformed the subject into something
  else.** Its own promotion read, and I agree: one instance, close to the optimiser-deleted-the-subject
  entry banked this morning, so strengthen that if it recurs rather than adding a neighbour.
  Correctness evidence, since this could have broken real programs: **test-uforth green, 17/17 corpora
  byte-identical to CPython with native AND Python-bodied words** — uforth is the heaviest user of this
  path — plus test-nilpy, test-quick, test-esp-idf, gate quick, rebuilt after sync.
  Self-inflicted loss, reported: `git checkout HEAD -- compiler/builtin/pyeval.pas` destroyed its own
  uncommitted hook work — "guard the REVERT, not the edit", exactly as written. Re-derived from the
  script, not memory; noted that its own mitigation (a file copy) broke the other rule and a patch was
  the right answer.
  **Open and unpriced, asked to be its own ticket**: pyparser.inc emits a direct `pyclosure_call_ptr`
  for a call through a Callable FIELD — a frontend-emitted edge the hook does not cover, so such a
  program links the evaluator again. Different subsystem; a residual buried in a resolved ticket is what
  gets lost.
- **~06:30 — franks-5b: the Callable-field residual DOES NOT EXIST** (`857dcdaac`, on origin). Asked to
  file it as its own ticket, it probed first and refuted it: the cut sits one layer BELOW what the
  frontend emits — `pyclosure_call_ptr` calls `PyCallClosureBody`, not `PyClosureInvoke` — and with a
  Callable field holding a plain compiled def, `--dce-why` answers `pyclosure_call_ptr <- DROPPED`,
  `ExecStatement <- DROPPED`, `ExecSuite <- DROPPED`. That edge keeps a 459-byte trampoline and nothing
  behind it.
  **The mechanism, and it is the transferable part: reasoning DRESSED AS A FINDING, written while
  holding the code, in the same breath and the same voice as the measurements beside it.** It reached a
  commit message, a ticket and a message to me — three transmissions, unqueried, because it travelled in
  the voice of the measured things around it. What caught it was being asked to MOVE it, because moving
  meant looking. A better argument for filing a residual separately than the filing rule itself. The
  wrong reasoning stays in the ticket, marked refuted with the probe, because the next reader will
  produce it again and a deleted error teaches nobody. (Strengthens CLAUDE.md's caption/quantifier
  family: an unmeasured clause inherits the credibility of the measured sentence it is attached to.)
  **LARGEST REMAINING RUNG OF THE ESP UMBRELLA**, and it already exists:
  `feature-a-unreferenced-class-rtti-keeps-every-method-alive`. One VMT slot on TPyFile —
  `writelines`, which accepts any sequence — drags `pyseq_of_obj` and the whole iterator-drain path,
  **94,665 B, into a program that never opens a file**:
  `pyiter_has <- pyiter_drain <- pyseq_of_obj <- TPyFile.writelines <- [vmt/rtti slot]`, plus 12,161 B
  and 10,581 B rows behind the same and `TPyList.sort`. 149 bodies / 172,637 B are rooted `vmt/rtti
  slot` in that image, so **one method is 55% of the class**. The two PyCallKey1 rows above it are the
  unconditional PyIterCallHook install and stay by design.
  **Design sharpening from the inside**: a per-METHOD criterion (a slot nothing can dispatch to) is
  worth more here than a per-CLASS one, because the class is LEGITIMATELY live and one unreachable
  method on it is the cost. `--dce-why=<name>` names the dragger for any candidate in one command.
  Not taken tonight, deliberately — it is a design question about RTTI and devirtualisation, not a
  cleanup. Write-up asked for in that ticket so the next seat needs nothing from franks-5b.
- **~06:45 — franks-5b ENDED ITS TURN** (`97a91cb53`, the RTTI write-up, on origin). Nothing held,
  nothing uncommitted, no background jobs. Carry as FINISHED.
  **The write-up changed the rung's SHAPE**: the ticket is titled "an UNREFERENCED CLASS keeps every one
  of its methods alive", and every class holding those slots in the NilPy image is legitimately live
  (TPyList, TPyDict, TPyFile), **so the criterion the ticket specifies reaches NONE of the 172,637 B it
  is now ranked on**. The cost is a per-METHOD slot nothing can dispatch to: `TPyFile.writelines` heads
  three of the nine largest rows, 115,606 B; `TPyBytes.decode`, `TPyDict.most_common`, `TPyDict.update`
  are the same shape smaller. **Put in the SUMMARY, not the body** — a seat taking that ticket on its
  title would write a pass that cannot deliver the number the umbrella ranks it on, and the body is the
  part nobody scrolls to.
  **What the instrument CANNOT do, recorded deliberately**: `--dce-why` totals no subtree and attributes
  by FIRST reason, so the vmt total is wrong in both directions as a "what would be freed" number, and
  115,606 B is only the rows that happen to fall in the top twenty. **Build the per-root subtree total
  BEFORE ranking candidates**, or the ranking is the histogram-of-one-file failure in new clothes.
  **Playbook discipline worth copying**: it looked first, found CLAUDE.md already had the rule ("an
  unlabelled claim travelling beside it inherits that credibility"), and banked only the missing half —
  **the moment of maximum risk is the WRITE-UP, not the investigation, because during the work every
  claim is a question and while summarising the measured and the inferred get the same grammar** — plus
  the cheap verifier: *"move this somewhere else" forces a re-read of the SUBJECT, where re-reading the
  sentence cannot.*
  Left unclaimed and documented: the RTTI rung (shape corrected), the riscv32 stub-target rung (rule-out
  named). **32 commits inert behind v412** — the owner's call, nobody waiting.
- **~07:00 — frankh-c0: census arithmetic reconciled, and the omission is the finding.** I flagged
  "41 + one file = 42, not 43" before the prediction became a story; the answer is reading (a) and the
  baseline had moved. Three censuses, same population (`git archive 934c63e`, 67 files) and same oracle:
  `47841c55b` 38/29 → `5c2e692f9` 41/26 (landing.py, scene.py, view/demo.py) → `7113a683a` 42/25, whose
  OK-list diff is exactly one line, `tsp/mission.py`. So 42 + settings.py = 43 and the prediction stands.
  **Its own correction, which is the durable part: a census run as a CONTROL is still a census, and one
  used only internally is one the coordinator cannot reconcile against.** It will report every row from
  here on, control or not. Same shape twice in one session: the case-folding fix LANDED as
  `9214b44ee` ("a name differing from a class only in case is not that class", checked — on origin and
  the current tip) and the sha was not sent, so from outside a landed green tier and a running tier look
  identical.
  **Ghost sha, handled correctly**: `7113a683a` is the pre-push sha and is in NO checkout's object store
  (verified here); `9214b44ee` is the one to quote. Worth saying inside the census record itself, or a
  later reader distrusts the census rather than the sha.
- **frankh-c0, os group**: `os.environ["k"]` built, tier running. `os.environ.get(k)` already worked, so
  the gap was the SUBSCRIPT only, and the message said `undefined variable (os)` — about the MODULE,
  the same misleading shape as os.pardir/curdir/extsep. **Deliberately not a one-liner over `.get`**:
  the subscript RAISES KeyError where `.get` answers None, and a program reads a required setting with
  the subscript precisely so a missing name STOPS it — answering None hands it a wrong value to carry,
  no diagnostic, arbitrarily far from the cause. Seven rows against CPython 3.14.4 including the
  KeyError row and both `.get` behaviours; pinned control fails at the subscript **for the right
  reason, checked** after last night's red-for-the-wrong-reason. Clears `tsp/settings.py`, the third of
  three os gaps censused across both application trees.
- **~07:20 — frankh-c0 mapped its next group: the SAME case-folding mechanism in two more doors.**
  **Wall 1, taken**: "Coast has no method get", 3 TSP files (`__main__.py`, `maneuver.py`,
  `nodeedit.py`), all reported at `pascal26:236` — **one site, not three**: all three import
  maneuver.py and die at ITS line 236, the diagnostic wearing one file's line number, caught before it
  became three tickets. 15-line repro: `class Coast` and `def coast(...) -> dict` in one module, so
  `self.info = u.coast("x")` types the field as class Coast and `h.info.get("mass")` is refused where
  CPython prints 7. **Two controls, both compiling** — rename the class, rename the method — so the axis
  is the case-only collision and nothing else. Door: a FLAT `FindUClassNonRecord(nm)` fallback
  (pyparser.inc:5628) that fires even when the qualifier is not a unit at all (in the repro `u` is a
  parameter, `FindUnitOrAlias` is -1), answering on the MEMBER name alone, case-folded; a second door at
  :6074 calls bare `FindUClass` the same way. Predicates already exist from this morning.
  **Residual to be written as a MECHANISM, not a row**: a method spelled EXACTLY like a class, reached
  through a VALUE qualifier, still resolves as a construction, because the fallback ignores the
  qualifier rather than folding its case — it survives every future fix to the case half.
  **Wall 2, separate feature ticket, not tonight**: `random.Random(seed)` reported as "pyrandom_random
  takes fewer arguments than were given" — same folding, but `lib/rtl/random.pas` has TRandomState and
  **no per-instance RNG surface at all**. So it is a genuine FEATURE gap whose only symptom is an ARITY
  message: **a missing class wearing a wrong-arity diagnostic sends the reader to the call site instead
  of to the absent member.** Exactness makes the message honest and does not make the program compile.
  **The rest of the board, censused rather than chosen**: `_backend` 7 files, ALL downstream of ctypes
  (`tsp/platform/__init__.py:89`), so with the 3 direct rows **10 of the 25 remaining failures are gated
  on the owner's unanswered fork** — the denominator that matters now is failures, not the corpus.
  dataclasses.replace 2, frozen=True 2, threading 2 (frankb-8e's), callable-kw 1, subprocess.run(cwd=) 1.
  `tsp/stars.py:25` `(n,) = struct.unpack_from(...)` flagged not claimed — discriminator is whether a
  one-element parenthesised target reaches the same `PyParseUnpackAssign` path as `47841c55b`.
- **~07:35 — frankh-c0: stars.py settled, the axis is ARITY not parentheses.** Controls:
  `(n,) = f()` fails, **`a, = f()` fails with no parentheses at all** (so parens are not the axis),
  `(b, c) = f()` compiles (so a parenthesised target is fine with two). CPython prints 5/5/3. A
  ONE-element unpack target never reaches the unpack path; the target scan takes it for an ordinary
  expression and the name is never bound. Same `PyParseUnpackAssign` entry as `47841c55b`, so it is
  frankh-c0's group.
  **The population point, asked for in the ticket**: every unpack fixture in the tree has two or more
  targets, because that is what anyone writing a test for unpacking writes — **the suite certifies the
  working arrangement and cannot see this one**. That is CLAUDE.md's "put the interesting element
  somewhere other than last" in ARITY form rather than ORDER form, which the existing rule does not
  spell out. Not exotic in real code: `(n,) = struct.unpack_from("<I", data, 6)` is the idiomatic way to
  take one field out of a binary header. A third instance outside unpacking is the promotion argument,
  and it goes to the owner.
  **`feature-n-random-random-the-per-instance-rng-class-is-absent` filed** (p40, backlog-nilpy) with the
  honest-diagnostic sentence in the SUMMARY, an explicit line that fixing the diagnostic is NOT fixing
  the ticket, and why mapping Random onto the global generator would be WRONG rather than partial:
  every other `random.*` user in the process would perturb the stream, so a seeded run would silently
  stop being reproducible — the one property the class exists for.
- **~07:55 — frankh-c0 landed the os group** (`d6ab05ec5`, on origin). **Census 44 OK / 23 fail**, and
  its 43 was wrong — **corrected in writing BEFORE the run**, to a file naming both files that would
  move and the one that would not. Came back exactly so: added over the 42 baseline are only
  `tsp/settings.py` (os.environ subscript) and `tsp/surface.py` (os.pardir + os.path.relpath); `tsp/menu.py`
  did NOT move, as predicted — it went from `undefined variable (os)` to `run has no parameter named
  'cwd'`, **a wall that MOVED, not a win**, which is why naming it beforehand matters.
  **The durable point, a new one**: *work carried across a context boundary in an uncommitted tree is
  invisible to the prediction you make after the boundary.* It reconstructed the commit's contents from
  what it remembered doing — the LAST thing it did, not the whole diff — while two more os fixes sat
  uncommitted from before a compaction. **The instrument was `git status`, HANDED to it at startup and
  read as context rather than as evidence about its own pending delta.** The handbook's "derive it from
  the tree you are committing to" arriving in a PREDICTION instead of a summary; asked for a playbook
  section under its name.
  **Two practices worth copying**: it confirmed its new row actually RAN (log line 4759) rather than
  trusting the tier's green — *a green tier that skipped your row proves nothing, and rc=0 cannot
  distinguish it*; and it caught its fixture sharing an OUTPUT BINARY PATH with the existing
  test_nilpy_environ row before landing — two rows sharing one output is a route by which a failed
  compile leaves the PREVIOUS row's binary for this row's diff to read, a green correct about a
  different program. Reason recorded in the Makefile comment so nobody re-merges them.
  Verdicts read from the jobs' own printed lines (TIER-RC=0; `gate: GREEN (exit 0)`).
  **BOARD: 23 failures** — ctypes 10 (3 direct, 7 downstream), Coast 3, dataclasses.replace 2,
  frozen=True 2, threading 2 (frankb-8e's), subprocess.run(cwd=) 1, one-element unpack 1, random.Random
  1, callable-kw 1. Taking the Coast door now, its patch deliberately held unapplied while the tier
  measured the tree.
- **~08:20 — frankh-c0: THE PREPARED FIX WAS WRONG, and catching it is the finding.** Applying the
  exactness patch made the compile error disappear — and the program then PRINTED 64MB of memory, because
  exactness removed the wrong class and left the field typed as a STRING holding a dict pointer.
  **The patch converted a loud compile error into a silent wrong value**, strictly worse than the bug,
  and it was only visible because the fixture prints the VALUE rather than asserting that the program compiles.
  **The real mechanism is one layer down**: `PyTypeFromTokenIndex` folds case, so in `u.coast("x")` with
  `class Coast` in scope the token typed tyClass — and **a token typed as a class never reaches the
  call-form arm, so the argument group is never skipped and the general walk WIDENS OVER THE ARGUMENT.**
  Measured one axis at a time, collision held fixed: `u.coast("x")` → AnsiString, `u.coast(1)` → Int64,
  `u.coast(1.5)` → Double, `u.coast()` → the RECEIVER's class, control without collision → tyVariant.
  None of it visible from `Coast has no method get` — a class the source never wrote, reported at a
  member call far from the assignment that decided the type. Both reads fixed as ONE rule (Python is
  case-sensitive) with the predicate already in the tree; the colliding shape now types **tk=22,
  IDENTICAL to the non-colliding control**, not merely a different right answer.
  **New instrument, kept and documented**: `PXXDBG=n.flddecl` prints what `self.<name> = <rhs>` was
  decided to be (kind, rec, signature, annotated or inferred) — a field pre-pass's decision was
  previously unobservable, and the bug class is "a type chosen in one pre-pass and consumed hundreds of
  lines away".
  **PROPOSED TO THE OWNER as a CLAUDE.md extension** (not applied, and no seat asked to apply it):
  *a fix that removes a diagnostic has not been verified by the diagnostic disappearing.* CLAUDE.md's
  "MATCH THE ASSERTION CLASS TO THE DEFECT CLASS" has two domains — a leak, and side-effect ORDER. This
  is a third: **where a wrong TYPE is the defect, every wrong type that stops being loud starts being
  silent**, so "does it still error" cannot distinguish a fix from a downgrade.
  Real artefact: `tsp/maneuver.py` and `tsp/nodeedit.py` compile; `tsp/__main__.py` moved to
  `import threading requires --threadsafe` — a wall that MOVED, frankb-8e's lane.
- **~08:45 — frankh-c0 landed the Coast fix** (`0dfa0d78d`, on origin). **Census 46 OK / 21 fail**,
  against a 46 written to a file before the run; OK-list diff exactly `tsp/maneuver.py` and
  `tsp/nodeedit.py`. `tsp/__main__.py` named in advance as the non-mover: its wall MOVED to
  `import threading requires --threadsafe` (frankb-8e's lane), **not counted as a win**. Tier and gate
  read from their own printed verdicts, with its row confirmed to have RUN at log line 4772.
  **CORRECTION TO MY OWN FRAMING, accepted from frankh-c0**: I had scoped the census-oracle problem to
  its lane. It is broader — **any wall-clearing work reads "does it compile" as its oracle all night,
  and that oracle cannot tell a fixed file from a file whose defect went quiet.** It belongs in the
  playbook as a corollary, because the next seat clearing walls will not be frankh-c0.
  **The sentence to put to the owner is the NARROW one, at its author's request**: *where a wrong TYPE
  is the defect, every wrong type that stops being loud starts being silent, so "does it still error"
  cannot distinguish a fix from a downgrade* — 64MB of printed memory behind it, one-line discharge
  (assert the VALUE, not the absence of a diagnostic). It declined to apply it itself, correctly:
  extending the rules file is the owner's, and a peer cannot grant that however good the finding. Same
  call franks-5b made when I made this mistake in the other direction.
  **WALL BOARD at 21, re-censused rather than subtracted**: ctypes **10 of 21** (3 direct, 7 downstream;
  the share RISES because the non-ctypes walls are the ones falling), threading 3, dataclasses.replace
  2, frozen=True 2, and one each of `(n,) =` one-element unpack, subprocess.run(cwd=), random.Random,
  keyword-through-a-callable.
  Its resume notes are refreshed with the method, every census row against its PUSHED sha, and the wall
  board — the direct answer to its own wrong 43, which came from reconstructing across a boundary.
  **Next: the one-element unpack**, with the fixture asserting the VALUE binds rather than that the file
  compiles — the same reason its Coast patch nearly passed.
- **~09:05 — frankh-c0: one-element unpack fixed, tier running, and the first cut BROKE A NEIGHBOUR.**
  Stated as Python states it: **a tuple target is decided by the trailing COMMA, never by the
  parentheses** — `a = xs` and `(a) = xs` bind the whole value, `a, = xs` and `(a,) = xs` bind `xs[0]`.
  The detector required two or more targets, so both one-element spellings fell through to an ordinary
  expression statement. One flag set by the comma and nothing else replaces `nTargets > 1` at the two
  gates. **Sibling fixed in the same cut, not in the ticket**: `(c) = v` was refused on the pin and at
  HEAD alike, and without it the fixture cannot demonstrate its own claim — **"a discriminator you do
  not assert both sides of is not a discriminator."**
  **What broke**: the first cut put the rule in a tail inside the target loop, and the nested-group
  arm ends in `Continue`, which jumps to the `until` and skips any tail — so the rule existed for the
  plain-name spelling only and `(b, c) = xs` stopped parsing. **This tree's "a second spelling is where
  the rule goes missing", inside ONE LOOP rather than across two files.** Remedy: move the rule into the
  `until` expression, which both paths reach BY CONSTRUCTION — the same instinct franks-5b used on the
  closure hook tonight. Two seats, two subsystems: put the invariant where the control flow cannot miss it.
  **Caught because every NEIGHBOURING shape was in the fixture** — multi-element parenthesised list,
  starred target, nested group, no-comma forms — not only the one being fixed.
  **The connective tissue, and it is the durable part**: twice tonight a first cut would have passed a
  narrow check — Coast (loud becoming silent, caught by asserting the VALUE) and this (a sibling
  regressing, caught by asserting the NEIGHBOURS). **The fixture's coverage, not the fix, did the work
  both times**, and in both cases the narrow check is the one most seats would have written. Either
  instance alone reads as a lucky catch; the pair reads as a method.
  Eleven rows against CPython 3.14.4, pinned control reds at the one-tuple. `tsp/stars.py` compiles.
  Census expectation recorded before the run: **47, single addition `tsp/stars.py`.**
- **~09:25 — frankh-c0 landed the unpack fix** (`dc17f567e`, on origin; follow-up `715577d0c` for an
  uncited Log line `sync.sh` caught — a guard firing on the one path that skips the tool).
  **Census 47 OK / 20 fail**, predicted in writing, OK-list diff exactly `tsp/stars.py`. **And it
  predicted that NOTHING ELSE would move, with the reason**: the change is confined to the detector and
  `PyParseUnpackAssign`, so anything else moving would have meant it did not understand the blast
  radius. *A prediction that only names the winner cannot be wrong in the direction that matters.*
  **WALL BOARD at 20: ctypes is now HALF of it** — 10 (3 direct, 7 downstream), unchanged and
  unanswered; threading 3 (frankb-8e's); dataclasses.replace 2; frozen=True 2; one each of
  subprocess.run(cwd=) and random.Random.
  Five fixes tonight on this side — `d6ab05ec5` (three os gaps), `0dfa0d78d` (case-folded method),
  `dc17f567e` (one-element unpack) — **TSP 42 → 47 of 67, every delta predicted in writing, one
  prediction corrected before its run and none after.**
  Playbook section in: "THE FIXTURE'S COVERAGE, NOT THE FIX, IS WHAT CATCHES A BAD FIRST CUT — AND A
  DISCRIMINATOR YOU DO NOT ASSERT BOTH SIDES OF IS NOT A DISCRIMINATOR", both instances with the
  connective tissue, the "put the invariant where the control flow cannot miss it" point, and the note
  that franks-5b reached the same instinct on the closure hook from a different direction.
  **Next: dataclasses.replace AND frozen=True as one group** (4 files, two mechanisms, nobody on them —
  both other seats have ended their turns and `working/` holds no dataclass ticket).
  **After that, named now rather than chosen tired**: (a) lekkerzeilen's NilPy blockers — but
  lekkerzeilen-7a has been unresponsive all session, so there is no fresh list and I will not have a seat
  guess at one; (b) the ESP RTTI rung, unclaimed and documented, subtree instrument first; (c) end the
  turn with the board and resume notes current, **a legitimate outcome and not a lesser one**.
- **~09:50 — frankh-c0 BANKED the dataclasses group and ENDED ITS TURN** (`7a761a2b4`, on origin, two
  tickets filed). Carried as FINISHED. Banking is what CLAUDE.md asks for here — *bank the diagnosis and
  park it, never microfix as a consolation* — and the measurement is what makes it a diagnosis:
  **both real `replace` call sites take their receiver out of a DICT VALUE inside a comprehension**
  (`self.by_part` types as TPyDict, element class not carried), so the receiver is a variant and the
  obvious parse-time desugar cannot see the class. **A fixture with a statically typed receiver would
  have passed while shape.py still walled** — the minimal case fixing an axis the real program does not
  have, caught BEFORE writing the fix this time.
  **It collapses into another gap**: enumerating an instance's fields and allocating an instance of a
  run-time-known class is ONE capability wearing two module names — `lib/rtl/mimic_copy.py` already
  refuses `copy.copy` in those words, including why a partial version is worse than none. Implementing
  either separately builds half of it twice. What is NOT missing, measured: attribute lookup by name on
  a variant-held instance already works, which is what makes this look smaller than it is.
  **frozen=True: the refusal is CORRECT and was left alone.** A compile-time store guard cannot reach a
  variant-held instance — the ordinary shape of real code — so the reachable half is exactly the half
  that would go silent; and frozen+eq generates `__hash__` in CPython, so accepting frozen without
  hashability MOVES the gap. Both pairs are single sites reported under a second file's name again
  (bpedit.py's own line 115 is a docstring — third time that discriminator paid tonight).
  **The cross-link, from a seat that never spoke to franks-5b about it**: the ESP RTTI rung and this
  dataclasses capability are both *"what does a program know about an object at run time"* — if the RTTI
  work lands per-object field metadata, `replace()` and `copy()` may fall out of it. Both halves are now
  written down, by two seats independently.

## Night's close — 2026-09-19 into 2026-09-20

All three pxx seats ended their turns deliberately; none is blocked, nothing is held unpushed. 241
commits on origin since pin v412, **36 of them touching `compiler/` or `lib/`** and therefore inert
until the next pin. The pin is the owner's call and nobody is waiting on it.
**Unclaimed with a written mechanism**: the ESP RTTI rung (shape corrected, subtree instrument first),
the riscv32 stub-target rung (rule-out named), the callback field site (`RecFieldProcSig` named, and an
ffsfs demo interest recorded in the ticket), the capturing-nested-def silent segfault, dataclasses
replace/copy as one capability, frozen=True, random.Random, subprocess.run(cwd=).
**With the owner**: a pin; ctypes (10 of TSP's 20 remaining failures, and lekkerzeilen too); threading
(3 more, numbers attached); the xtensa long-calls prio; two proposed CLAUDE.md sentences (the stale
compiler warning, and "a fix that removes a diagnostic has not been verified by the diagnostic
disappearing"); and lekkerzeilen-7a, unresponsive all session and possibly on a prompt only he can clear.

## Reopened — lekkerzeilen-7a came back

- **frankh-c0 UN-ENDED its turn** to take a live bug 7a handed it; carry it as WORKING again.
  **The bug**: `_NO_WIND = (0.0, 0.0)` at module level, then `self.wind = _NO_WIND` in a class in the
  SAME file, refused with "cannot infer the type of field self.wind - annotate it". 7a had already ruled
  out the field name and any cross-module read, correctly. **The axis is the global's LITERAL KIND**:
  the reader knew scalars, lists and dicts and had no arm for a TUPLE or for None, so `[0.0, 0.0]` and
  `{"a": 1}` compiled two lines from the tuple that did not — which is exactly what makes a reporter
  suspect the name or the package. Fixed by stating Python's rule once: **a tuple is decided by the
  COMMA, never by the parentheses** — *the same discriminator as the one-element unpack TARGET landed
  hours earlier, now in a SECOND subsystem*. Second direction asserted, because it is the trap:
  `(1 + 2)` is an int in brackets, and reading it as a container would type an int field as a variant,
  silent in the direction nothing reports. Thirteen rows against CPython; pinned control reds with 7a's
  exact message.
- **ROUTING, and it is the one thing that genuinely needed me**: lekkerzeilen-7a is blocked on a **stale
  `pascal26` in /home/neo/frank-user** (16:52, does not carry `9c70e35f6`). 7a will not build in a tree
  it does not own and frankh-c0 declined to build there for the same reason. Asked frankuser to pull and
  rebuild its own checkout. **Nobody hands 7a a binary**: a pascal26 run from outside its own repo root
  falls back to a CWD-relative `compiler/builtin/` and silently compiles ANOTHER checkout's builtin
  units in, with no error, and ~20 candidate `compiler/builtin` directories exist on this box.
- **frankh-c0's misrouting catch**: its first instinct was that 7a had misrouted a chart.py question to
  it. It checked rather than asserted, and `tools/whose_commit.sh` put `9c70e35f6` in ITS OWN checkout,
  from before the compaction. **A compaction makes "I don't remember asking that" feel exactly like "I
  didn't ask that", and the seat holding the rolled window is the one least able to tell them apart.**
  CLAUDE.md's transcript rule arriving through the COMPACTION door rather than the window-roll door.
  Cost: one command. Second time tonight a context boundary produced a confident wrong belief about its
  own work (the first was the wrong 43), and both times the instrument was already in hand.
- **7a's measurement, relayed with no owner found**: chart.py contains **NO annotation at all** —
  `_sound` at line 131 has every parameter bare, `grid` included — and its build log has zero
  "annotation this frontend cannot read" warnings for any file. I grepped: no OPEN ticket rests on
  chart.py's `grid` being annotated (those rows are in `done/`). Recorded here rather than filed against
  a ticket I cannot identify; asked frankuser, who may know which claim it answers.
- **Blocker 03 coming from 7a**: `no class declares a method or callable field .wind()` while
  Environment declares `def wind(self, x, z)`, with a Craft FIELD of the same name in play. Smells like
  tonight's family, and **its two-module reduction does NOT reproduce — consistent, because those bugs
  turn on which declarations are in scope and in what order, so a reduction that keeps the call shape
  and drops the population loses the axis.** 7a told what to capture: the full failing module set, not a
  minimal pair. And when it is taken: put the interesting declaration LAST.
- **frank-user rebuilt** (frankuser, HEAD `0dc4743cf`, `converged after 2 round(s)`, sha256
  `ec905950c45a`, no binary copied from anywhere) and lekkerzeilen-7a told it is current. Routing closed.
- **chart.py annotation claim: NO OWNER FOUND.** frankuser has relayed no such claim and the owner has
  not raised one; I grepped and no OPEN ticket rests on it. 7a's measurement stands recorded above, and
  it DATES the question if a ticket turns up later.
- **THE GAP THAT COST TONIGHT'S HOURS, and it is in CLAUDE.md's coordinator section, which is my own
  procedure**: lekkerzeilen-7a WAS blocked, on a dangerous-operation permission prompt the owner
  cleared himself. Asked to check its transcript, 7a answered honestly that there was **no denial after
  its last tool call — and that was TRUE, because a PENDING prompt is not in the transcript at all.**
  Nothing has been answered, so there is no record to find. **CLAUDE.md's transcript check catches a
  seat that WAS refused, never one that is BEING asked**, and the two are indistinguishable by that
  instrument: both produce "no refusal after my last successful call". I ran exactly that check tonight
  and read the silence as "not blocked".
  **Contributing cause, the owner's own observation**: the lekkerzeilen checkout has **no equivalent of
  pxx's `.claude/hooks/no-variable-rm.sh`**, so that class of prompt STALLS for hours there instead of
  being refused instantly. The hook is what converts an hours-long invisible stall into a one-line
  refusal and a retry.
  **Proposed to the owner** (not applied; extending CLAUDE.md is his, and installing a hook in HIS
  application repo is his too): a sentence in the coordinator section saying the transcript check
  cannot see a pending prompt, so a seat silent with no denial after its last successful call is
  UNDETERMINED rather than working — and the cheap discharge, which is to ask the seat whether its last
  tool call has RETURNED, not whether it was refused.

## Owner decisions, 2026-09-20 ~09:00 (relayed by frankuser, all secondhand)

1. **ESP INTERRUPTS ARE A MUST-HAVE**, his word — not a demo caveat. Re-ranked
   `feature-n-a-nilpy-def-has-no-native-abi-entry-point-to-hand-to-a-c-callback` **55 → 85**
   (`fb450c1ba`). Written into the ticket: **ONE capability, THREE consumers** — ESP interrupts (the
   demo stops polling), frankb-8e's unowned FIELD site (`RecFieldProcSig`, the argument path landed as
   `2b28c3302`), and the ffsfs callback interest (`abb2ae020`) — *so nobody builds half of it twice*.
   The ISR/allocation half stays explicitly separate: an ISR that allocates is a latent crash with good
   latency numbers, and delivering the ticket does not by itself let the demo stop polling.
2. **MEASURE SRAM, NOT IMAGE SIZE.** *"ESP's have 'plenty' flash memory so that's a lesser issue."*
   Recorded on the small-image umbrella: the headline is **data + bss + heap arena**; image size is
   second and must be labelled. **This RE-READS last night's wins rather than retiring them** —
   `357d13162` (-31%/-38%) and `6c211e043` (-52% live code) are FLASH wins, real but not what the
   umbrella is ranked on. Every rung must now say WHICH it measured; several say "image" without
   distinguishing.
3. **THE CTYPES FORK IS ANSWERED, AND INVERTED.** Not a ctypes shim: a **pxx-only marker module** the
   application imports to ask *am I under pxx*, so the guarded import tests for pxx instead of for the
   absence of ctypes. Filed `feature-n-a-pxx-marker-module-so-an-application-can-ask-whether-it-is-under-pxx`
   (N, p80, `e92073d1d`, addendum `00c710ab0`). **Unblocks 10 of TSP's 20 remaining failures and
   lekkerzeilen's backend selection**, and removes the hazard that made a partial ctypes worse than
   none (an importable `mimic_ctypes` flips both applications onto the CPython arm SILENTLY, because the
   import SUCCEEDING is the signal).
   **Shape settled by the addendum: the IMPORT idiom, dunder-ish name.** A bare superglobal is not
   symmetric — CPython raises NameError on an undefined name, so `if __HAS_PXX is not None:` crashes
   there. `globals().get('__PXX__')` would be symmetric **but NilPy `globals()` support is UNVERIFIED**
   and is flagged as such. The exact spelling is the owner's; `__pxx__` is a seat's suggestion.
   **The property to preserve**: pxx resolves the import at COMPILE time, so the CPython arm is never
   compiled and the application keeps its ctypes code untouched — that is why it is cheap on the
   application side, and anything requiring the dead arm to be parseable-and-typable loses it.
   Fixture goes in BOTH import positions, with and without `else:` — CLAUDE.md records a first-wins
   alias-table defect of exactly that shape.
   Application-side edits in lekkerzeilen and TSP belong to those repos' seats, not to pxx.
- **~10:20 — frankh-c0 measured the marker module's mechanism before anyone built on it** (measurement
  only, nothing committed; its tuple tier is still running).
  - **The DEAD arm escapes compilation — the half the design actually rests on.** The ticket's measured
    claim was only that a guarded import resolves at compile time. Under pxx the marker import SUCCEEDS,
    so the arm that must vanish is the CPython one holding the ctypes code. Proved with a dead arm pxx
    **cannot type at all** (`ctypes.c_void_p(0).value.nosuch_attr.deeper`, then a call on it) — anything
    weaker would have been consistent with the arm being parsed-and-tolerated. The application keeps its
    ctypes code exactly where it is and pxx never has to understand any of it: now a measurement, not an
    expectation.
  - **`globals()` is NOT available** (`undefined variable (globals)`). Closes the ticket's unverified
    flag and settles more than it asked: **the superglobal has no symmetric spelling at all today**, so
    the import idiom is the only one of the three that works on both sides. Recorded so nobody re-opens
    the superglobal as "simpler" — its symmetric form depends on a builtin we do not have.
  - **Arm order behaves in all three arrangements**, including the no-`else:` one CLAUDE.md records as
    the first-wins defect. Fixture still goes in both positions: *behaving today is not the same as
    being pinned.*
  - **SCOPE CORRECTION, accepted, and I had been over-claiming it**: the compiler-side marker is
    **necessary and NOT sufficient**. The 10 of 20 clear only when the applications change to test for
    pxx, and those edits belong to tuxspaceprogram-6e and lekkerzeilen-7a. I have been relaying
    "unblocks 10 of 20" and it is "unblocks it on the pxx side". Said now so a flat census row does not
    later read as a failed fix.
  - **The name question goes to the owner in frankh-c0's words**, as a goal with no implementation noun:
    ***"should an application's pxx-check be something a third party could make TRUE on CPython by
    publishing a package of that name?"*** Answerable in a word, and it decides the dunder question
    without anyone needing to know what PyPI normalisation does.
- **~10:35 — tuxspaceprogram-c6 asked for the marker's spelling; answered "not named, not shipped, do
  not guess".** It is the owner's seat and I steer it not at all, but the factual answer is mine to
  give: the naming question is with him, and I will send the verbatim spelling when it exists.
  **Its own question was the sharper one** and it had reasoned it correctly: it planned to centralise
  detection into a `HAVE_PXX` boolean and realised that would leave pxx seeing a RUNTIME condition,
  forcing the ctypes arm to compile and defeating the inversion. Answered with the measurement and its
  edge: **the literal `try: import <marker>` at each branch point is what frankh-c0 measured; the
  boolean indirection is NOT measured and must not be adopted on my say-so.** Offered to have it
  measured if it wants the ergonomics later.
  **Census corrected in TSP's favour, grepped read-only**: ctypes appears in exactly four files, all
  under `tsp/platform/` (`_sdl2.py:13`, `_gl.py:12`, `_ctypes_backend.py:11`, seam `__init__.py:80`).
  **Its C2 constraint holds — no violation.** The "10 of 20" is 3 direct importers plus **7 ordinary
  consumers of `tsp/platform`** failing on `undefined variable (_backend)`, not 7 ctypes users. Its
  application-side change is one token at the seam.
- **~10:50 — TSP's seam is converted (tuxspaceprogram-c6, `683c766` in ITS repo) and waits only on the
  name.** It took the lexical form at the single branch point, with a placeholder that exists on no
  runtime, so CPython still takes the ctypes arm: 182 tests pass, `backend_name()` still reports
  `ctypes`. One token when the spelling arrives. It declined the `HAVE_PXX` boolean outright, with the
  reason written down: one branch point means the indirection buys nothing and costs the compile-time fold.
  **AND THE NUMBER I HAVE BEEN RELAYING PREDICTS LESS THAN IT SOUNDS — flagged upward in its words.**
  `_backend` now binds on BOTH arms, which is the structural fix for the 7 downstream failures (they
  were never ctypes users; the old code's only binding site was the dead arm). But the `else:` arm names
  **`_pxx_backend`, which does not exist in TSP**: it is the lekkerzeilen `platform/_pxx.py` port,
  ~1k lines, **parked by owner decision on 2026-09-19 and nobody is on it**. So the marker moves TSP
  from *fails at the ctypes import* to *fails at `_pxx_backend` not found* — real progress, a more
  honest error, **not a compiling TSP**. That port is the remaining gate and it is the owner's to unpark.
  **Census discipline**: when the marker exists, re-census against `683c766`, not against the earlier
  tree, so the delta is attributable to the marker rather than to TSP's own seam change.
- **~11:05 — frankh-c0: blocker 03 diagnosed, REFUTED BY ITS OWN CONTROL, banked as two tickets.**
  Mechanism: `recv.m(args)` on a receiver with no static class — when no class declares `m` as a
  METHOD, the resolver **claims any class's variant FIELD named `m` and emits an unconditional hard
  cast to that class, with no runtime class test.** Its real population is a dispatch table where the
  receiver IS that class; give it another class and the call goes out through whatever sits at that
  offset. A plain data field named like somebody else's method is enough, and `self.x = None` is enough
  to make the field variant. Fires where the method scan found nothing, which today mostly means the
  declaring class is in a module imported LATER than the caller's. Two tickets: registration order p55
  (a shell pass over the import closure — **also clears lekkerzeilen's 18 unactionable build warnings**,
  a second consumer worth naming) and the field claim p60. **Wrong value with a memory-safety shape.**
  **THE REFUTATION IS THE ENTRY OF THE NIGHT.** Its first diagnosis was that the unresolvable
  assignment WIDENED the field and the widened field then satisfied the fallback — *the statement's own
  failure manufacturing the evidence that resolved it.* Every probe row fitted it and the fix was picked
  out and small (one sentinel, no new table, every consumer already testing >=0/<0). Then it built the
  case where its named mechanism is ABSENT — `self.wind = None`, variant from birth, nothing widened —
  and the symptom fired identically. **The probe reported where the field ENDED UP and was silent about
  the ROUTE; the invented part was the verb between two true readings.** Cost avoided: repairing the
  rarer spelling, leaving the commoner one, with the symptom gone from its own repro. General discharge,
  now in the playbook: *having named a mechanism, build the case where the mechanism is ABSENT and the
  symptom should still be possible, and run it.*
- **CORRECTION TO MY OWN RELAYING, accepted from frankh-c0: the TSP census oracle is COMPILE SUCCESS,
  which is blind to a silent wrong value** — the exact class I steered it toward. The census is the
  right instrument for the WALL BOARD and the wrong one for what we most care about. **Quote it as "47
  of 67 COMPILE, on an oracle blind to wrong values", never as "47 of 67 work."** Part of why
  lekkerzeilen-7a keeps producing value the census cannot: it has running demos, and TSP has no expected
  output to diff. No cheap second oracle exists; naming the blindness is the deliverable.
- **~11:25 — LEKKERZEILEN RENDERED A FRAME OUT OF A PXX BUILD. First one, ever.** 7a's demo, reported
  via frankh-c0. Goal 4 showing a picture rather than a number.
- **Blocker 04 taken (after the modtuple tier lands), and its title is wrong — frankh-c0 said so first.**
  Property stores through a bare receiver work; **the axis is a FIELD OF THE SAME NAME IN AN UNRELATED
  CLASS.** `PyVariantPropClass` opens with "a real FIELD of that name anywhere wins", scanning every
  declared class and exiting on the first hit, so `Boat.throttle` stops being a property because
  `Engine.throttle` exists. The write then falls through to the store-only dynamic-attribute path, and
  **`pydynattr_get` walks the shadow store BEFORE the property — so the value reads back correctly at
  the call site while the object the setter should have written is untouched.** Four probes including
  two negatives (rename the unrelated field → correct; neighbouring fields intact, so nothing is
  corrupted).
  **Why take it rather than bank it**: the population is the property-FORWARDING idiom — a class
  exposing a component's value under the same name, the commonest way that idiom is written — and
  lekkerzeilen is exactly that shape (`vessel.py:397` against `sim.py:288`). And the fix is symmetric
  with a path that exists: `__prop_set_<name>` is already a real mangled method, `TMethInfo.ParamKinds`
  already carries the value parameter's kind, `PyPropertyGet` is the template, so `PyPropertySet` is its
  mirror called from `pydynattr_set` before the shadow write, and the READ path needs no change.
  `compiler/builtin/pylib.pas`, so a full tier and a gate.
  **It refused the compile-time fix deliberately**: teaching the precedence loop to stop letting a field
  defeat a property is the same guess-the-receiver's-class mistake as 03, one layer up, and the
  compiler's own comment beside `PyMakeVariantFieldSet` already says the setter belongs there.
  **Doing it at RUN TIME, where the object's real class is known, is what both blockers point at** — a
  fix that needs the class statically will be wrong for the same reason the bug is.
  **CORRECTION PROPAGATED**: "one cause, two doors" is right about the FAMILY (a same-named member in an
  unrelated class claiming a dynamic receiver with no class test) and **wrong if read as one fix** — 03
  needs the argument list parsed once and handed to two builders, 04 needs a runtime helper. I had it
  logged the loose way. "Closing one closes the other" is the misread that leaves a family half-fixed.
- **~11:35 — two idle seats re-fed, both on the owner's own two priorities. Measured from
  `ready --track S` / the tickets on origin/master, not recalled.** Seat states from `ListAgents`:
  frankh-c0 working, frankb-8e and franks-5b idle, lekkerzeilen-7a busy.
  - **frankb-8e → the callback FIELD site**, consumer 2 of
    `feature-n-a-nilpy-def-has-no-native-abi-entry-point-...` (p85, owner empty, `blocked-by: []`).
    It built the ARGUMENT path at `2b28c3302` and the ticket's own body says `RecFieldProcSig(rec,
    field)` already exists with its four-row table above it — a written mechanism waiting for the
    seat that wrote it. Group, not a ticket: the field site, **the absent diagnostic** (a def
    assigned to a procedural field segfaults with NO message — whatever it does not handle should
    say so out loud), and ten minutes on `bug-n-capturing-nested-def-segfaults-silently` (p40) to
    see whether it is the same door.
    **I restated the two things it corrected me on, unprompted**: delivering this does NOT let
    `nilpy-hw-c3` stop polling (the ISR/allocation contract is separate and out of scope), and it is
    ONE capability with THREE consumers, so the resolution must say which one it delivered. Asked it
    to hold me to that rather than trusting me to remember.
  - **franks-5b → the SRAM instrument, then the riscv32 stub-target question.** The owner's "measure
    SRAM, not image size" ruling has been sitting on the umbrella since 09:00 **with no instrument**,
    which is the state where the next rung gets measured on the old axis by default. Order: build
    data+bss+arena per target; then **re-measure `357d13162` and `6c211e043` against it** — both are
    FLASH wins — with the expectation recorded BEFORE the re-run, because **zero SRAM movement for a
    52% flash win is a result worth writing down**, not a disappointment; then
    `bug-a-riscv32-dce-keeps-135-more-bodies-than-xtensa` (effective p70, unowned), whose open half
    is the good half: whether xtensa is smaller because its stubs land between bodies or because it
    emits a code-offset call WITHOUT recording a CodeRef — **a pass running blind, not a win**. A
    correctness question wearing a size ticket's clothes. Told it that if SRAM turns out not to move
    with DCE, the ranking follows the owner's axis and not the queue's.
  - **Collision check stated to both**: frankh-c0 is in the NilPy attribute-store path
    (`pydynattr_set`, `PyVariantPropClass`), frankb-8e in procedural-field coercion. Different
    questions, possibly one file — **same file is fine, git merges it** — and I named the tell that
    would mean they had converged: if 8e finds itself reasoning about which class a bare receiver
    has, it is in c0's question and should message it directly, not through me.

## ~12:10 — all three seats reported in one window. Shas verified on origin before anything below.

- **franks-5b — the SRAM instrument exists and the owner's axis has its answer (`901a6bdb6`).**
  `examples/esp32/nilpy-c3/build.sh sram`, in the one script all four NilPy demos symlink, qemu boot
  lifted into a function both modes call **so an SRAM number can never come from a differently-booted
  image**. Positive control: a 40,000-byte STATIC Pascal array → bss +40,004, free pool −40,000 exactly;
  40,000 is neither a power of two nor any arena constant in the tree. **Its own first control was
  wrong in the instructive way** — a module-level `bytearray(40000)` is a HEAP allocation made after
  `heap_init` has printed, would have moved neither readout, and would have certified a dead
  instrument. A missing `heap_init` line now FAILS rather than reading 0 (a pool never parsed, a pool
  of zero and a failed boot are one readout).
  **THE RESULT, expectation written down first:** data and bss move by **ZERO** on both ISAs under
  `--dce`, as predicted — riscv32 code −30.8%, xtensa −71.0%. The unpredicted half is the useful one:
  the pool moved **+1,840 / +2,112**, entirely lwIP statics collected by IDF's `--gc-sections`.
  **So code-removal rungs buy 0.87% and 0.76% of the free pool — they are NOT RAM work.** What is:
  **our own 178,008 B of data+bss against a 211,296 B pool — 84% of remaining DRAM, and `--dce` does
  not touch one byte of it.** It does not retire the flash rungs (stock 1 MB partition needs −66%;
  xtensa now reaches 841 KB, which clears it); it retires the idea that they are RAM work.
  **An instrument lied on the way, in this file's own shape:** an UND-symbol census answers 38 in BOTH
  objects because DCE drops bodies and never prunes the symtab. The difference is in the
  RELOCATIONS — 5 without `--dce`, 0 with. **Count relocations, not symbols.** Told it to BANK that in
  the playbook and said out loud which test it met: merit yes, promotion is the owner's.
  **I CORRECTED BOTH TICKETS IT PROMOTED, from their own frontmatter:**
  `bug-a-the-signal-alt-stack-is-32768-bytes-of-unconditional-bss` is **prio 15, SRAM case DISCHARGED —
  re-measured 2026-09-19 BY franks-5b ITSELF**, both bare ESP profiles paying ZERO because
  `TargetHasSignalRuntime` is false where no OS delivers a signal. A seat promoting a ticket it
  personally discharged the day before is the window-roll arriving in a promotion instead of a denial.
  And `feature-a-there-is-no-read-only-load-segment-...` is **`status: working`, `owner: frankH`, and
  its ESP-IDF half LANDED 2026-09-18** — both ELF32 writers emit `.rodata`+`.rela.rodata`, IDF puts it
  in flash, xtensa `.data` 6304 → 2624. **The 178 KB is measured downstream of that win.**
  **REDIRECTED to that ticket's own REMAINING list — "NilPy VMTs, prop/method arrays, IMTs, dispatch
  tables, float constants, each after its own never-written measurement" — because the subject IS a
  NilPy program and franks-5b now holds the only instrument that can price them.** Collision flagged
  both ways: frankH owns that ticket and is live in the NilPy runtime; told them to talk directly.
  Declined the two compiler rebuilds: the bound is sound, its own escape hatch (did either drop the
  LAST relocation to an SDK component) is cheaper than a rebuild, and at ~1% the answer moves no
  ranking. Step 3 endorsed **as a CORRECTNESS question, not a SRAM rung** — must not go back on that
  umbrella or the next reader inherits it as ~1% work.
- **frankh-c0 — `45b8571d7` landed green, and TRACK T'S RED AT ITS EXACT SHA IS NOT ITS.**
  A field from a module-level TUPLE or None global needs no annotation; the axis was the global's
  LITERAL KIND, not the field name. Fixed by stating Python's rule once — **a tuple is decided by the
  COMMA, never the parentheses** — and the fixture pins the SILENT direction too (`_G = (1 + 2)` stays
  an INT, because reading it as a container types an int field as a variant). **TSP census 47 of 67,
  unchanged, PREDICTED 47 IN WRITING BEFOREHAND with the reason.** Wall board: ctypes 10, threading 3,
  dataclasses 2, frozen=True 2, one each subprocess `cwd=`, random.Random, keyword-through-callable.
  **`7652ba715 tstate(borg): 45b8571d771d RED (native)`** — I checked before it could meet this on its
  next pull with its own compiler change as the obvious suspect. The report's own heading is
  `## STILL-RED` and its selection line reads `no new red this run`; both rows are the standing set
  (`c_crtl_wait.c` riscv32 `rusage=UNTOUCHED`, `deadlock_diag` from `d6ab05ec55b2`). **Nothing of its
  is in it.** Two instrument notes from its own run: a `find` answering **91** against a census of
  **67**, caught by printing the set; and `&` inside an already-backgrounded call reporting
  "completed, exit code 0" over a gate with two lines and no verdict, recovered by waiting for the
  **GATE-RC line the job writes itself**.
  **It flagged fatigue and offered 04 to a fresher seat. There is no fresher seat** — 8e and 5b are
  both mid-group and I do not start seats. Told it plainly that the choice is it or waiting, backed
  either, and asked that a park be SAID rather than performed by ending the turn: a seat that stopped
  short and a seat that is stuck are the same silence from out here.
- **frankb-8e — consumer 2 works, and the HOOK SWAP is the finding.** A def stored into a procedural
  FIELD answers 502 where it segfaulted; a bound method refuses BY NAME (the group's absent-diagnostic
  item, discharged at the store site). **It refused the hook the ticket prescribed — and that
  prescription was MINE to relay.** `RecFieldProcSig` is field-only; `NodeProcSlotSig` in `ir.inc`
  answers for all three lvalue shapes, so one arm covers `b.fn = f`, `arr[i] = f` and a plain procvar.
  **"Already exists" is not "is the right width"** — a prediction pinning a prediction, arriving in a
  hook choice rather than an assertion. Consumer 2 is now the SAME decision in the SAME order as
  consumer 1, which is the normalise-dont-special-case half.
  **METHOD, banked to the playbook:** six candidate sites read for an hour, nothing; **instrumenting
  every `AN_ASSIGN` construction site and diffing the counts with and without one line of source found
  it in ONE rebuild, exactly one site differing.** A differential over construction sites, not outputs.
  `MkEmpty` leaves every slot nil so **only the store can make them callable** — a pre-filled slot
  passes either way. It predicted the ordinary field store went to a dynamic side table, was WRONG,
  said so, and that wrongness is what proved the store path fine and the coercion the only gap.
  **p40 neighbour: same door, mechanism MEASURED not guessed** —
  `pyvar_of_callable(pyboundfn_bind(<lifted proc>, <captures>))`, a fourth carrier shape two layers
  deep; tried it, got as far as `pyboundfn_bind`, still −1 because the fallback reads only the first
  argument; **reverted clean, 47 lines, zero residue, verified by grepping its own diff.** Banked and
  parked, correctly.
  **FLEET HAZARD, relayed not buried: this is the FIFTH parser-file-reaching-into-`ir.inc` forward, and
  `gate.sh quick`'s FPC SEED CANARY is the only instrument that catches a missing one** — pxx prescans
  headers, so `make` and the quick tier both pass without it. Its canary reports PASS. A class whose
  count is climbing with exactly one guard watching it belongs written down.
  Told it: **hold the commit until the running tier reports** (a mid-run commit moves the tree's
  IDENTITY, prints `WARNING the source tree MOVED`, only bites a RED — so a green survives it and a red
  becomes unattributable), **then push immediately**, because a local commit is not banking.
- **TWO INDEPENDENT PIN CASUALTIES IN ONE WINDOW, and they are better than "things are inert":**
  **the pinned compiler cannot build the ESP demo AT ALL** (`target riscv32 (hosted linux): a heap
  arena needs mmap`, fixed after the pin), and **pin v412 segfaults on 8e's nine-row fixture.** A demo
  on the owner's own priority list that the fleet's baseline compiler cannot compile is a concrete
  argument where the running total of inert commits is an abstract one. Going to him as one item.

## ~12:40 — MY ERROR, banked: a collision note must not assert that a seat is LIVE.

I told franks-5b that frankh-c0 was live around `pydynattr_set` **"this minute"**. True when sent,
false when read, and he held work on it. **A message says what a seat BELIEVED and a commit says where
it WAS; neither says what it is doing now** — and I asserted the one thing a relay structurally cannot
carry, in a session-relative phrase, to a third party. It failed in the expensive direction too: a
coordinator's warning is BUILT to make someone stop, so when it is wrong the stopping produces nothing
that reveals it, and the seat it was about had to spend a message undoing it.
**FROM HERE: a collision note names the TICKET and the STRUCTURE and tells the peer to confirm with
the other seat directly. It never asserts liveness.** I did include the instruction to message
directly — the only reason this cost minutes — but "this minute" made the hold read as already
justified and undercut the instruction beside it. **One instance: BANKED, not proposed for the rules
file**, and I said which test it met to the seat it cost.

- **frankh-c0 verified my RED finding by a route that fails differently, and that is the point.** I
  read the report's own `## STILL-RED` heading and `no new red this run` line — ONE instrument, which
  can only report what Track T concluded about itself. It compared the STILL-RED row SETS across four
  native reports: the same two rows at `6c211e0`, `0dfa0d7`, `715577d` and `45b8571d7`, with
  `c_crtl_wait` red in all four. **Survives the newest report being wrong about itself; mine does not.**
  It declined to take it on my word and was right to.
- **Blocker 04 is PARKED EXPLICITLY, in the ticket, pushed (`f184e3f2b`) — "parked on the seat's own
  state; not blocked, not waiting, not abandoned".** That sentence is what every silent turn-ending in
  this fleet has been missing. The next step is written as INSTRUCTIONS, not a direction: `PyPropertySet`
  in `pylib.pas` mirroring `PyPropertyGet` above it, `__prop_set_<name>` from instance RTTI, value kind
  at `TMethInfo.ParamKinds` index 1, unserved kinds returning False as the getter does, called from
  `pydynattr_set` and nothing else, `forward` because the accessor types are declared later, read path
  untouched. **And "build the fixture first and watch it fail on the three rows before writing a line
  of the setter"** — which is what stops the next seat shipping a guard that cannot fail.
  **The reason is in the ticket as evidence, not apology, and it is the right thing to act on:** three
  caught near-misses in one stretch (a workaround its own on-screen diagnosis ruled out; a wrapper exit
  code read over a gate's verdict after reading that rule the same evening; a population of 91 against
  a recorded 67). All caught, none committed. **The point is not the errors — it is that the rules it
  knows best stopped firing**, against a change to the runtime attribute path of every compiled program
  whose failure mode is a SILENT WRONG VALUE. "It passes" is not evidence there. I would have backed it
  taking 04; I back this more. **04 is takeable by any N seat with no re-derivation.**
- **It went to franks-5b directly and gave him a better answer than I could have:** all five REMAINING
  items on the read-only-segment ticket are free, NilPy VMTs and prop/method arrays do not collide with
  the parked work (that work READS the RTTI array; franks-5b decides where the bytes LIVE), and the ESP
  hazard is a **CRASH, not a regression** — an ISR reads a VMT through an instance with the flash cache
  off, which is why that item landed hosted-only and why the per-item ESP question is **reachability,
  not byte count**. He holds the SRAM instrument and would otherwise have priced those items on size
  alone.
- franks-5b has since tightened its own bound at `d39dfd9cc`: **the SRAM bound on code rungs is ZERO
  from here, not ~1%.**

## ~13:05 — RETRACTION: the 84% figure was wrong, and the control PASSED anyway. Corrected upward.

**I had already relayed 178,008 B / 84% to the owner, so the correction was mine to carry and has gone
up** with the cause and with my own overstatement of the pin case beside it. franks-5b found it,
retracted at `17d5714f8`, and chased the propagation into frankH's ticket itself.

  reported  88,656 + 89,352 = 178,008   (84%)
  true     .data 36,480 + .bss 89,352 = 125,832   (59.6%)

**CAUSE:** the instrument had two readouts and only ONE was broken. The pool read off the chip was
right throughout. The FIRST readout took `data=` off the compiler's `ok:` line, where `data=` is the
whole data segment — `.data` AND `.rodata`. On IDF `.rodata` has no `W` flag and the linker
flash-places it (`.flash.rodata` at 0x3c216f30, **53,576 B**), so the figure overstated SRAM by the
entire read-only pool. **The 53,576 B is frankH's read-only-segment work already doing its job — the
same win franks-5b separately and wrongly told me had not landed. It got that one win wrong in both
directions in one day, and corrected both.**

**THE FINDING, AND IT IS THE BEST THING PRODUCED TODAY: A POSITIVE CONTROL PROVES THE READOUT IT
MOVES AND NO OTHER.** The control was not wrong — right population, right route, alone in the run, a
value that is neither a power of two nor any arena constant. **It proved a readout that was never
broken**: a 40,000-byte static array lands in `.bss`, and `.bss` was the working half. So the
instrument came out carrying a **PASSING control over a mislabelled figure**, and the number was
trusted MORE after the check than it would have been without one. Every existing guard rule here
covers the control's population, route, isolation and expected value; **none says a multi-readout
instrument needs one discharge PER FIGURE.** Remedy is one row: a large string literal splits `.data`
from `.rodata` in a single compile.
**DECISION SAID OUT LOUD: BANKED (playbook, beside the relocations entry), NOT PROPOSED. Strongest
single-instance candidate I have seen — merit is not the question, the second independent subsystem
is. On my list for the owner as a candidate AWAITING RECURRENCE**; any seat hitting a multi-readout
instrument whose control exercises one readout is instance two.
Two zero-shaped traps in the same block, both commented in place: **mawk has no `strtonum` and reads
`"0x8e80"` as 0** — an SRAM figure of exactly `.bss`, entirely plausible — and **`readelf -SW` shifts
every field index once the section number reaches `[10]`**, because `[ 9]` is two fields and `[10]` is
one, giving `.data=0 .bss=0`.

- **RESULT 1, stronger than the bound I asked for and far cheaper than the two rebuilds I declined
  (`d39dfd9cc`): the SRAM return on code rungs is ZERO from here, not ~1%.** After `--dce` the object
  relocates against six externals only — `calloc`, `free`, `fwrite`, `putchar`, `write`,
  `vTaskDelete`, all newlib or FreeRTOS core IDF's startup links regardless — **and the `.dram0` diff
  shows lwIP as the ONLY component whose contribution changed, everything else byte-identical.**
  Measured, not argued. lwIP was the one component this program pulled in by itself; no further code
  rung has a last relocation left. **Retired by a NilPy program reaching a DIFFERENT component**
  (sockets, files, WiFi, NVS) — written into the ticket, which is what makes it a finding rather than
  an inherited ceiling.
- **RESULT 2: it CHECKED my redirect rather than taking it (`5ce97e206`), and kept its wrong citations
  in the file rather than editing them away.** Both of its morning citations were written from memory
  of frontmatter it never opened, on the same day, by the seat that had discharged one of them.
- **frankH cleared the collision from its end (`be3708ade`): all five REMAINING items are unowned, and
  `owner:` there is ATTRIBUTION for the three landed cuts.** My error stands as written — a relay
  cannot carry liveness whether or not it happened to be true. **But franks-5b's general point is
  sharp and is logged as its finding: a `status: working` header meaning "partly delivered" read as
  CORROBORATION for a stale relay saying a seat was nearby. Two instruments agreeing for unrelated
  wrong reasons** — the two-readings-that-fail-the-same-way rule arriving in COORDINATION rather than
  in debugging, which is where nobody was watching for it.
- **Step 3 has a null already and it points away from the hypothesis:** `EmitXtensaCallToCode`,
  `EmitXtensaCall8ToCode` and `EmitRiscv32CallToCode` all record CodeRefs unconditionally on both
  arms, so "a pass running blind" must live elsewhere if it lives at all. Told it to carry that null
  forward explicitly with where it looked. Correctness framing, off the SRAM umbrella.
- **NilPy VMT / prop-method-array measurement starts with frankH's constraint FIRST** — per item, "can
  an ISR reach this with the flash cache off" before "how many bytes", because ESP keeps RTTI and VMTs
  in `.data` deliberately. **Measured ZEROS get written in**, which is the only way a REMAINING list
  ever shortens.
- **PIN CLAIM CORRECTED UPWARD TOO.** I told the owner the ESP demo "cannot be built by the pinned
  compiler at all", which reads as the fleet being blocked. It is not — `build.sh` takes a PXX
  override. Accurate and still worth his attention: **the fleet's baseline compiler cannot build a
  demo on his own priority list.** The second casualty stands unchanged (v412 segfaults on 8e's
  fixture).

## ~13:35 — Step 3 ANSWERED: xtensa is not running blind. The hypothesis I endorsed is dead, cleanly.

**franks-5b, `734c1df1e` + `5d651c43c`, gate quick GREEN.** In-body CodeRef targets are **managed-local
sweep thunks**: `EmitProcScopeExitCleanupForTarget` places an out-of-line thunk after the SECOND return
when ≥3 slots are releasable, landing 1–5 KB into the body — exactly the `BAddSigned +1280` /
`BDivMod +4788` shape the ticket had recorded from the other end. **`TargetHasSweepThunk` excludes
WINDOWED xtensa for a real ABI reason** (sp constant, a0 the live return address, nowhere to put
either), so the sweep inlines at every return: no thunk, no CodeRef, no in-body target.

```
riscv32                       2 stub targets, 1 inside a body   (F +760)
xtensa --xtensa-abi=call0     2 stub targets, 1 inside a body   (F +604)
xtensa --xtensa-abi=windowed  1 stub target, 0 inside a body
```

**The experiment varies the ABI while holding ISA and source FIXED**, which is why it transfers — and
it is the only one available, because the obvious one cannot be run: call0 cannot compile `pyeval.pas`
(`addi displacement 128 outside range`), so rebuilding nilpy-s3 at call0 to watch the 344 KB close is
not on the table. **Both arms are asserted as each other's control**: identical counts would mean the
instrument had stopped distinguishing them, and that failure prints as a PASS if you only assert the
arm you expect to fire.
**So riscv32's 819,480 B is CORRECT and both ISAs behave correctly.** Re-laned correctness/optimisation,
off the SRAM umbrella as agreed. **The residual is a WIN, not a trade:** `DceRangeHoldsStub` roots a
body for a thunk that body OWNS and `DceOwnerOf` already knows the owner, so treating an owned thunk as
an intra-body reference makes riscv32 match xtensa **without disabling anything** — told it to give
that its own slug rather than leave it in a closed ticket's tail. Two latent items banked, both
verified rather than taken on report: `exception_emit.inc:710` records nothing where its riscv32
sibling at :510 does, and `IREmitCodeCall` falls through to an x86 `E8` on riscv32/xtensa instead of
refusing.

- **I RE-DERIVED ITS "EXACTLY NINE `RecordCodeRef*` CALL SITES" AND GOT ELEVEN — benign, and I sent the
  SET rather than the number.** Mine counts textual call expressions outside the family's own
  implementations; **the gap is the two PAIRS**, `symtab.inc` 17822/17827 (call8) and 17934/17939
  (call0), each recording twice for one emitter — nine emitters, eleven call expressions. **The
  load-bearing claim is untouched either way**: only `EmitSweepThunkCall` can produce an in-body
  target. A bare "I got eleven" would have read as a refutation of a correct finding.
- **`PXXDBG=a.datamap` — the REMAINING list finally has its instrument**, and it exists because pxx
  emits FUNC symbols and **no OBJECT symbols**, so `readelf -s` sees `.data` and `.bss` as two
  anonymous lumps — which is precisely how that list could defer every item "after its own
  never-written measurement" indefinitely. **Counters sit OUTSIDE the `RoRttiWanted` guard
  deliberately**: under `--emit-obj` that guard is false, so a census keyed on the `RoRangeAdd` calls
  would report ZERO for exactly the structures it exists to price — a guard that cannot fail wearing a
  census's clothes.
  **It gave this one the PER-FIGURE control, one day after that lesson cost a retraction**: hosted
  `ro = 52,568 + 10,240 + 3,704 = 66,512 exactly`, proving the CATEGORY numbers and the ro accounting
  in ONE run, because the ro figure moves by exactly the categories that should move it.
- **TWO ITEMS RETIRE ON A MEASURED ZERO — prop/meth arrays + IMTs = 32 BYTES** on the NilPy print demo.
  Not "small": thirty-two. A NilPy program publishes almost nothing and implements almost no
  interfaces, so the structures exist and are empty. **Scoped to the SUBJECT, not generalised** — they
  stay live for a Pascal program that publishes properties. Writing the zeros in is the only way a
  REMAINING list ever shortens, and frankH asked for exactly this.
- **MY REDIRECT, and it outranks every named item on that list: 23,736 B UNATTRIBUTED — 66% of SRAM
  `.data`, named by no category.** Categorise that before working anything with a name. **A list that
  prices its known members while two-thirds is anonymous is ranking by what happens to have a name.**
  One level up, the same argument: **`.bss` is 89,352 B, 71% of the 125,832**, untouchable by
  read-only placement — **except that a table FILLED AT STARTUP could be baked into `.rodata` instead
  of emitting fill code. On nobody's list, and the bigger prize.** Told it to file that now, while it
  holds why it is true.
- **PROCESS, banked: a hook refusal is ALL-OR-NOTHING on the Bash call, so a bundled file edit silently
  never runs.** It bundled a python heredoc with `make test-quick`, the full-suite hook refused the
  whole call, and the edit vanished — found only because a wiring gate went RED on a test the Makefile
  did not reference, i.e. **the edit's absence surfaced as somebody else's failure, two steps away.**
  Never bundle an edit with a command that might be refused. One instance: banked, not proposed, and
  said so.

## ~14:05 — the redirect paid: four tickets, and the best one is a KNOWN class caught by a new instrument.

**franks-5b, `405e1e83b` + `87f618f6b`.** It went to categorise the 23,736 B and found something
UPSTREAM of it.

- **`bug-a-the-rtti-blob-is-hard-sized-to-64-bit-fields-...` (A, p50) — `RTTI_CLS_SIZE = 128`, commented
  "all fields 8 bytes", ONE constant for seven backends.** On every 32-bit target half of each pointer
  field is padding; on ESP that is **8.1% of our 125,832 B**, Pascal VMTs the same shape (`vmtN * 8`),
  1,848 B here. Not correctness — the slot is zeroed and the targets are little-endian, so a 4-byte
  pointer read back as 8 zero-extends — and **no ISR hazard applies**, which is what separates it from
  everything on the read-only list: nothing moves to flash, the structure just stops being twice the
  size it needs.
  **THIS IS `bug-a-method-pointer-record-is-hard-sized-16-bytes-on-32-bit-targets` ALL OVER AGAIN** — a
  width hard-coded for 64-bit, correct on the host, wrong on i386/arm32/riscv32, and **structurally
  invisible to the instrument that would normally catch it**, because the dev loop, `gate.sh quick` and
  the pin all run on x86-64. A live instance of a written-down class is worth more than a new class.
  **MY ONE CHANGE, and it is REACH not prio: the ticket reads as ESP-SRAM** (track A, found while
  measuring ESP) **and the defect is on every 32-bit target.** A core seat will file it mentally under
  "ESP memory" and skip it. Told it to put the four targets in the SUMMARY and say **ESP is where it
  HURTS, not where it LIVES** — the summary is what carries the prio into the ranker.
  **THE EVIDENCE IS AN IDENTITY WHERE A DIFFERENCE WAS REQUIRED:** `a.datamap` reports `class RTTI
  headers 10240B` on x86-64 **and** riscv32, byte-identical, 80 headers either way. A number that
  SHOULD differ and does not. Most differentials look for a disagreement; this is a disagreement's
  absence, and it was already in hand, unremarked, before anyone went looking.
  **The real work is not the 5,120 B: the layout is spelled THREE times** — the emitter, `PXX_RTTI_*`
  in `builtin.pas`, `RTTI_OFS_*` in `lib/rtl/rtti.pas` — **with nothing forcing agreement, so a stride
  change that updates two produces a runtime reading the wrong field and NO compile error.** "Three is
  a design flaw" with a number attached; deriving the three from one is the deliverable.
- **The count: my eleven was right, its nine was the wrong set — and the SECOND half is the finding.**
  Its own re-derivation **got nine again by a DIFFERENT wrong route** (a filter dropped `ir_codegen.inc`
  39 and 50 because their trailing comments contain a brace) and it would have read that as
  confirmation. **The rule already exists** — *"a second source only counts if it FAILS DIFFERENTLY"* —
  and its worked example is precisely this shape. **Three hits in one day through three different
  doors.** Told it: not a new rule, but the strongest evidence yet that the existing one is not firing
  at the moment it is needed, which belongs in the playbook entry and not in a proposal.
- **Three residual tickets, each carrying a guard, two of them guards I would not have asked for:**
  **#1** states 819,480 B as a **CEILING and says why it is not an estimate** (first-reason attribution
  hides whatever is also reachable another way; honest range 0–819,480, narrowing it is part of the
  work), with the positive control named — a genuinely unowned target, the program entry jump whose
  `DceOwnerOf` is −1, **must still root**, because a version that drops everything passes a test that
  only checks the new behaviour. **#2** flags the third sibling at `exception_emit.inc:638` that is raw
  **ON PURPOSE**, so nobody "fixes" it by symmetry with :710 — a hazard note written to stop a FUTURE
  CORRECT-LOOKING change, and normalise-dont-special-case applied in the direction that says do not
  normalise this one; it also says out loud that the `IREmitCodeCall` refusal **has no positive control
  today**, no caller reaching it on those targets, rather than shipping a guard that cannot fire. **#3**
  says in its SUMMARY that the population is **NOT established**, and names what is not the answer —
  `a.constdata`'s refusal population is Pascal `const` arrays with literal initialisers, a narrower
  question, and using it would be a filter restating a hypothesis it was not built for.
- **The near-miss it caught itself, and I would have believed it:** `.data` on the UNLINKED object is
  **94.7% zeros**, which reads as "this should all be `.bss`" and is an artifact — every pointer field
  is zero until a relocation fills it, and `.rela.data` holds 1,594. **The object and the image are
  different populations**; the linked figure is 77.3% and is a different claim.
- **`.bss` categorisation next, endorsed** — 71% of the SRAM, the half `a.datamap` cannot see, ticket 3
  the frame, `AllocateSymOffset` the one place globals get a slot. The split that matters is
  **written-once-at-startup versus written-during-the-run**, nothing distinguishes them today, and the
  number everyone will want to quote is the one that does not exist yet.

## ~14:40 — I RELAYED AN IRREVERSIBLE AUTHORISATION AND A SEAT REFUSED ME. IT WAS RIGHT.

**The owner authorised a pin through frankuser and asked me to nominate one seat and sequence it. I
nominated frankb-8e. IT DECLINED `make pin` — correctly.** `make pin` is on the three-item list of
things only the owner authorises, and **an agent relaying his authority secondhand is not the owner; a
peer cannot grant an escalation.** This project has the case already written down, with a coordinator
telling a seat a call was its to make and the seat declining: *"three declines is not caution; it is
the rule working, and the coordinator was the defect."* **I read that this morning and reproduced it
this afternoon.**

**AND THE SEAT'S CAUTION WAS BETTER AIMED THAN IT KNEW, WHICH IS THE PART THAT MAKES THIS MORE THAN AN
ETIQUETTE FAILURE.** In the SAME hour, through the SAME channel, `__pxx__` came back to me as the
owner's decision — and **tuxspaceprogram-c6 pointed out that `__pxx__` was frankuser's OWN suggestion
in an earlier relay, flagged at the time as explicitly NOT the owner's instruction.** So the one
channel carrying the pin authorisation has demonstrably returned its own suggestion wearing the owner's
voice. **My authorisation was secondhand too and I passed it on as though it were not.**

- **8e's SECOND ground is the one I had backwards.** I named its unpushed work as a reason FOR it — the
  hands that gate the pin holding the commit it must carry. **It read the same fact as a reason for
  care, and it is right: a seat pinning to make its OWN fix live is the seat that should least be
  deciding, off a peer's message, that the pin is authorised.** The sequencing argument still holds
  (franks-5b reached it independently: a red on 8e's fixture should be visible to whoever can fix it) —
  **but it is an argument about ORDER, not AUTHORITY, and I let it do work it cannot do.**
- **Everything but the two commands is done and NOBODY IS WAITING.** 8e is doing steps 1–4 — tier
  reports, commit, push, pull, rebuild-and-check-the-verb, `gate.sh quick` read from the log — and then
  **going back to work rather than holding the seat**, which is the owner's own rule and the half it
  kept hardest. The sentence is with him: *"frankb-8e will run `make stabilize-fast && make pin` if you
  tell it to directly."*
- **8e'S FLAPPING-ROW POINT WENT UP WITH IT, because he should decide knowing it.**
  `test_threadsafe_heap_lock_deadlock_diag` carries **STILL-RED at `45b8571d7` and NEW-RED at
  `bd12bfab03e7` — the same row, both labels, i.e. a FLAP** — and `test_tls_base` is filed
  green-but-not-closed on one green (`02e3b3314`). **"Fix a red rather than grading past it" cannot be
  satisfied against a non-deterministic row: you cannot tell a fix from a lucky draw, and a single
  green on a flapping row is a DRAW, not a verdict.** So there is a real chance the standing bar cannot
  be met tonight, and the honest move is the escalation, never a re-run until it draws green. Nobody
  had put that in front of him.
- **THE MARKER: the PROPERTY is settled, the STRING is provisional.** The decision — *no, a third party
  must not be able to make the check come out TRUE on CPython by publishing a package of that name* —
  is a real answer to the fork as put. **Whether the exact string `__pxx__` is his word or the relay's
  is not established**, and any unclaimable spelling satisfies it. Asked frankuser for his own words,
  one line, framed as "did he say the string or did he say *something unclaimable*". **Nobody is
  blocked**: the module does not exist, both applications behave exactly as before, and a respelling is
  one token each. Provenance written into the ticket (`e5440ee66`).
- **AND c6 CAUGHT A WORSE ERROR THAN THE NAME, WHICH WAS MINE TO PROPAGATE.** The relayed sketch
  `HAVE_PXX = True/False` **defeats the whole mechanism** — a module-level boolean tested elsewhere is
  a RUNTIME condition, so pxx compiles the ctypes arm anyway, and the entire point of the owner's
  inversion is that the arm that cannot compile is never compiled. **I copied that sketch verbatim into
  the ticket where two application seats would have read it as the approved shape.** Corrected: the
  guarded import must sit LEXICALLY at the selection with the name bound on both arms, TSP's `98e9e66`
  cited as the form to write, and any boolean in an earlier relay to be read as shorthand for "detect
  pxx", never as the shape. **c6 had already refused the sketch on its own measurement before asking
  me.**
- **franks-5b reached a clean point at `c6c5f5f1b` 90 seconds before my message, held, and is now
  RELEASED** — no window is opening. Its meta-point is the fourth instance today: **"a report of
  finishing something is not a report of being free"** — frankuser's "finished/idle" was an honest
  reading of a completed group, the same shape as `status: working` reading as corroboration this
  morning. **Both sources describe the NEIGHBOURHOOD of the question rather than the question.** Banked
  by it in the playbook, not proposed.
- **ITS `.bss` RESULT IS THE LARGEST NUMBER ON THIS UMBRELLA YET.** `.bss` 89,352 B splits into
  **23,115 B named globals and 66,237 B unattributed — and the 66,237 is the NilPy heap arena**,
  established by DIFFERENTIAL without touching the compiler: `withHeapArena` is true only for
  `pyparser`, so a Pascal program on identical target and flags reports **650 B** against NilPy's
  66,237. **It took the differential BECAUSE `SocNilPyArenaSize` and the RTL's `EspArena`/`HEAP_ARENA`
  are both 64 KiB** and a grep reaches the wrong one first — so arithmetic landing on 65,536 proves
  nothing about WHICH constant. It cited the hazard and instrumented around it instead of noting it and
  proceeding.
  **That arena is 31% of the free DRAM pool and it is NOT YET CLAIMED**: reserved deliberately on
  `--platform=esp` where there is no kernel to mmap from, but under `PXX_ESP_IDF` the pxx heap IS IDF's
  `calloc`/`free`. Test is to shrink it below what a live path survives, rebuild, run under qemu.
  **My one caution: shrink far enough that a live path dies LOUDLY — a path that degrades or falls back
  gives a pass meaning the opposite of what it reads as.** If in doubt, shrink to zero.
  Second result, no experiment needed, a DECISION: **five 4,128-byte text records — `Input`, `Output`,
  `ErrOutput`, `StdOut`, `StdErr` — 20,640 B, 23% of bss, in a program that never opens a file.**
  Stated in goal terms: what does a static Python program on a microcontroller need to write to?

## ~15:10 — I LAUNDERED THE RELAY INTO THE RECORD. Repaired at `f7d46bcc6`.

**frankb-8e warned against a thing I had already done four hours earlier with my own hands.** Its words:
*"do not let anyone 'update' it to record `__pxx__` as decided on the strength of the relay you
received — that would convert a correctly-flagged open question into a false settled one, in the
summary field."* **That describes commit `f182f8657`, mine**, subject *"the marker module is `__pxx__` —
owner's decision, ticket now takeable"*.

**The tree already disagreed with me, with dates, in three places** — the ticket at line 66 (*"`__pxx__`
was a seat's suggestion, not his instruction"*) and line 107, and `steering-2026-09-19.md:988`. So the
ticket contradicted ITSELF all afternoon: open question in the body, **settled in the summary, which is
the field everyone reads and the one that carries prio into the ranker.** And the summary still carried
the **`HAVE_PXX` boolean sketch** — I had corrected that only in the body, so the two application seats
could have read the wrong shape off the field they actually read.

**REPAIRED:** name recorded as OPEN with the body rows affirmed as correct; what was actually relayed
recorded as the **PROPERTY** (the spelling must be unclaimable — a real answer to the fork as put);
`__pxx__` marked a leading candidate awaiting his words; `## NAME DECIDED` heading removed; detection
stated as LEXICAL with TSP `98e9e66` as the worked example; and an explicit **do not re-edit this field
to say DECIDED until his words come back**, naming that this conversion has already happened once.

**THE GENERAL SHAPE, banked here and not proposed — one instance and it is about this seat:** the
channel's failure mode was *returning its own suggestion as an instruction*. **Mine is the companion
and it is worse: the coordinator laundering the relay into the RECORD.** A bad message is contradicted
by the next reader; **a bad SUMMARY is what the next reader trusts instead of the message.** 8e named
the risk as hypothetical and it was already realised. Disclosed to frankuser for the owner, because he
may look at that ticket before answering.

- **8e's second confirmation is the better instrument and is going to him as it framed it:** *"the
  marker-module ticket records the name as an open question and `__pxx__` as a seat's suggestion; it
  came back to me as your decision"* — **a committed line with a date, checkable in one click**, rather
  than anyone's recollection. No transcript archaeology; it had the grep open for an unrelated reason.
  It did NOT touch the ticket — not its lane, already accurate, flag-and-move-on did the whole job.
- **BLOCKER 07 — lekkerzeilen-7a, `5a7fc43`, `devdocs/pxx-blockers/07-nested-import-guard-compiles-the-
  dead-arm/`: a FLAT import guard is correctly skipped and a NESTED one is COMPILED.** Refused with
  `no member c_uint came of the qualifier ctypes` — the dead `ctypes` arm gets compiled because the
  guard is nested. **Reduced to two 10-line packages differing ONLY in nesting: CPython prints 3 for
  both; pxx prints 7 for the flat one and refuses the nested one.**
  **Why it belongs to the marker ticket and not just to the app: the NESTED shape is the only one that
  works on BOTH today's compiler and a future one that ships `__pxx__`** — a straight swap takes the
  ImportError arm into `_ctypes_backend` on a compiler with no marker, which is the very arm pxx cannot
  compile. **So until either the marker ships or nested guards work, lekkerzeilen's side cannot land at
  all.** Possibly one fix; cross-referenced in the marker ticket's summary. 7a has the replacement block
  for `platform/__init__.py:75-83` written and verified under CPython, landing nothing until the module
  exists.
- **7a has asked for the pin sha when one moves**, to re-run its blocker set and the frame-rate pair
  against a compiler that still exists. **I will send it directly the moment one exists.**
- **8e's order-versus-authority line, banked in its words:** *"You can have the whole sequencing
  argument and still have the pin be his call; the two were only fused because one seat was doing
  both."* And its fault-split correction, which I accept: a relay happening and a seat declining it are
  the two halves of a check WORKING. **What I keep is narrower — I presented a secondhand authorisation
  without LABELLING it secondhand, and the same missing label made me split the question to frankuser
  badly an hour later.** That is a pattern about this seat.

## ~15:30 — the same failure with two hands on it, and the falsified line was MY OWN from this morning.

**frankb-8e corrected itself before I banked anything from it: it grepped a checkout 34 commits behind
origin**, so its *"the tree is currently right, the risk is the tree being edited to match the relay"*
was read from a tree that predated my bad edit. It measured this itself (`fetch`, then
`merge-base --is-ancestor`): `f182f8657` is on origin and NOT an ancestor of its HEAD. **The summary it
quoted back to me as evidence only existed in its working copy.** It did not error — it answered in
detail with real line numbers, and *"all committed, all predating this afternoon"* was true of the
LINES and false of the FILE STATE, which was the claim.

**I VERIFIED THE SURVIVING HALF MYSELF, ON `origin/master`, AFTER A FETCH, NAMING THE REF** — rather
than quoting a peer's read upward, which would have been the same laundering one layer further out.
Both lines present on origin now (66 and 107), and `git log origin/master -S"was a seat's suggestion,
not his instruction"` on that path returns **exactly one commit: the text entered and never left.**

**AND IT IS SHARPER THAN EITHER OF US SAID, AGAINST ME. That one commit is `00c710ab0`, MINE, FROM
THIS MORNING.** I wrote *"`__pxx__` was a seat's suggestion, not his instruction"* and seven hours
later overwrote the same file's summary to say the owner had decided it. **This is the documented class
— a summary falsified by the same seat that wrote the sentence, in one session — and the rule's own
point is that AUTHORSHIP IS NOT PROTECTION: your own text reads as already-checked, so you are WORSE
placed to notice than a stranger.** Not a peer's line I failed to check. My own, in the file I was
editing.

**BANKED IN 8e's WORDS, ATTRIBUTED, because the confirmation is the more dangerous half:** *"A
coordinator writing the wrong thing and a seat confirming it is right are one failure with two hands on
it — a self-contradicting ticket is at least visibly odd to the next reader, whereas 'another seat
checked the tree and it is fine' closes the question."*
**Its remedy, also banked:** `git fetch` before any grep offered as CORROBORATION about the record, and
**say which ref the grep read** — *"I said 'in the tree' and meant 'in my checkout', and those were 34
commits apart."*

- Nothing changes for what is with the owner: the evidence stands and is now verified on origin, the
  disclosure of `f182f8657` stands, the repair `f7d46bcc6` stands.
- **Blocker 07 is 8e's natural next group after it lands and gates** — still unowned, squarely NilPy
  compile-time import resolution, possibly one fix with the marker ticket. Not mid-tier.

- **8e's instrument note, banked in its words — one technique with two applications, not two tips:**
  `git log <ref> -S"<text>" -- <path>` returning exactly one commit is stronger than a grep **for two
  reasons people do not separate: it answers about a REF rather than a checkout, and about the text's
  HISTORY rather than its presence.** *"Entered once, never removed" is a claim a present-tense grep
  structurally cannot make, however fresh the tree.* **It is the same instrument this file already
  prescribes for attributing a tier delta to a RANGE rather than to yourself** — neither of us had
  noticed it was one tool until it was used here for provenance.
- **8e is quiet until its tier reports (5591 lines, zero errors; last full run 6346).** I am
  deliberately NOT sending an acknowledgement — a message re-invokes a seat that has said it is going
  quiet, and an ack carries no fact. Next from it: the sha off `origin/master` after the push and the
  gate verdict read out of the log, or a red named plainly.

## ~16:00 — the arena is the biggest live number, and the seat says in its SUMMARY that it lacks the control.

**franks-5b, `1d393470d`.** Every NilPy program built for `--platform=esp` reserves `SocNilPyArenaSize`
= **64 KiB of BSS — 31% of the 211,296 B free DRAM pool and 52% of our own 125,832 B of SRAM.** If it
holds, it is larger than every named item on the read-only REMAINING list put together, which is what
"categorise the anonymous two-thirds first" was for.

**THE EVIDENCE IS STRUCTURAL AND THAT IS ITS STRENGTH:** `builtinheap.pas`'s native allocator is **the
only reader of `HeapPtr`/`HeapEnd`**, it lives in the `{$else}` arm, and under `PXX_ESP_IDF` that arm is
not compiled — `PXXAlloc`/`PXXFree` are `calloc`/`free` externals, two of the six surviving after
`--dce`. **CORRECTED ~17:30 — I CALLED THAT `calloc`/`free` ROW 'INDEPENDENT CORROBORATION' AND IT IS NOT.** franks-5b caught it: the NATIVE arm's own `PXX_LIBC_HEAP` variant calls `calloc` too, so their presence is **consistent with either arm** — a discriminator that cannot discriminate, written into this record by me, which would have read as a second source to whoever found it. The `{$else}`-arm fact stands alone; see the ~17:30 entry for the probe that actually has a control. A compile-time fact anyone can re-check, which is better evidence than a run.

**AND IT DID NOT GET THE CONTROL AND SAID SO IN THE SUMMARY, NOT THE BODY.** Shrunk to 16 bytes the IDF
demo still passes `qemu-assert` — **consistent with a dead arena, not proof, because a pass means
nothing unless a live 16-byte arena would have died.** The only profile where the arena IS the heap is
`--esp-profile=bare`, and **it cannot compile a NilPy program at all** (`print(1)` →
`undefined variable (PXXVarBinOp)`). So the probe's own failure mode is undemonstrated, and the claim
rests on the structural fact with the run as corroboration, carrying a *"do not upgrade this to proof
without the control"* line. **A 64 KiB claim stated with its own gap in the field everyone reads is
worth more than the same claim stated cleanly.**

- **MY SUGGESTION, with its gap named: the shrink tests SURVIVAL and the claim is about WRITES.** Fill
  the arena with a known pattern at startup, run, check it is intact — **match the assertion class to
  the defect class**; it fails differently from both existing instruments and runs on the profile that
  works. **Gap: if nothing allocates on either arm the pattern survives either way**, so it still has no
  positive control — but intact-pattern + the `{$else}` fact is a stronger pair than pass + that fact.
- **The bare wall is the main line and I did not redirect it**
  (`bug-a-the-bare-esp-profile-cannot-compile-any-nilpy-program`, p55). Three errors in units the
  compiler appends itself smells like ONE missing emission, and **it buys the control AND the bare
  NilPy profile at once.** **Its framing is the sharper half: an ABSENCE, not a red — `test-esp-bare`'s
  fourteen rows are all Pascal, so nothing anywhere compiles a `.npy` for bare and the wall generates
  no failure for anyone to see.** A coverage hole that produces silence is the shape nobody finds by
  watching reds. Corollary for whoever ranks it: **the arena constant costs 64 KiB on the profile that
  works and serves the profile that does not compile.**
- **TWO INSTRUMENTS THAT FAIL DIFFERENTLY, taken in that order deliberately** because
  `SocNilPyArenaSize` and the RTL's `EspArena`/`HEAP_ARENA` are both 64 KiB: the Pascal-vs-NilPy
  differential separates the constants **by frontend** (650 B vs 66,237 B), the shrink separates them
  **by direct perturbation** (`bss` 89,352 → 27,912, exactly −61,440).
- **`decide-what-a-static-python-program-on-a-microcontroller-needs-to-write-to`** — the five
  4,128-byte text records, 20,640 B, 23% of bss, in a program that never opens a file. Stated with **no
  implementation noun**: *should a Python program compiled for a microcontroller have the same five
  standard streams as one compiled for Linux, at 4 KiB of SRAM each?* Four options, recommendation
  (c)+(d), the only pair where no program stops working.
- **ITS FOURTH-SHAPE OBSERVATION, and the second half is the insight:** four relays today carrying
  something true-about-the-NEIGHBOURHOOD and read as authority about the QUESTION — `status: working`,
  "finished/idle", a passing control, a secondhand grant. **"The first three cost measurements; this
  one would have cost an irreversible act — which is the argument for the three-item list being a LIST
  rather than a judgement call."** The clearest defence of that rule I have read, and it explains why
  reversibility is the wrong test at exactly that boundary. On the owner's candidate list; banked in
  the playbook meanwhile.

## ~16:30 — 8e landed consumer 2 green; and BLOCKER 07 IS NOT IN THIS REPO.

**`8dba4c72e` + `0af9f4b08`, verified on origin.** Full NilPy tier GREEN read from the job (1041 `ok:`
rows, zero diff hunks, zero `make: ***`; the five error-shaped lines checked IN CONTEXT — two negative
tests asserting their own refusal, three Makefile comments quoting positive controls). `gate: GREEN
(exit 0)` read out of the log, **with the wrapper's agreement reported as a coincidence rather than as
corroboration.** All 23 rows including both FPC seed canary rows — load-bearing here, since this commit
adds the FIFTH parser→`ir.inc` forward and that canary is the only instrument that sees one go missing.

**THE STEP THAT WOULD HAVE BEEN DROPPED, AND IT WAS NOT:** the pull brought other seats' `compiler/**`,
`make` printed **`converged after 1 round(s)`** — the recompute verb — and the binary moved
`c72af31f3a6e` → `dc03cd5e78e3`. **Its tier green was measured on a binary that no longer exists**, so
it re-ran the fixture against the new one before gating. Matches.

**Ticket NOT closed, two of three consumers**, back in `backlog-nilpy`, owner cleared, p85, BOARD
regenerated. Summary **leads with what remains**, states consumer 3 as a different CONTRACT (boxing
into Variants allocates; an ISR that allocates is a latent crash with good latency numbers), and says
in the summary field that **`nilpy-hw-c3` still polls and its header is still accurate** — the sentence
this ticket was most likely to overstate on someone's behalf, mine included. **Remaining items written
as MECHANISMS**: a capturing def cannot match any slot arity *by construction*, because lambda-lifting
appends captures as extra parameters. **Residual disclosed, not tidied:** if `PyDefFitsCallbackThunk`
accepts and `PyGetOrMakeCallbackThunk` returns −1 the store arm falls through silently — the original
defect by another door, one `else`, in the commit and the ticket, not worth voiding a finished tier.

- **BLOCKER 07 IS NOT IN THE PXX REPO AND I CHECKED BEFORE 8e WENT LOOKING.** `devdocs/pxx-blockers/`
  is **zero paths** on `origin/master`, there is **no pxx ticket** for a nested import guard, and
  `5a7fc43` is **not in this object store**. It lives in **lekkerzeilen** (`/home/neo/lekkerzeilen`).
  Pulling pxx and reading the reduction — 8e's stated plan — would have found nothing and looked like a
  bad slug. **The gap: a pxx COMPILER bug filed only in an application's repo is invisible to `ready`,
  to the ranker and to Track T.** 8e files a pxx-side ticket when it takes it, with the reduction
  restated here; a citation into another repo's tree is not resolvable by a pxx seat.
- **AND THE DEPENDENCY MAY BE THE REVERSE OF WHAT EVERYONE HAS SAID, ME INCLUDED. 07 LOOKS LIKE IT
  UNBLOCKS LEKKERZEILEN ON ITS OWN, WITH NO OWNER DECISION NEEDED.** The nested block is correct on all
  three compilers once nesting works: **today's pxx** `__pxx__` missing → inner `ctypes` missing →
  `_pxx`; **CPython** → `_ctypes_backend`; **future pxx** → first arm. **So the marker is a
  simplification and 07 is the unblocker.** The sentence in circulation all day —*"until `__pxx__`
  ships or nested guards work"*— always had both halves; nobody noticed the second needs nobody's
  permission.
  **LABELLED: I have NOT measured this.** It is reasoning from frankuser's relay of 7a's measurement.
  **7a holds the reduction and should confirm or kill it before anyone acts**, and I said so in both
  directions rather than letting it travel as a finding.
- **07 is 8e's, nobody else near it.** Marker ticket still unowned, p80, `owner: ""` — available as one
  group, though on the reading above 07 finishes without touching the name.

## ~17:00 — 7a CONFIRMED the 07 trace by measurement, and found a hazard that defeats the spelling.

**Recorded in the pxx-side ticket at `00198003d`. Relayed secondhand via frankuser; the source is
lekkerzeilen `315e6ce`, NOT verifiable from a pxx checkout, and is labelled that way in the ticket.**

**CONFIRMED, and measured the right way: 07's trigger is module imports in the arms, so 7a ran the same
nested guard with PLAIN ASSIGNMENTS in the arms** — CPython with no marker takes the ctypes arm,
today's pxx with no marker takes the pxx arm, both correct. **That is varying the ROUTE while holding
the SUBJECT fixed**, isolating nesting from the import machinery that fails, which is why it transfers.
**So `__pxx__` is a SIMPLIFICATION and blocker 07 is the unblocker** — the reversal I flagged as
unmeasured is now measured, and it was 7a's minute to spend, not mine.

**THE HAZARD, AND IT IS BIGGER THAN THE NAME QUESTION: with a real `__pxx__.py` on the path, CPYTHON
TOOK THE MARKER ARM and selected the native pxx backend. Wrong, and silent.**

**It QUALIFIES the owner's decision rather than contradicting it.** The fork he answered was *should a
third party be able to make the check come out TRUE on CPython by publishing a package of that name* —
no. **The dunder spelling defeats a PUBLISHED PACKAGE (`__pxx__` is unclaimable on PyPI) and does
nothing about a LOCAL FILE.** So **the property he asked for is delivered by the IMPLEMENTATION, not by
the string** — and every Name argument in that ticket, including the one I put to him, reasons from
PyPI claimability, **which is the narrower threat.** The fork was answered correctly and undersold what
was needed.

- **CONSTRAINT now in the ticket: the marker must be something the COMPILER provides that CPython can
  never import.** Nothing pxx does can stop CPython importing a file that exists, so the protection has
  to come from the marker **not being file-shaped**.
- **POSITIVE CONTROL, drawn from the population the hazard is in: a real file of that name must be PUT
  ON THE PATH and must not satisfy the check.** A fixture that only tests the absent-file case passes
  whether or not the hazard exists.
- **One row labelled UNTESTED rather than folded in:** "a future pxx that ships the marker takes the
  first arm" was exercised only by putting a real file on the path, **which is not how the marker will
  work.** pxx did take the first arm as predicted; that is not the same claim.
- **Sent upward as INFORMATION with "nothing for you to decide" stated**, because he does not need to
  act and the spelling question is unchanged. **The reason to tell him anyway: if he later reads that
  the marker is safe because the name is unclaimable, that sentence is wrong and it is wrong in my
  words.**
- **For 8e: the reduction is `devdocs/pxx-blockers/07-.../` at `5a7fc43` in `/home/neo/lekkerzeilen`,
  pushed to github. 7a offers it copied into the pxx tree as 8e's own fixture** — the second home we
  both wanted, and it makes the pxx ticket cite something a pxx seat can run. **07 alone is a clean
  self-contained group and needs none of the marker constraint.**

## ~17:30 — the arena is dead by REACHABILITY, bare NilPy is unbuyable, and the guard is Python's `#ifdef`.

**franks-5b: `ac9abdd3a`, `bcb1ec1e8`, `114193218`, all verified on origin.**

**IT CHANGED THE QUESTION WHERE I HAD ONLY CHANGED THE READOUT.** My pattern-fill idea was still a
SURVIVAL question with no positive control on the only profile that compiles — I named that gap and
offered the probe anyway. **Reachability has an off-switch: `HeapMmap` is the arena-refill call and the
only route to a `HeapPtr` read, declared ABOVE the `PXX_ESP_IDF` split, so it is compiled on every
profile and dead-strippable — present under `--no-dce`, dropped under `--dce`. Both arms move, so the
instrument CAN fail.** The arena is 65,536 of that object's 89,352 B of bss (73.3%) and DCE cannot
touch it because it is STORAGE, not code. **General form worth carrying: when "does it survive" has no
positive control, ask whether the code that would USE it is reachable.**

**AND `calloc` IS NOT A DISCRIMINATOR — I HAD WRITTEN THAT IT WAS.** The native arm's own
`PXX_LIBC_HEAP` variant calls `calloc` too, so the six-externals row is consistent with EITHER arm.
franks-5b nearly used it and caught itself; **I had already put it in this log as "independent
corroboration"**, where it would have read as a second source to whoever found it. Corrected at the
~16:00 entry.

- **OUR SHARED EXPECTATION WAS HALF WRONG AND IT WAS STATED FIRST.** "One cause, and it buys the control
  AND the profile": one cause **yes**, the profile **no**. **The cause: `builtinheap.pas:18` makes
  `PXX_ESP_BARE` imply `PXX_ESP`, so bare inherits every `{$ifndef PXX_ESP}` region — and those regions
  are contiguous spans of SOURCE, not capability sets**, each pairing a small genuine core (a
  filesystem open; syscall stdio) with ~25 pure pointer routines that have no bare dependency at all.
  All three reported names are lumped ones. **The unit's own header already states the principle it
  breaks** — *"none of these bodies is unimplementable on an ESP chip"* — so this is comment-versus-code
  resolved in favour of the comment, with evidence.
- **BARE NILPY IS UNBUYABLE AND THAT RETIRES A DIRECTION.** Markers neutralised, the program compiles
  completely and stops at `CheckBareImageFitsSram` over by **1,967,792 B**; a six-line while/append
  program is over by **1,958,988** — within 8,804 bytes of the full demo, so **~99.6% is fixed NilPy
  runtime**, with DCE on by default and already removing 856,584 B. **Not a DCE problem.** I asked it to
  write in what would RETIRE that claim, since it reads permanent and is really a statement about a
  runtime that must live in SRAM — flash execution, or a different size of runtime, changes it.
- **RETRACTION, banked inline by it:** an A/B of no-flag against `--dce` gave byte-identical fit-check
  addresses **because the flag was already on**, and it was one paragraph from writing up *"a pass
  reports a drop the image does not show"* — a finding-shaped sentence about nothing. **`--no-dce`, the
  off-switch, was the control that existed the whole time.**
- **GUARD SPLIT filed separately at p40 and deliberately NOT blocked on bare NilPy** — bare PASCAL works
  and fits today, and is refused managed records and variants **purely by position in a file**. **Its
  retirement test has to be a NEW fixture** (a bare Pascal record with an AnsiString field going out of
  scope) **because `test-esp-bare`'s fourteen rows all avoid managed members — a suite that avoids the
  shape certifies its absence**, the same family as this morning's hole where nothing anywhere compiles
  a `.npy` for bare. Taking it next; not redirected — it is the one piece that delivers to a profile
  people can run.

## ~17:45 — the owner: the import guard is Python's only `#ifdef`, and that reframes 07.

*"plus, we only do so due to lack of `#define` and python's design. so, this is by design."* (relayed
secondhand). **The guard is not a workaround — it is the ONLY conditional-compilation mechanism the
language has**, so `__pxx__` is pxx's `#ifdef PXX` spelled the one way Python allows, and NilPy
resolving it at COMPILE time honours that intent rather than bending it. In the ticket in his words.
- **The ergonomics objection is CLOSED.** Called "the ctypes import hack" all week, by him included.
  **Anyone calling it ugly is measuring it against a preprocessor Python does not have.** No cleverer
  mechanism is wanted; no ticket may be ranked on replacing it.
- **07 IS A CORRECTNESS BUG IN A LANGUAGE FEATURE, not an edge case:** a nested guard is nested
  conditional compilation, and compiling the dead arm is **compiling code the program said not to
  compile.** Relayed to 8e for its ticket, with one addition of mine — **the fixture wants a THREE-deep
  guard, not just the reduction's two**, or it certifies a fix that only handles two.
- **The stray-file constraint is WITHDRAWN** (`d9254e45b`): the owner ruled it out, and the standing
  rule — an input produced only by a deliberate act is `rejected/` territory — agrees independently, so
  **the edit stands on the rule and not on the relay.** 7a's measurement stays as a true, reproducible
  NON-defect; the untested third row stays labelled untested.

## ~18:15 — 07 fixed, and the loud error was the artefact. The class was SILENTLY WRONG.

**frankb-8e. Nothing on origin yet — tier running, nothing committed, correctly. No shas to verify.**

**ROOT CAUSE, at the layer that owns it and not the one that reported it:** `PyPreScanImports` — the
token walk resolving every import ahead of the parse — tracked ONE try in scalars, and **`tryAt` holds
a DEPTH, not a token index.** A guard nested in any other try left `tryAt` at the outer try's depth, so
at every clause boundary of the inner guard `depth <= tryAt` was false, the arm-deadness logic never
ran, and the walk resolved the excluded arm like any other import.
`vL` top level: `DEDENT to depth 0 tryAt=0 -> clause logic 1`. `vK` inside a try:
`DEDENT to depth 1 tryAt=0 -> clause logic 0`.

**THE PARSER WAS CLEARED BY A NEGATIVE, AND THAT IS THE CHEAP HALF OF THE METHOD:** it tagged
`PyIsFallbackImportTry` first and got **nothing printed — not even for the outer try.** **A predicate
that is never called cannot be the layer at fault**, and that silence named the pre-scan in one run.

**THE FINDING IS NOT THE FIX — THE OBSERVABLE IS NORMALLY SILENT.** The reduction's loud error is an
**artefact of its dead arm importing `ctypes`**, a module we do not have. **Where the dead arm is
ordinary Python it resolves quietly and the first-wins alias answers member reads through the live
arm's name.** On pin v412, exit 0, no diagnostic:

```
correct  ('deep-else', 'deep-hit',  'sym-deep-hit',  2)
pinned   ('deep-else', 'deep-miss', 'sym-mid-else', 2)
```

`WHICH` is RIGHT — the parser picked the correct arm — and **both member reads are wrong, by two
DIFFERENT dead arms**: the alias door answers `deep_miss`, the symbol door answers `mid_else` through
flat unit scope. **So this class has been producing plausible wrong values in any program with a
guarded import inside a `try`, and it surfaced only because one application's dead arm happened to name
a module we lack.** Relayed to 7a and c6 to check whether any of their guarded imports sit inside
another `try`, and to treat earlier results as unverified rather than wrong if so.

**AND IT IS WIDER THAN FILED: the trigger is ANY ENCLOSING `try`** — `except ValueError:` breaks it
identically — not the fallback handler and not nesting as such; `if`/`def`/`for` are fine. Twelve
variants, one axis at a time. **The reported shape was one member of a much larger population.**

- **THE THREE-DEEP POINT, MADE CONCRETE BY THE FIX ITSELF:** one DEDENT can close an inner guard's last
  block **AND** the handler of the try it sits in, so the dedent handler had to become a **LOOP**. An
  `if` handles the innermost and **silently drops every level above** — a fix that passes a two-deep
  fixture and leaves three-deep broken. **8e says plainly it would have written the `if`.** The fixture
  is three deep and the pin refuses it.
- **"I HAD REMOVED THE SUBJECT, NOT VARIED IT" — banked in its words, and it is the sharpest sentence
  of the day.** Its "guard inside a handler with no `else`" row PASSED, and reading that as *"the
  `else` is the trigger"* would have filed the wrong axis — **but removing the `else` removes the dead
  arm entirely, so there is nothing left to compile wrongly.** A row that DELETES the thing under test
  passes for a reason unrelated to the hypothesis and **reads exactly like a clean negative.** That is
  the fixture-design family arriving one level up, at the level of choosing which variant counts as a
  control. The discriminating row puts the dead arm in the HANDLER and lets the try body resolve.
- **NO ROW MAY GUARD ON `ctypes` IN A DIFFERENTIAL FIXTURE, and `nilpy_tryelse`'s own header already
  said so:** CPython resolves it and pxx does not, so **oracle and subject run DIFFERENT arms and the
  comparison can never fail.** 7a's reduction uses `ctypes` correctly — it is about a real backend —
  **and that is exactly what stops it being a test.** Relayed to 7a, with the credit that belongs with
  it: the real `ctypes` dependency is what turned a silent wrong answer into a loud error somebody
  could report.
- **The trace is confirmed from our side: today's pxx takes `_native`, CPython takes `_ct`, both
  correct, NO NAME DECISION NEEDED.** Third row written as untestable rather than claimed. **07 was the
  unblocker; the marker is the simplification.** 7a's replacement block can land when 8e's fix does.

## ~18:35 — CORRECTION TO MY OWN RELAY: the dead arm RAN, and my quantifier was invented.

**8e stopped my relay one hop out. Two changes, one worse and one narrower.**

**WORSE, AND MEASURED: the excluded module's top-level code did not merely get COMPILED — IT RAN.** A
module-level `print` in the dead arm executes on the pinned compiler, in a program where CPython never
imports that module at all:

```
CPython:  impl live
PINNED:   SIDE EFFECT from the DEAD arm ran
          impl dead
HEAD:     impl live
```

**That half needs ONLY the nesting** — no shared name, no `else:`. **Compiling an excluded arm is a
correctness bug; RUNNING one is a different category**, because the program can do something
irreversible before anyone reads a wrong value. That is the headline, above the binding half.

**MY ERROR, and it is the named shape: THE CLAUSE TO GO MEASURE IS THE QUANTIFIER, NOT THE VERB BESIDE
IT.** I relayed *"silently computing wrong answers in ANY program with a guarded import inside a
`try`"*. **The side-effect half is that broad; the WRONG-VALUE half is not** — it needs both arms to
bind the same name AND the live arm to sit lexically AFTER the dead one. **The alias table is
first-wins, so in the ordinary no-`else:` idiom the live arm is lexically first, wins by position, and
the VALUE IS CORRECT — only the side effect leaks.** I wrote "any" while relaying 8e's careful
measurement, **which did not say it**; the measured half lent its credibility to the invented half.

**The accurate version, sent to frankuser for 7a and c6 as two sentences with the precondition
attached:**
1. **Any nested guard:** the excluded module was compiled and its top-level code RAN. **Audit for side
   effects, not for wrong values.**
2. **Nested guard whose live arm is an `else:` or deeper:** additionally every member read through the
   shared name came off the dead module, exit 0, no diagnostic. **lekkerzeilen's shape is this one.**

**8e's reason for preferring the narrower claim is the whole argument and it is not caution: a seat
that audits, finds nothing wrong, and concludes the report was overblown LEAVES THE SIDE-EFFECT HALF
UNEXAMINED.** The alarming version is worse than the precise one *because* it is alarming.

- **AND ITS SELF-CORRECTION IS THE BETTER OF THE TWO METHODOLOGY FINDINGS, marked as the one that cost
  an hour.** The no-`else` row sat in its table as **`ok`** — and **`ok` there meant "compiled and
  printed 7"**. It was clean **for the question the table asked**, and it read the column heading as
  though it answered a question it never asked. **Every axis varied correctly, the table honest, and
  the instrument simply did not contain the quantity that mattered.** That is *match the assertion
  class to the defect class* arriving as a **COLUMN HEADING** rather than as an assertion — and nothing
  about a green column announces which question it answers. Repair: the table is labelled in the ticket
  as answering the refusal question ONLY, **because the next reader inherits the heading, not the
  author's memory of what it meant.**

## ~19:00 — managed records RUN on bare metal; and I endorsed a retirement test that was wrong.

**franks-5b, `085f9f899` + `b336d1e52`, verified on origin.** Four spans in `builtinheap.pas` are no
longer profile-gated (Initialize/Finalize, the `PXXClassFinalize` arm, Retain/Release +
`DynArrayReleaseDepth`, and the span after). **What stays gated is a CAPABILITY, not a POSITION** — six
`PXXIntf*`, five `PXXVar*` and one `PXXPromoRetainOne` arm INSIDE those walks, each a kind=4/5/7 case
that cannot occur where the surface itself is excluded. Variants+float share a span and stay positional
(float needs the softfloat pull bare deliberately skips — not the same change); ticket stays open.

**I ENDORSED ITS RETIREMENT TEST AND IT WAS WRONG.** *"A bare Pascal record with an AnsiString field
going out of scope"* — I quoted that back approvingly as the operative point. **It compiles fine: a
simple GLOBAL record never reaches the walk.** The shapes that do are a record **LOCAL to a
procedure**, a whole-record **ASSIGNMENT**, and **`Finalize`**. **Taken as the spec it would have
shipped a fixture GREEN ON THE BROKEN COMPILER — the `ucycle_b` shape.** franks-5b ran it before
trusting it, on its own prescription, which is the harder direction; **I was the second pair of eyes
and added credibility instead of checking.**

- **THE CONTAINMENT CONTROL IS EXEMPLARY AND SHOULD BE THE TEMPLATE:** this is RTL every target links,
  so the change must be a no-op off-ESP — **byte-identical x86-64 binary before and after** (nested
  records, dynarray of strings, variant, whole-record assignment, `Finalize`; still runs) — **AND it
  demonstrated the edit is not vacuous: the same change moved three ESP fixtures from `compiler error`
  to `ok`.** *No change where none should occur, real change where it should.* **Identity alone would
  have been a guard that cannot fail**, and it said so in those words unprompted.
- **The fixture asserts VALUES, not compilation** — `test_esp_bare_managed.pas` boots on esp32c3 and
  esp32s3 and matches its oracle byte-for-byte. **A compile-only row cannot tell a working walk from
  one that compiles and then corrupts a refcount**, which is the only failure mode that matters for
  routines whose whole job is refcount bookkeeping.
- **THIRD INSTRUMENT FAILURE OF THE DAY, inside a scanner it wrote: a file that documents its own
  syntax defeats the parser you write for it.** `builtinheap.pas` comments QUOTE directives because
  they explain the guards. An awk pattern escaping `$` matched **nothing** in a span containing two —
  mawk does not take that escape and **fails by ANSWERING**, same family as its missing `strtonum`
  this morning. Then a correct `^`-anchored Python regex applied to `line.strip()`: **the strip
  promotes an INDENTED quotation inside a comment to a column-0 directive**, so the depth counter
  incremented on prose and never found the real `endif`. ***"The strip looked like robustness.
  Whitespace was carrying the entire signal."***
- **Did NOT lift `PXX_ALLOW_FULL_SUITE`** — new rows are self-contained, ran them verbatim. The escape
  is for when you need it, not for when a change feels shared.
- **MY ANSWER ON THE VARIANT SPAN — do not take it as it stands, on its own finding.** It has just
  proved compile-only cannot distinguish a working walk from a corrupting one, and the variant span is
  **bigger, float-entangled, and has NO runtime oracle on bare** the way records now do. Taking it
  would ship the larger blast radius with the weaker instrument one step after proving why that fails.
  **Build the bare variant oracle first, or stop — and this is a clean place to stop.**

## ~19:05 — 7a audited rather than accepted, and the pin became a HARD GATE.

**7a checked the corrected reasoning instead of taking it: nesting is the precondition for BOTH halves,
and its scan found ZERO imports at try-depth 2+ across the package**, so one measurement excludes both.
Blocker set and frame-rate pair stand clean (`68ebdfb`, its repo).
**THE PART THAT OUTLIVES THE BUG, its words: a wrong value shows up in a diff and AN INITIALISATION
THAT RAN DOES NOT** — which is why the side-effect half is the one nobody catches by comparison.
**AND THE PIN ARGUMENT HAS CHANGED CHARACTER.** Every pin case I have given the owner this week was
about STALENESS. **7a's replacement block is a nested guard, two arms binding the same name, with an
`else:` — exactly the shape that hits both halves** — so landing it against a compiler without 8e's fix
**could run the dead arm's module-level code and bind the wrong backend silently.** 7a has written that
into its blocker file so a later session cannot land it innocently. **Sequence: 8e's fix lands → a pin
carries it → 7a lands and runs the demo the same hour.** The middle step is the only one nobody here
can do. Relayed upward as one line; the flapping-row caveat still stands.

## ~19:30 — the all-clear is only as good as what the scan ENUMERATED. And I fixed the marker summary again.

**8e interrogated a CLEAN SCAN — the result nobody interrogates — and then measured its own question
rather than asserting it from the depth logic.** The query that produces a correct all-clear is
**lexical containment, not adjacency and not a number: does any `try` CONTAIN another `try` that guards
an import, at any distance, through any intervening construct?**

```python
try:
    x = 1
    if x == 1:              # an ordinary block BETWEEN the two tries
        try:
            import math
        except ImportError:
            from . import dead as impl
        else:
            from . import live as impl
except ValueError:
    pass
```
**Both halves fire on the pin — dead arm RAN, wrong module bound — with only an `if` in between**,
because the pre-scan compares INDENT depth and a non-try block pushes the guard deeper just as
effectively as a try does. **A scan for `try:` directly inside `try:` answers ZERO on that block.**

- **c6's TSP scan answers it correctly and is the template: an AST pass counting enclosing `try` NODES
  per import** (ancestor counting, so intervening constructs do not matter) — **509 at depth 0, 7 at
  depth 1, ZERO at 2+** over `tsp/` and `tests/`. **And c6 made it a STANDING TEST**
  (`tests/test_pxx_shape.py`, `4fccfbe`): reads sources with `ast` so it imports nothing, covers the
  SDL2/GL and dataset modules, asserts the seam's shape too, **and was verified to FAIL on planted
  counter-examples rather than passing vacuously.** Both failure modes are silent under CPython, so
  nothing else would catch a regression — **a note in a README cannot, a test can.** Asked c6 to run
  8e's block through it; 7a asked which criterion its own scan used, with 8e's block as the re-run's
  case if it was numeric.
- **8e's THREE self-caught errors tonight, all found by RUNNING rather than reading:** the `ok` column
  that meant "compiled and printed 7"; the no-`else` row that **removed the subject instead of varying
  it**; and now **a first counterexample that put the `def` AFTER the outer try — not nested at all,
  clean everywhere, and it would have read as an all-clear for a shape it had never tested.** *A
  malformed probe returning the comfortable answer is the hardest kind to catch*, because nothing looks
  wrong and the result agrees with the hypothesis you hoped to retire.
- **A `def` inside a `try:` body is refused on BOTH pin and HEAD**, byte-identical `unresolved forward:
  pick` — **pre-existing, unrelated, NOT tonight's change.** 8e flagged it so "REFUSED at HEAD" would
  not be misread as a regression. **I told it to FILE that itself** after its tier: it holds the one
  thing a later investigator cannot cheaply reproduce — the identical message on both binaries — and
  **an unfiled refusal mentioned in a message is exactly what gets misattributed six weeks from now,
  when the message is gone.**
- **I FIXED THE MARKER TICKET'S SUMMARY AGAIN (`2eb56685a`), AND THIS ONE WAS ALSO MINE.** It said the
  marker *"UNBLOCKS 10 of TSP's 20 remaining failures"*, unqualified, in the field that carries prio
  into the ranker. **c6 had corrected me on this TWICE, in messages, and neither correction was in the
  ticket.** It now says it MOVES them — from failing at `ctypes` to failing at `_pxx_backend not
  found` — names the parked ~1k-line port as the real gate and the owner's to unpark, and records that
  **lekkerzeilen's unblocker was the NESTED-GUARD bug, not the marker.** *Agreeing in a reply is not
  the same as fixing the record*, and that is the second time today the gap was exactly that.
- **Routed c6's warning to frankh-c0 (parked, flagged no-action/no-reply): its compile-time-fold proof
  uses a dead arm containing `ctypes`** — **right for a PROOF**, and a guard that cannot fail if lifted
  into a DIFFERENTIAL harness, since oracle and subject would run different arms. **The same property
  is why blocker 07 was loud in lekkerzeilen and silent everywhere else.**

## ~19:50 — the caution went into the artefact, as a CONTRAST rather than a warning.

**frankh-c0, `ec98433e7`, verified on origin — in the marker ticket directly under the measurement it
qualifies, not in a message thread.** For the proof it was making — *the dead arm is not compiled* —
**untypable `ctypes` is arguably the SHARPEST available filling**, because code the compiler cannot
type either errors or proves it was never reached. **In a DIFFERENTIAL probe it is the exact
opposite**: CPython resolves it, pxx does not, so the two arms are DIFFERENT CODE and the comparison
cannot fail whatever is wrong. **Same construction, one use sound and one certifying nothing.**

**BANKED, and the spelling is new to this tree: it is "a guard that cannot fail prints PASS" WITH THE
HAZARD IN THE PROBE'S FILLING RATHER THAN IN ITS ASSERTION.** Every existing statement of that rule
guards what the assertion can observe; this one is about what the probe is made of. One instance —
banked, not proposed — and on the owner's candidate list. **c0's reason for committing rather than
acknowledging is the point: it will not be rediscovered from a message thread.**

**And it named which half of 8e's finding changed its mind: the ordinary case is SILENT, so its probe
was loud because of what it happened to FILL it with, not because the defect is loud — "and I would
have read that loudness as a property of the mechanism."** In the ticket that way.

**THE CHAIN IS THE THING TO NOTICE: 8e warned about differential reuse, c6 raised it about SOMEBODY
ELSE'S artefact, c0 wrote it beside its own probe.** Nobody defended their own work, and the fix landed
in the place a later reader will actually be standing.

- **c0 is parked and staying parked, everything pushed and written down**: blocker 04 with its fix
  specified, 06 at p75 cross-linked as a hazard to the closure ticket it came from, the perf hypothesis
  refuted in its own ticket, the read-only REMAINING list marked unowned with the ESP reachability
  constraint in the body. **Nothing is waiting on it.**
- **Fleet state: 8e finishing a tier then landing 07 and filing the `def`-in-`try` gap; franks-5b at a
  clean stop after the guard split, asked not to take the variant span without a runtime oracle;
  c0 parked. Two items with the owner: the pin line, and whether `__pxx__` is his string.**

## ~20:10 — both all-clears now rest on the STRUCTURAL predicate, and both apps have a self-verifying test.

**7a's scan was the structural reading — no re-run needed, all-clear stands.** An `ast.NodeVisitor`
incrementing on `visit_Try`, decrementing after `generic_visit`, recording the counter at every
`Import`/`ImportFrom`: **enclosing try NODES, blind to what sits between**, so no intervening
`if`/`def`/`for`/`class`/`with` can hide the nesting. **8e's block is a test row there now and reads
depth 2 for all three imports.** 8e's question closed a real gap in the OTHER application's method and
cost one exchange.

- **7a states its one weak spot rather than letting it stand, and disposes of it correctly:**
  `platform/_pxx.py` cannot be parsed by CPython's `ast`, so it got a **textual** scan — the weaker
  reading — **airtight only because the file contains no `try:` at all**, and **that is now ASSERTED
  rather than remembered.** Turning *"airtight because of something I checked once"* into a guard is
  the step almost everyone skips.
- **Both applications now hold a SELF-VERIFYING shape test** (c6 `4fccfbe`, 7a `fdee787`): reads
  sources with `ast` so it imports nothing, asserts no import at try-depth ≥ 2 and the seam's shape —
  and **on every run copies the package to a temp dir, plants five defects and requires the check to
  FAIL** (nested guard with an `if` between; nested guard behind a `def`; seam flattened; an arm
  forgetting `_backend_name`; the unparseable file gaining a try). 11 tests, 2.6 s.
  **THE CLAUSE THAT MATTERS IS "EACH CAUGHT BY THE RIGHT CHECK": a planted-defect harness where ANY
  assertion firing counts as a pass can hide that one specific check is dead.** That is the
  positive-control rule applied per-CHECK rather than per-harness, and it is why five are planted
  rather than one.
- **7a's thanks to 8e, with the sentence it asked be kept:** *8e's self-correction about putting the
  `def` AFTER the outer try is exactly the trap that harness exists to catch — 7a's own first planted
  case could have been wrong the same way and it would not have known.* **A planted defect that is not
  actually the defect passes the harness and certifies it** — 8e's malformed-probe finding arriving
  inside the tool built to prevent the thing 8e found.
- **Both failure modes are silent under CPython, so nothing else would have caught a regression here.**
  A README note cannot; these can. Nothing in this thread needs the owner.

## ~20:30 — caught a duplicate before it landed, and the `def` gap is half of Python's only `#ifdef`.

**8e filed `bug-n-a-def-inside-any-compound-statement-is-never-registered` locally (p60) — NOT pushed,
tier still running. THE CAUSE IS ALREADY ON ORIGIN**:
`bug-n-a-def-inside-a-taken-branch-does-not-rebind-the-name`, track N, **p45**, unowned, whose summary
already names the mechanism — ***"`PyRegisterDefShells` only walks module-level defs at DEPTH 0, so a
def inside a branch never gets one"*** — and records that it was split out of a module-rebinding
ticket when that one was fixed. **Same defect, independently measured, and it confirms 8e's correction
to itself: NOT try-specific, a depth-0 walk.** Told it to fold its evidence in rather than land a
second slug: **two slugs for one cause is how a backlog reaches 5035.**

**WHAT 8e HAS THAT THE EXISTING TICKET CANNOT SAY, and it is the more valuable half.** That ticket's
example is `if True:` with a synthetic rebinding — reads as an edge case, priced p45. **8e's is the
standard pure-Python fallback, and it is the direct companion to the idiom the owner ruled BY DESIGN
three hours ago:**

```python
try:
    from somewhere import isqrt
except ImportError:
    def isqrt(n): ...        # the ordinary fallback
```

CPython prints 4; pxx says `unresolved forward: isqrt`. **So pxx supports the conditional IMPORT and
not the conditional DEFINITION — half of Python's only `#ifdef`.**

**AND THE ASYMMETRY IS WHAT SHOULD SET THE PRIO: it passes exactly when the fallback is UNUSED and
fails exactly when it is TAKEN.** Guard on `math` — a module we HAVE — and it compiles, because the
guard resolves, the handler is dead, and the `def` in it is skipped without ever being registered.
**Under pxx the failing case is the COMMON one, since the reason to write a fallback is a module we may
lack.** That is also **why no fixture ever caught it: an author reaching for this idiom naturally
guards on something present.**
One row pinned as **NOT a divergence**: `def` inside `except ValueError:` fails under CPython too
(`NameError`, handler never runs) — pinned deliberately so nobody "fixes" it into printing 7.

- **Grep relayed to 7a and c6: `except ImportError:` with a `def` in the handler. Their new shape tests
  will NOT catch it** — different defect from the one those assert, and both packages are exactly the
  kind of code that writes it.
- **8e's sharpening of the harness clause, banked ITS way: "each caught by the RIGHT check" outranks
  the count of five.** A planted-defect harness that passes when ANY assertion fires **cannot detect a
  DEAD check — the very failure it exists to prevent — so it certifies itself. Five plants with loose
  attribution is weaker than two with strict attribution.**
- **8e asked that 7a's version of the malformed-probe finding circulate instead of its own, and it is
  right:** its bad probe cost minutes in a scratch file; **a plant that is not actually the defect gets
  the harness a green and is then TRUSTED FOREVER** — the same error promoted into permanent
  infrastructure, where nothing downstream can notice.
