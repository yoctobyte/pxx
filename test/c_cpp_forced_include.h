/* Included via `-include`, never by name from the .c -- see
   test/c_cpp_macro_arg_shapes.c. If a change ever makes the test source
   include this itself, the -include row stops testing anything. */
#define FORCED_BY_MINUS_INCLUDE 41
