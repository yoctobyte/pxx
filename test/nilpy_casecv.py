# The MODULE half of the reportlab shape: a module whose name differs from an
# exported class's name only in case (there, `canvas` the module beside `Canvas`
# the class). Imported by test_nilpy_import_case_folds_onto_a_class.npy.
WIDTH = 612


def make():
    return "page"
