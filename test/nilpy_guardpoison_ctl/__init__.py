# THE CONTROL, and it differs from nilpy_guardpoison in the guarded import and
# in nothing else: same constants, same layout, read by the same spelling in the
# same run. Without it, a row asserting 27 is satisfied by a compiler that never
# looked at either package.
KEY_ESCAPE = 27
QUIT = "quit"
