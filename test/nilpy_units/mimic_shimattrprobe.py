# Reached as the shim `shimattrprobe`, never by this literal filename — the
# mimic_ mapping is what registers the unit alias the bug rode in on.
SOMEGLOBAL = 42

def somefunc():
    return 7

class Node:
    ELEMENT_NODE = 1
    TEXT_NODE = 3
    DOCUMENT_NODE = 9
