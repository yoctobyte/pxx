# Test files deliberately not wired into any build rule, one per line as
#   <path>  <reason>
# A reason may not start with `#` — everything from a `#` is stripped as a
# comment, so `foo.c  #include <x.h> ...` reads as a reason-less line and is
# REFUSED (loudly, which is the right call — it just costs a rewrite).
# An entry with no reason is REFUSED by tools/check_test_wiring.py — an
# unexplained exemption is the invisible-work problem the checker exists to
# remove, one level down.
#
# Empty on purpose right now: the 98 currently-unwired files are NOT exemptions,
# they are the backlog this checker was written to surface. Exempt a file only
# when "nothing runs it" is the correct end state and you can say why.

# --- consumed by a WIRED test, so "nothing runs it directly" is correct ---
# check_test_wiring already reaches helpers through a wired subject's `uses` /
# `#include`, but it matches by STEM and test_relpath_uses.pas writes the
# path form (`uses './relpath/relstr'`), which that match cannot see. Listed
# here rather than patched into the checker: tools/check_test_wiring.py is
# Track T's file and T owns the tool.
test/relpath/relext.pas  helper unit for test/test_relpath_uses.pas (wired); named there in path form, which the checker's stem match misses
test/relpath/relstr.pas  helper unit for test/test_relpath_uses.pas (wired); also used by relpath/sub/relmath.pas
test/relpath/sub/relmath.pas  helper unit for test/test_relpath_uses.pas (wired); exercises the `uses '../relstr'` relative form
test/my_pas_lib.pas  helper library compiled by test/test_basic_comprehensive.bas (wired)
test/test_pal_signal.pas  helper unit for test/test_signal_handler_callback_b336.pas (wired)
test/carray_field_decay_exprlist_b120.c  included by test/carray_field_decay_nested_item_b120.c (wired)
test/cbridge_via_unit.c  pulled in by test/unit_c_bridge.pas via `uses './cbridge_via_unit.c'`
test/cabi_bridge.c  implementation of test/unit_cabi_bridge.pas via `uses './cabi_bridge.c'`; reached by the wired test/test_c_abi_pascal_caller.pas. Same path-form blind spot as test/relpath/* above -- the checker matches by stem. Compiling it standalone would test a shape that never occurs: its whole subject is a Pascal caller reaching a bodied C callee, which needs the Pascal unit around it
test/cabi_intra.c  implementation of test/unit_cabi_intra.pas via `uses './cabi_intra.c'`; reached by the wired test/test_c_abi_intra_c_calls.pas. Same path-form blind spot. Its subject is a C-to-C call INSIDE a translation unit that a Pascal program uses (CProgramMode and CUnitOfPascalProgram both True), a state no standalone compile of this file can enter
# NOTE: check_test_wiring reports the next line as a STALE exemption. It is
# not — tools/testmgr_hardcoded_tmp_devtest.py merely NAMES this path as data
# (it asserts the file's hardcoded /tmp path), and the checker's "referenced by
# a tools/ script" match cannot tell naming from running. Keeping the reason is
# worth one false STALE line; deleting it would lose why the file is unrunnable.
test/csqlite_file_probe.c  needs the gitignored sqlite amalgamation ($(SQLITE_SRC)/sqlite3.c), like the other csqlite_*_probe files; the earlier reason here ("used by csqlite_parity_selfcompiled.c") was wrong — that file only NAMES it in a comment, it does not include it
test/c_def_hijack.c  C unit consumed by test/test_c_def_hijack.pas (wired, test-c) via `uses './c_def_hijack.c' as cx`
test/olf_cmath.c  C unit consumed by test/test_own_lang_first_alias.pas and test/test_own_lang_first_c_first.pas (both wired) via `uses './olf_cmath.c'`
test/kwarg_overload_unit.pas  Pascal unit imported by test/test_nilpy_kwarg_overload_set.npy (wired, test-nilpy) via `import 'kwarg_overload_unit.pas' as ko`
test/qualified_default_unit.pas  Pascal unit imported by test/test_nilpy_qualified_proc_omitted_default.npy (wired, test-nilpy)

# --- wired, but in a form the checker's `test/`-prefixed token match misses ---
test/test_exc_resident_param.pas  RUN by test-opt across -O0/-O1/-O2/-O3 (Makefile `for t in ... test_exc_resident_param`); the loop names the STEM and builds `test/$$t.pas`, so no literal `test/test_exc_resident_param.pas` token exists to be found

# --- deliberately manual: needs a checkout this repo does not vendor ---
test/manual/try_synapse_compile.sh  the hand-run driver for the four synapse_smoke_*.pas above; nothing names it because nothing should — it needs external/synapse, which this repo does not vendor, and it exits 2 with an install hint when the checkout is absent
test/manual/synapse_smoke_blcksock.pas  Synapse compile inventory; run by hand via test/manual/try_synapse_compile.sh, needs a Synapse checkout
test/manual/synapse_smoke_synaip.pas  Synapse compile inventory; run by hand via test/manual/try_synapse_compile.sh, needs a Synapse checkout
test/manual/synapse_smoke_synautil.pas  Synapse compile inventory; run by hand via test/manual/try_synapse_compile.sh, needs a Synapse checkout
test/manual/synapse_smoke_synsock.pas  Synapse compile inventory; run by hand via test/manual/try_synapse_compile.sh, needs a Synapse checkout

# --- external-dependency probes: no header in this repo, so CI cannot build them ---
test/gamelib/cglm_probe.c  game-library ladder probe; needs a cglm checkout (#include <cglm/cglm.h>)
test/gamelib/enet_probe.c  game-library ladder probe; needs an enet checkout
test/gamelib/zengl_probe1.pas  game-library ladder probe; needs a ZenGL checkout

# --- oracle probes and display-bound apps: correct that nothing runs them ---
test/t_rw.pas  GTK3 form app (`{$R TMainForm t_rw.lfm}` + Application.Run): needs a display and blocks in an event loop, so it cannot assert anything in a suite. The LFM streaming it demonstrates IS covered headlessly by test/test_lfm.pas and test/test_wildcard_lfm.pas (both wired, incl. the i386 cross row)
test/manual/test_pylexer.pas  under test/manual/ and deliberately hand-run: it {$include}s compiler/defs.inc + lexer.inc + pylexer.inc under FPC and DUMPS the token stream for eyeballing — a debugging instrument with no pass/fail

# --- need the gitignored sqlite amalgamation ($(SQLITE_SRC)/sqlite3.c) ---
# Same dependency as csqlite_file_probe.c above and as the graceful-SKIP rules
# test-sqlite-parity / test-cjson / test-sqlite-threads. A skipping rule is the
# better end state for these five than an exemption, but it cannot be WRITTEN
# honestly from a checkout that has no amalgamation to run it against, and the
# sweep does not record expectations it did not see produced. See
# chore-a-sweep-the-unwired-tests-into-the-suite.
test/csqlite_suite.c  includes sqlite3.c itself (the amalgamation, gitignored); runs a SQL script through sqlite3_exec and is diffed against the system sqlite3 `.mode list` oracle
test/csqlite_extended_test.c  includes sqlite3.h plus the crtl unity sources; needs the gitignored amalgamation to link
test/csqlite_layout_gcc.c  gcc-side half of a struct-layout differential over sqlite3.h; needs the gitignored amalgamation, and its oracle is a gcc build rather than a recorded expectation
test/csqlite_layout_probe.c  pxx-side half of that same struct-layout differential; needs the gitignored amalgamation
test/csqlite_schema_exec_probe.c  schema/exec bringup probe over the crtl unity build; needs the gitignored amalgamation

# --- a Lazarus PROJECT's unit, not a test subject ---
# test/gui/helloworld/ is the stock Lazarus "helloworld" project kept verbatim
# as a reference fixture (helloworld.lpr/.lpi/.res/.ico + main.pas/.lfm). main.pas
# is a `unit`, not a program: there is nothing for a suite to run, and the .lpr
# that would consume it is FPC/Lazarus input, not a pxx test.
# test/gui/test_pcl_helloworld.pas is the pxx-side MIRROR of this project and IS
# wired (tools/gui_suite.sh) -- it is what asserts the behaviour; this tree is
# what it is checked against by eye.
# chore-t-six-orphan-gui-tests-the-blanket-was-hiding
test/gui/helloworld/main.pas  unit of the stock Lazarus helloworld reference project (helloworld.lpr), kept as a fixture to mirror; the pxx equivalent test/gui/test_pcl_helloworld.pas is wired and carries the assertions
test/kwpadprobe.pas  Pascal unit imported by test/test_nilpy_keyword_call_tuple_on_a_skipped_default.npy (wired, test-nilpy) via `import 'kwpadprobe.pas' as kw`
test/strhelperprobe.pas  Pascal unit imported by test/test_nilpy_str_method_vs_pascal_string_helper.npy (wired, test-nilpy) via `import 'strhelperprobe.pas' as probe`

# The CPython differential for the minidom shim. It PASSES under CPython today
# (34 checks against the real xml.dom.minidom) and is banked so its expectations
# are recorded against the oracle rather than re-derived later. It is deliberately
# NOT wired because lib/rtl/mimic_xml_dom_minidom.py cannot be built: as a .py in
# lib/rtl it HANGS THE COMPILER FOREVER (100% CPU, flat RSS, no output), which
# would take out every lane running a library gate. The implementation is parked
# beside it as mimic_xml_dom_minidom.py.parked.
# Wire this in the commit that closes
# bug-n-a-class-with-two-definitions-of-one-method-hangs-the-compiler-forever
# and delete this line -- the two Makefile lines are given in
# feature-b-a-real-minidom-is-an-implementation-not-a-shim.

# --- PARKED: not wired HERE, YET, and a live ticket owns that ---
# A park is not an exemption. It borrows the named ticket's authority to say
# "not yet", and check_test_wiring REFUSES it the moment that ticket leaves an
# open folder — so these files return to the census when their owner is done
# rather than staying hidden behind a sentence nobody re-reads.
# A path ending in `/` covers everything beneath it: 37 rows saying one thing is
# how a real census becomes unreadable and a gate becomes permanently red.
test/wasm/  parked:feature-target-wasm the wasm backend and its slice tests are developed in a standalone checkout on branch `wasm`, which is what that ticket records; nothing on master references this tree except a generator inside it, and nothing here should wire it until that work lands

# --- positive controls for the -dPXX_HEAP_DEBUG instrumentation ---
# Not tests: they assert no output and pass trivially without their -dPLANT*
# define, so wiring them would add two green rows that can never fail. They are
# run by hand, twice each, when the heap instrumentation is changed -- a
# modified instrument is a new instrument and its silence must be re-earned.
# Committed because the commit messages for kinds 8/9/10/11 cite them, and a
# control nobody can find is a control nobody can re-run.
test/test_heapdbg_control_stale_string.pas  positive control for PXXStrDecRef's stale-handle check (kind 8); needs -dPXX_HEAP_DEBUG -dPLANTDEC and a non-x86-64 target, so nothing runs it directly
test/test_heapdbg_control_stale_dynarray.pas  positive control for the dynarray stale-handle checks (kinds 10/11) and the implausible-size branch; needs -dPXX_HEAP_DEBUG -dPLANTDYN or -dPLANTDYN2, so nothing runs it directly

# --- probes for work that has NOT landed; wiring them would gate on a feature
# --- that does not exist yet, which is a red row that says nothing new ---
# MEASURED 2026-09-01 before exempting, rather than inferred from the ticket:
# built both files with --emit-obj and linked them. `take_pair` (a struct BY
# VALUE as a PARAMETER, step 1) prints the right answer -- that half landed.
# `make_pair` (a struct RETURNED by value, step 2) SEGFAULTS. The identical
# pair built entirely with gcc prints `take 307` / `make 4 9`, so the source is
# right and the gap is ours. Step 2 is enumerated in
# devdocs/progress/done/bug-a-c-a-by-value-struct-parameter-is-passed-as-a-pointer-to-every-c-abi-callee.md
# and rests on the still-open bug-a-a-record-parameters-type-is-not-resolved-
# when-its-slot-is-sized. Wire this pair the day step 2 lands -- it is already
# the acceptance test for it.
test/c_abi_struct_byval_main.c  probe for step 2 of the C by-value struct ABI (a struct RETURNED by value), which has not landed: measured 2026-09-01, take_pair is correct and make_pair segfaults. Blocked on bug-a-a-record-parameters-type-is-not-resolved-when-its-slot-is-sized; wire it when step 2 lands
test/c_abi_struct_byval_pxx.c  the pxx-compiled half of the pair above; same reason

# MEASURED 2026-09-02 before exempting, and it is the reason the file exists:
# main thread + one pthread_create'd thread, 300k `try/except` each, 3 runs of 3
# print `Unhandled exception`. The control is in the same binary -- the identical
# 600k of work with the worker called DIRECTLY instead of through
# pthread_create is 3 of 3 clean -- so this is the thread, not the loop. At 2k
# each, one run of three produced no output at all.
# A libc pthread never runs the clone stub that zeroes the TLS block, so it
# inherits its creator's exception chain head. Wire this the day
# decide-a-a-foreign-thread-needs-its-own-tls-block-and-the-bounds-are-the-hard-part
# is answered and the install lands -- it is already the acceptance test for it.
test/test_foreign_thread_exception_chain.pas  a libc pthread inherits its creator's exception chain and an exception escapes its own try; measured failing 3 of 3. Blocked on bug-a-the-exception-chain-fix-is-defeated-by-a-libc-pthread and the decide- it names; wire it when a foreign thread gets its own TLS block

# A FIXTURE UNIT, not a test: it exists only to be one hop further away than
# the directive that asks its type's width. prepasserr_mid.pas holds the width
# ladder and test_p_a_prepass_cannot_raise_an_error_directive.pas runs it; this
# file is the third level, and the third level IS the defect -- with the type
# declared in prepasserr_mid the same ladder answers correctly, so a two-unit
# fixture would pass on the unfixed compiler and prove nothing.
test/prepasserr_base.pas  fixture unit for test_p_a_prepass_cannot_raise_an_error_directive.pas: declares the type ONE UNIT FURTHER than the directive, which is the variable the test turns on; nothing runs it directly by design
test/discardlist.pas  helper unit imported by test_a_discarded_class_result_from_a_pascal_call_is_released.npy (test-nilpy)
test/nilpy_inline_boxed_unit.pas  fixture: the Pascal unit test/test_nilpy_inline_boxed_args.npy imports (-Futest); run by that row
test/mpy_drivers/m_ads1x15.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged ads1x15 driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_drivers/m_bh1750.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged bh1750 driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_drivers/m_bme280.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged bme280 driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_drivers/m_dht.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged dht driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_drivers/m_ds18x20.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged ds18x20 driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_drivers/m_ds3231.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged ds3231 driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_drivers/m_hcsr04.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged hcsr04 driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_drivers/m_ina219.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged ina219 driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_drivers/m_max7219.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged max7219 driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_drivers/m_mpu6050.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged mpu6050 driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_drivers/m_neopixel.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged neopixel driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_drivers/m_sdcard.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged sdcard driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_drivers/m_sh1106.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged sh1106 driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_drivers/m_ssd1306.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged ssd1306 driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_drivers/m_st7789.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged st7789 driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_drivers/m_tm1637.npy  census input: compiled by tools/mpy_driver_census.sh against the unchanged tm1637 driver that tools/install_lib_candidates.sh micropython-drivers fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched drivers
test/mpy_net/m_asyncio_streams.npy  census input: compiled by tools/mpy_driver_census.sh net against the unchanged asyncio_streams library that tools/install_lib_candidates.sh micropython-net fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched libraries
test/mpy_net/m_microdot.npy  census input: compiled by tools/mpy_driver_census.sh net against the unchanged microdot library that tools/install_lib_candidates.sh micropython-net fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched libraries
test/mpy_net/m_mqtt_as.npy  census input: compiled by tools/mpy_driver_census.sh net against the unchanged mqtt_as library that tools/install_lib_candidates.sh micropython-net fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched libraries
test/mpy_net/m_ntptime.npy  census input: compiled by tools/mpy_driver_census.sh net against the unchanged ntptime library that tools/install_lib_candidates.sh micropython-net fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched libraries
test/mpy_net/m_uaiohttpclient.npy  census input: compiled by tools/mpy_driver_census.sh net against the unchanged uaiohttpclient library that tools/install_lib_candidates.sh micropython-net fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched libraries
test/mpy_net/m_umqtt_robust.npy  census input: compiled by tools/mpy_driver_census.sh net against the unchanged umqtt_robust library that tools/install_lib_candidates.sh micropython-net fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched libraries
test/mpy_net/m_umqtt_simple.npy  census input: compiled by tools/mpy_driver_census.sh net against the unchanged umqtt_simple library that tools/install_lib_candidates.sh micropython-net fetches (gitignored); it reports a first wall, not a pass/fail, and cannot run without the fetched libraries
test/nilpy_destructor_box.pas  helper unit imported (via -Futest) by test_nilpy_the_last_reference_runs_the_pascal_destructor.npy; its destructors bump a counter the test prints
