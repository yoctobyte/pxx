# SPDX-License-Identifier: 0BSD
# Network census: library compiled unchanged (tools/mpy_driver_census.sh net).
from microdot import Microdot

app = Microdot()


@app.route("/")
async def index(request):
    return "hello from pxx"


app.run(port=8080)
