# The unit a getattr probe asks about. A PROC, a SYMBOL and -- deliberately --
# no `open_audio`, because the absent arm is half of what the fold decides.
CONST = 27

def present(a, b):
    return a + b
