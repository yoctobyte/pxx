# THE CONTROL, and it is the half that makes the pair mean something: the same
# diagnostic raised in the file the reader INVOKED must print no `in:` line.
# Without this row, a fix that printed the path unconditionally would pass the
# importer row while telling every reader the name of the file they just typed.
print(this_name_is_not_defined_anywhere)
