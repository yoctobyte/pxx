# The second colliding name, and a FLAT module rather than a package -- the two
# reach ParseUsesUnit's already-compiled guard by different spellings.
MARKER = 27


def who():
    return "shadow-textfile"
